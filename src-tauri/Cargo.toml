[package]
name = "kokobrain"
version = "1.0.1-alpha"
description = "Markdown second-brain app"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "kokobrain_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-fs = { version = "2", features = ["watch"] }
tauri-plugin-dialog = "2"
tauri-plugin-deep-link = "2"
tauri-plugin-clipboard-manager = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.8"
uuid = { version = "1", features = ["v4"] }

# Debug / metrics
sysinfo = { version = "0.33", default-features = false, features = ["system"] }

# Shared infrastructure
chrono = "0.4"
rusqlite = { version = "0.32", features = ["bundled"] }
sha2 = "0.10"

# File History
similar = "2.7"
percent-encoding = "2"

# Encrypted Notes
aes-gcm = "0.10"
rand = "0.8"
base64 = "0.22"
zeroize = "1"

# Semantic Search
ort = { version = "2.0.0-rc.11", features = ["download-binaries", "ndarray", "half"] }
half = "2.7"
ndarray = "0.17"
tokenizers = { version = "0.21", default-features = false, features = ["onig"] }
reqwest = { version = "0.12", features = ["stream"] }
futures-util = "0.3"
tokio = { version = "1", features = ["rt-multi-thread", "fs", "io-util"] }
tauri-plugin-http = "2"

<<<<<<< HEAD
=======
# LAN Sync
libc = "0.2"
mdns-sd = "0.12"
snow = "0.9"
argon2 = "0.5"
hkdf = "0.12"
hmac = "0.12"
subtle = "2"
globset = "0.4"
tokio-util = "0.7"
tracing = "0.1"

>>>>>>> fc03494 (feat(sync): implement TCP/Noise sync server with rate limiting and TOCTOU-safe I/O)
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-core-foundation = "0.3"
objc2-foundation = { version = "0.3", features = ["NSString"] }
objc2-security = "0.3"
security-framework = "3"
objc2-local-authentication = "0.3"
block2 = "0.6"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

