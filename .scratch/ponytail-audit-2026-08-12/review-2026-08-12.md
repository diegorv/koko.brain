# Review of the 2026-08-12 ponytail audit — adversarial re-verification

Date: 2026-08-12. Requested rule: nothing in the audit may be assumed true; deep dive by priority order.

**Method:** 16 priority-ordered clusters covering all 71 findings. Stage 1: an opus-5 agent per cluster
re-derived every claim from scratch (symbol sweeps across src/, src-tauri/, e2e/, scripts/, configs,
capabilities, pnpm-lock/cargo tree; CLAUDE.md + 30 ADRs as kill criteria). Stage 2: a fable-5 agent per
cluster attacked every stage-1 verdict with its own searches. Stage 3: 5 opus/fable disagreements were
settled by independent arbiter agents that re-ran the decisive checks. 37 agents, 1,201 tool calls,
~3.55M subagent tokens, 34 min. Read-only: nothing was applied.

**Tally: 27 CONFIRMED | 43 PARTIAL | 1 REFUTED** (of 71). Arbitrated disputes: #11, #18, #31, #64, #67.

Raw per-finding output (full evidence text): `review-raw.json` in this directory.

## Executive summary

- **The audit's dead-code detection is essentially sound**: 70 of 71 findings have a true core claim.
  Only #57 (`getWatcherCounters`) was refuted outright — the audit's decisive verification sentence
  ("ADR 0017 does not mention the counters accessor anywhere — I grepped it") is false: ADR 0017 is
  `status: active` and names `getWatcherCounters()` verbatim in its Consequences section, and the
  proposed "tests can assert effects instead" replacement is not equivalent (both models refuted independently).
- **But only 27 findings are safe to apply exactly as written.** The dominant systemic defect (~30+ of the
  43 PARTIALs): the audit filtered `src/tests` out of its evidence greps and never lists the mandatory test
  collateral, so applying the finding as written fails `pnpm check` + `pnpm vitest run` — the CLAUDE.md
  rule-6/11 commit gate. Worst case #1: the 11 parser files are also imported by 13 `combined-*.test.ts`
  files (469 call sites, 6,102 lines), which the audit's broken `grep -v '/src/tests/'` filter missed.
- **One finding is dangerous as written**: #18's header range `373-411` includes line 411 =
  `duplicateNode`, which is LIVE (CanvasInner.svelte:38, :350). Deleting by the header range breaks canvas
  duplication. The correct range (374-408) appears only in the entry's Verification body.
- **One deletion has an off-main landmine**: #67 (cargo `uuid`) is dead on main, but the pushed, unmerged
  `feature/p2p-sync` branch (tip 4b64de7) calls `uuid::Uuid::new_v4()` at src-tauri/src/sync/engine.rs:321
  and inherits main's Cargo.toml line — deleting it on main leaves that branch compile-broken at merge time.
- **#69 (objc2-core-foundation) is not the no-op the audit claims**: kokobrain is the sole enabler of the
  crate's `default` feature; removing the line flips 27 features (24 CF* + block2 + dispatch2 + libc) for
  the whole build. Safe in principle (depsSaved 0 is correct) but must be validated with a real cargo build.
- **Deps verdict**: npm `codemirror`, `@fortawesome/fontawesome-svg-core`, `@types/katex` — all three
  CONFIRMED clean removals. Cargo `percent-encoding` CONFIRMED; `uuid` and `objc2-core-foundation` PARTIAL
  (see above). #28 (`watch` feature) PARTIAL: actually saves 2 crates (notify-debouncer-full, file-id),
  not 0, but active ADR 0017 describes the plugin-fs watch design and must be superseded in the same change.
- **The -2861 headline is misleading in both directions**: several counts are wrong (#10 is 123 not 115,
  #20 ~34 not 30, #29 ~-30 not -20, #37 realistically -25..-40 not -14, #47 -23 not -9), and the true total
  cut is much LARGER once mandatory test collateral is included (e.g. #1 alone: 628 source + 911 own-test
  lines + edits across 6,102 lines of combined tests; #3: +388 test lines; #7: +306-line Rust test file).
- **Bonus finds from this review** (not in the audit): `TagsPanel.svelte` is an entirely orphaned component
  (commit bf55080 moved tags to a virtual tab and forgot it — surfaced while verifying #65); `sortTree` in
  fs.logic.ts has zero production callers (surfaced in #39, the audit claims it "stays for the store");
  #12's deletion newly orphans `get_sample_chunks` + `ChunkSample` (~35 more lines); #13 missed a fifth
  dead export (the `processTemplate`/`evaluateExpression` re-export).
- **Sequencing constraints discovered**: #1 and #10 edit the same 13 combined test files — do together.
  #51, #59 and #27 propose competing homes for the same basename/path helpers — resolve as one change.
  #13 and #62 both edit the `today` surface in date.ts — sequence them. #67 should wait for (or coordinate
  with) the p2p-sync branch decision.

## Systemic audit defects (patterns across the 43 PARTIALs)

1. **Test collateral omitted everywhere.** Evidence greps excluded `src/tests`, `src-tauri/tests`, `e2e/`;
   remediation steps never include the test edits. Findings where a test file directly imports the deleted
   symbol (build-breaking as written): #1, #3, #7, #10, #13, #19, #20, #21, #23, #26, #29, #31, #42, #44,
   #46, #50, #51, #56, #57, #59, #62, #71. #46's evidence outright says "no test reference" while
   src-tauri/tests/search_fts_test.rs:231-245 calls the command directly.
2. **Compile-breaking unstated prerequisites in Rust findings.** #12 leaves the deleted fn registered in
   `generate_handler!` (lib.rs:333); #7 omits removing `pub mod text_search;` (search/mod.rs:4); #9 leaves
   3 now-unused imports in commands/vault.rs.
3. **Wrong line counts / ranges** (both directions): #8 (-165 vs real 360-line surface, widget math wrong),
   #10, #18 (range includes a live function), #20, #21, #29, #37, #47, #50, #64.
4. **"byte-identical" claims that are only semantically identical**: #27 (1 of 3 copies differs), #51
   (the named pair differs; other pairs are the identical ones), #59 (all four lines differ textually).
5. **Newly-created orphans the entries never mention**: #12 (get_sample_chunks/ChunkSample), #21
   (setViewQueryResult must go too), #23 (collection.store removeRecord), #39 (sortTree already dead).
6. **Doc surface omissions**: #9 (CLAUDE.md:262 lists the five commands), #28/#57 (active ADR 0017), #64
   (help/documentation/19-settings.md:245 lists all 12 decorator names), #47 (help doc still documents a
   removed throttle), #26 (ADR 0026:41 documents behavior no code implements).

## Verdict table

| # | Final | opus-5 | fable-5 | Arb | Finding |
|---|-------|--------|---------|-----|---------|
| 1 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` 11 regex/Lezer `find*Ranges` parser files superseded by the unified inline handler registry — e |
| 2 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` debug-composition.ts — a 251-line debug extension whose only two references in the codebase are |
| 3 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` Entire task-metadata.logic.ts — emoji task-signifier parser with zero production importers (Rus |
| 4 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `codemirror` meta-package dependency — never imported anywhere; the app imports the individual  |
| 5 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `@fortawesome/fontawesome-svg-core` dependency — the three FA icon-set packages are imported by |
| 6 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `@types/katex` devDependency — katex 0.18.1 ships its own declarations, so the @types stub is n |
| 7 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `search_vault` Tauri command and its only dependency `search/text_search.rs` — the frontend doe |
| 8 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` audio and video are three copy-pasted file pairs (parser, plugin, widget) differing only in the |
| 9 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` Five `#[tauri::command]`s registered in `invoke_handler` that the frontend never invokes — `get |
| 10 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` dead halves of two surviving parser files: findInlineMathRanges/InlineMathRange in math.ts and  |
| 11 | **CONFIRMED** | PARTIAL | CONFIRMED | yes | `delete` `scripts/settings-watcher.py` — orphan script, nothing invokes or documents it |
| 12 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `debug_semantic_embeddings` — a hardcoded diagnostic that embeds six fixed Portuguese/English q |
| 13 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` Five exports in periodic-notes.logic.ts plus the two lookup tables that feed them have zero pro |
| 14 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` KBDateTime hand-rolls a 269-line date library (quarter, ISO weekNumber, plus/minus, startOf/end |
| 15 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` The file read/write transform hook system in editor.hooks.ts: `FileReadTransform` + `FileWriteT |
| 16 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` block-math-widget.ts and inline-math-widget.ts are near-identical KaTeX widgets, each carrying  |
| 17 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `shrink` DataArray's Proxy gates on KNOWN_PROPS, a hand-maintained 42-entry Set that must be kept in syn |
| 18 | **PARTIAL** | CONFIRMED | PARTIAL | yes | `delete` canvas.logic.ts:373-411 — addNode, removeNode, updateNode, createEdge, addEdge, removeEdge (imm |
| 19 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` properties.logic.ts dead exports: detectPropertyType (L31-39), serializePropertyValue (L192-205 |
| 20 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` type-sidebar.logic.ts dead exports: isInsideSystemFolder (L57-68), countInbox (L361-368, a byte |
| 21 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` view-parse-cache.ts: getCachedViewYaml, getViewQueryResult, clearViewParseCache, clearAllViewPa |
| 22 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `components/ui/label` — a shadcn-svelte component nobody imports, plus its one-symbol barrel |
| 23 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` Scattered single dead exports: tags.service.ts:103 flushScheduledTagIndexRebuild, collection.se |
| 24 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` MetaBindNumberWidget and MetaBindDateWidget are identical classes whose only difference is the  |
| 25 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `extract_wikilinks_from_str` in entry.rs — a second hand-rolled `[[target]]` byte scanner livin |
| 26 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` inbox-workflow.logic.ts: isInboxEnabled and shouldNewNoteBeUnorganized are identity functions ( |
| 27 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` Three byte-identical copies of getRelativePath in features (search.logic.ts:146, quick-switcher |
| 28 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `watch` feature on tauri-plugin-fs plus the `fs:allow-watch` / `fs:allow-unwatch` capability gr |
| 29 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` Seven single-range clamp wrappers in settings.logic.ts (`clampFontSize`, `clampLineHeight`, `cl |
| 30 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `sanitizeMathHtml` — an exported KaTeX/MathML DOMPurify config that no caller uses; the two mat |
| 31 | **PARTIAL** | CONFIRMED | PARTIAL | yes | `delete` lifecycle-filter.logic.ts: excludeArchived, onlyArchived, countArchived unused; only buildArchi |
| 32 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `yagni` getPeriodicNoteTitle / getTemplatePathForPeriod / getDailyInlineTemplate are one-line property  |
| 33 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `VaultSearchMatch` interface — the TS mirror of the dead `search_vault` command's return type |
| 34 | **PARTIAL** | PARTIAL | PARTIAL |  | `stdlib` `find_triple_backtick(bytes, start)` — a 14-line manual byte loop looking for three consecutive |
| 35 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` extractAliasesFromContent in the wikilink completion logic — no completion path calls it |
| 36 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` 5 exported Decoration constants in styles.ts with no producer (inline marks moved to the Highli |
| 37 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` heatmapCalendar() and yearlyCalendar() open with the same ~44-line intensity-resolution preambl |
| 38 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `yagni` calendar.service.ts openCalendarFile() is a pure rename of openFileInEditor, and openOrCreateDa |
| 39 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` The entire file-tree sort-option feature: `changeSortOption`, the `sortVersion` counter, the `e |
| 40 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `removeThemeOverrides(theme)` in theme.service.ts — walks a theme's CSS vars and removes them f |
| 41 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` HorizontalRuleWidget and its `.cm-lp-hr` CSS rule — horizontal rules are rendered CSS-only via  |
| 42 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` todoist-bridge.logic.ts:41-52 mapPriorityFromTodoist — the inbound half of a bidirectional mapp |
| 43 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` frontmatter-icon.service.ts:49-59 setFrontmatterIconColor — writes _color to frontmatter; no ca |
| 44 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `clearRecentSaves(paths)` in editor.hooks.ts — the watcher was supposed to call it after consum |
| 45 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` stripCardDate and stripCardColor are byte-identical duplicates of removeCardDate / removeCardCo |
| 46 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `get_search_index_stats` — registered command returning the FTS document count; nothing asks fo |
| 47 | **PARTIAL** | PARTIAL | PARTIAL |  | `yagni` `shouldAutoCheckNow(autoCheck) { return autoCheck; }` — an identity function with one internal  |
| 48 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `stdlib` `split_key_value` — 9 lines to split a `key: value` line on the first colon |
| 49 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `isValidFolderName(name)` in settings.logic.ts — path-traversal validator for vault-relative fo |
| 50 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` tag-colors.logic.ts:30-37 getContrastTextColor — perceived-brightness contrast picker with no c |
| 51 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` Four separate copies of the same basename one-liner in core: `getFileName` in editor.logic.ts,  |
| 52 | **PARTIAL** | PARTIAL | PARTIAL |  | `native` Hand-rolled local YYYY-MM-DD formatting duplicated in collection/calendar.logic.ts:174 (formatD |
| 53 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `stdlib` hand-rolled local-time `YYYY-MM-DDThh:mm:ss` formatter (a `pad` helper plus a 4-line template-l |
| 54 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `flushPendingSaves()` in editor.service.ts — flushes the two autosave debounce timers. No produ |
| 55 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `closeVault()` in vault.service.ts — a 3-line wrapper that only forwards to `vaultStore.close() |
| 56 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` `Reranker::with_batch_size` — a builder override for `batch_size` that no caller ever chains |
| 57 | **REFUTED** | REFUTED | REFUTED |  | `delete` `getWatcherCounters()` in fs.watcher.ts — snapshot accessor for the watcher debug counters, rea |
| 58 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` core/types.ts — DecorationEntry has zero references and LineInfo just restates CodeMirror's own |
| 59 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` search.logic.ts:154 getFileName is byte-identical to backlinks.logic.ts:59 getNoteName (split ' |
| 60 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `stdlib` `is_char_boundary(s, byte_pos)` free function that re-wraps the std method with two redundant g |
| 61 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `parseDate` in date.ts — exported dayjs parse wrapper with no production caller |
| 62 | **PARTIAL** | PARTIAL | PARTIAL |  | `shrink` `today(format)` in date.ts is a duplicate of `formatNow(outputFormat, 0)` — same dayjs call, di |
| 63 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `SETTINGS_SECTIONS` — a flattened copy of SETTINGS_SECTION_GROUPS "for lookups and iteration".  |
| 64 | **PARTIAL** | CONFIRMED | PARTIAL | yes | `delete` 6 of the 12 live-preview decorator kill-switches in the Troubleshooting settings pane are no-op |
| 65 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` The `tagsVisible` setting: declared in settings.types.ts and given a default in the store, but  |
| 66 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` `ColorPresetName` type export in color-presets.ts — never referenced |
| 67 | **PARTIAL** | CONFIRMED | PARTIAL | yes | `delete` cargo `uuid` dependency (with the `v4` feature) — zero references in Rust source or tests |
| 68 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` cargo `percent-encoding` dependency — zero references in Rust source or tests |
| 69 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` cargo `objc2-core-foundation` dependency — fonts.rs declares its own `extern "C"` CoreFoundatio |
| 70 | **CONFIRMED** | CONFIRMED | CONFIRMED |  | `delete` file-icons IconPackMeta.iconCount — hardcoded 0 for all 12 packs and never read anywhere |
| 71 | **PARTIAL** | PARTIAL | PARTIAL |  | `delete` quick-switcher.logic.ts:4 barrel re-export `export { fuzzyMatch, type FuzzyMatchResult } from ' |

## Per-finding detail

CONFIRMED = safe to apply exactly as written. PARTIAL = core claim true, but apply only with the listed
corrections. REFUTED = do not apply.

### #1 — PARTIAL — 11 regex/Lezer `find*Ranges` parser files superseded by the unified inline handler registry — every export is referenced only by its own test file

- Path: `src/lib/core/markdown-editor/extensions/live-preview/parsers/bold.ts` | tag `delete` | claimed 628 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) FALSE CLAIM (material): "every export is referenced only by its own test file." Each of the 11 parsers is ALSO imported by one or more of the 13 `combined-*.test.ts` files living in the same test directory. Mapping (dead-symbol occurrence count per file): combined-inline-nesting 65, combined-list-inline 62, combined-heading-inline 51, combined-link-image-wikilink 44, combined-edge-cases 44, combined-highlight-comment 41, combined-inline-in-containers 39, combined-math-block-ref 27, combined-footnote 26, combined-blockquote-inline 22, combined-block-structures 18, combined-callout 16, combined-table-meta-bind 13 = 469 call sites over 6,102 lines. Every one of the 11 appears there, including the two the audit's spot-check would most likely have missed (horizontal-rule -> combined-edge-cases.test.ts:15; bold-italic -> 5 combined files).

2) THE REMEDIATION AS WRITTEN BREAKS THE BUILD. "delete the 11 files and their test files" leaves 13 combined test files with unresolvable `$lib/.../parsers/{bold,italic,...}` imports. vitest.config.ts `test.include = ['src/tests/**/*.test.ts']` picks all 13 up, so `pnpm vitest run` fails on module resolution and `pnpm check` reports missing modules — i.e. the CLAUDE.md rule-6 commit gate fails.

3) The combined files cannot simply be deleted alongside: every one of them also exercises SURVIVING parsers (link, math, comment, footnote, blockquote, callout, table, meta-bind-input, meta-bind-button, wikilink-embed, block-reference, frontmatter, fenced-code-block, mermaid, collection-block, plus extensions/wikilink/decoration.logic). Wholesale deletion silently drops that coverage. The finding needs an unstated extra step: strip the 469 dead-parser call sites from 13 files while preserving the live-parser assertions.

4) The -628 figure is source-only and understates the churn: the 11 own test files are another 911 lines (bold 88, italic 79, bold-italic 92, highlight 68, inline-code 74, strikethrough 58, heading 124, image 77, ordered-list 75, task-list 98, horizontal-rule 78), and the combined-file surgery touches 6,102 more.

5) ADR 0008 overstated slightly: it never names these files. It documents the consolidation generically ("Keep 8 separate inline `ViewPlugin`s (the pre-Híbrido-D state) … Replaced by one unified pipeline") and lists bold/italic/strikethrough/code as HighlightStyle-driven. It is supporting context, not an explicit "parsers/bold.ts is dead" statement — and crucially it does NOT protect them either, so the audit's conclusion on that axis stands.

**Corrections (fable-5 cross-exam):**

One error in the first agent's own corrections: it stated 'bold-italic -> 5 combined files'; the actual count is 7 — `rg -l "parsers/bold-italic'" src/tests | grep combined` → combined-blockquote-inline, combined-callout, combined-edge-cases, combined-heading-inline, combined-highlight-comment, combined-inline-nesting, combined-list-inline. All other first-agent figures (13 combined files, 469 call sites, 6,102 combined lines, 911 own-test lines, 628 source lines, per-file counts spot-checked: combined-inline-nesting 65, combined-list-inline 62) reproduced exactly. One caveat to sharpen: the 'pnpm check fails' part of correction #2 is asserted, not executed (read-only pass) — but the vitest module-resolution failure is structurally certain from the static imports plus the include glob, so the commit-gate conclusion stands regardless.

**Risk of applying as written:**

Applying the finding EXACTLY AS WRITTEN (delete 11 source files + 11 own test files) breaks the repo: 13 `combined-*.test.ts` files retain imports of the deleted modules, so `pnpm vitest run` fails at module resolution and `pnpm check` (svelte-check over src/tests) reports unresolved modules. That trips CLAUDE.md Quick Reference rule 6 — the commit gate — so the change cannot be committed as specified.

ZERO runtime risk: no production module imports any of the 11, so no live-preview behavior can change. `pnpm build` would pass; only check/vitest fail.

Correct procedure: (1) delete the 11 source files (-628) and their 11 own test files (-911); (2) in each of the 13 combined-*.test.ts files, remove the dead-parser import lines and the 469 dead-parser call sites while PRESERVING the assertions against surviving parsers (link, math, comment, footnote, blockquote, callout, table, meta-bind-input, meta-bind-button, wikilink-embed, block-reference, frontmatter, fenced-code-block, mermaid, collection-block, and extensions/wikilink/decoration.logic) — several combined files (e.g. combined-inline-nesting 65 hits, combined-list-inline 62) are majority dead-parser assertions and may end up nearly empty, in which case check whether the surviving remnant still asserts anything before deleting the file outright; (3) re-run `pnpm check` + `pnpm vitest run` + `pnpm build`.

Interaction to watch: combined-block-structures.test.ts imports `findMarkdownLinkRanges` (parsers/link) and `findInlineMathRanges` (parsers/math), which audit finding #10 separately proposes deleting. Findings #1 and #10 touch the same combined test files and must be sequenced together or they will conflict.

Docs/tests needing updates: none in docs/ or CLAUDE.md (no references found). ADR 0008 needs no edit — it already describes the unified pipeline as the current state.

### #2 — PARTIAL — debug-composition.ts — a 251-line debug extension whose only two references in the codebase are both commented out

- Path: `src/lib/core/markdown-editor/extensions/debug-composition.ts` | tag `delete` | claimed 251 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) MISSED CAVEAT — a prior verified remediation explicitly protected this file: `tasks/done/negative-patterns-remediation.md:21` reads "Leave `debug-composition.ts` (intentional dev tool) and `utils/log.service.ts`/`utils/debug.ts` (logger infra) untouched." That is not a caller (and per the ground rules not counted as one), and its scope was the console.*→appendLog task, but it is a recorded contrary judgment calling the file an intentional dev tool. The audit entry does not surface it.

2) UNSTATED EXTRA STEP — the audit's `-251` counts only the file. Deleting it also requires removing 4 more lines in `src/lib/core/markdown-editor/setup/editor-extensions.ts`: the 2-line explanatory comment at :28-29, the commented import at :30, and the commented call at :108. Left in place they reference a module that no longer exists.

3) UNSTATED EXTRA STEP — `composition-aware-bracket-matching.ts:62` ("This was diagnosed using a custom debug extension (see `debug-composition.ts` in this same directory)") plus :67 and :75 ("Disabling `bracketMatching()` via `dbg.bracketMatching()`") become dangling references to a deleted file. The audit lists the :62 hit as evidence but never says the comment must be updated.

4) The audit says "only two references ... both commented out"; strictly there are three non-self reference sites (two commented code lines + one prose block spanning :62/:67/:75) plus the tasks/ note.

**Corrections (fable-5 cross-exam):**

One addition beyond the first agent: the file is self-documenting as an on-demand dev tool — its header (lines 16-28) defines toggle Compartments for six suspect extensions and editor-extensions.ts:28-29 carries explicit 'Uncomment to enable' usage instructions, which strengthens the 'intentional dev tool' caveat from the tasks note. Everything else in the first agent's correction list re-verified exactly (tasks note line 21, the 3 non-self reference sites, the ~4 extra comment lines to remove in editor-extensions.ts).

**Risk of applying as written:**

No runtime, type-check, test or build risk: nothing imports the module, so it is already tree-shaken out of the bundle; there is no test file for it. `pnpm check` and `pnpm vitest run` are unaffected. The real risk is judgment, not breakage: this is the only in-repo tool for diagnosing IME/composition bugs, and `composition-aware-bracket-matching.ts:55-80` documents that a real WebKit dead-key composition bug was found with exactly this tool. Deleting it makes the next composition regression cost a re-write. No tests need updating; `editor-extensions.ts:28-30,108` and `composition-aware-bracket-matching.ts:62-75` comments need updating; no docs need updating.

### #3 — PARTIAL — Entire task-metadata.logic.ts — emoji task-signifier parser with zero production importers (Rust parses tasks at scan time via get_all_tasks_v2)

- Path: `src/lib/features/tasks/task-metadata.logic.ts` | tag `delete` | claimed 241 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) The -241 figure is only the source file. Applying the finding as written breaks the build: `src/tests/lib/features/tasks/task-metadata.logic.test.ts` (`wc -l` = 388) must be deleted too. The audit cites that file as evidence of non-use but never lists deleting it as a step, and 388 lines of collateral is larger than the -241 headline.

2) `src/tests/lib/features/tasks/tasks.logic.test.ts` is NOT a one-line import removal, which the entry implies by lumping it under "2 hits, BOTH in src/tests". Its `makeTask` helper builds the REQUIRED field at line 17: `metadata: parseTaskMetadata(overrides.text)`. `TaskItem.metadata` is non-optional (`src/lib/features/tasks/tasks.types.ts:16`), so the helper needs a substitute literal (e.g. `{ description: overrides.text, tags: [] }`) or the file fails `pnpm check` and every test in it (227 lines, 4 describe blocks) fails. Unstated prerequisite.

3) The parsing.rs caveat is understated. It is not "doc comments" in the abstract but 8 specific citations: parsing.rs:1177, 1178 (module header Mirrors list), 1207 (DATE_PATTERNS), 1228 (PRIORITY_PATTERNS), 1285 (buildRecurrenceRegex), 1325 (TAG_RE), 1338 (mapCheckboxChar), 1508 (parseTaskMetadata). Mitigating: parsing.rs:1174-1176 already cites `tasks.logic.ts::extractTasks/extractTasksFromSection/toggleTaskInContent`, all deleted in Phase 7.6, so the repo already carries dangling parity refs; this is cosmetic, not blocking.

4) Missed entirely: `tasks/todo/bug-hunt-fixes.md:67` is an OPEN (unchecked) bug entry `L38 tasks/task-metadata.logic.ts:59-60 — dependsOn regex drops all IDs after a space-separated comma`. Two consequences the audit does not flag. (a) The project rule "Never leave stale files" means that entry must be removed with the file. (b) More importantly, the bug is NOT fixed by deleting the TS copy — the Rust mirror has the identical regex at `src-tauri/src/vault/parsing.rs:1310-1316` (`r"{}\s*(\S+(?:\s*,\s*\S+)*)"`), and Rust is the production path. Deleting the TS file destroys the only tracked anchor for a live production defect.

5) Worth stating so nobody over-deletes: `src/lib/features/tasks/task-metadata.types.ts` MUST stay. It is imported by tasks.types.ts:1, todoist-bridge.logic.ts:2, todoist.service.ts:9, TodoistPopover.svelte:10 and named by parsing.rs / task.rs / vault-v2.types.ts parity comments.

**Corrections (fable-5 cross-exam):**

Two additions beyond the first agent: (1) tasks/todo/performance-architecture-refactor.md:96 also names task-metadata.logic.ts (records the Phase 7.6 deletion of its serializer/inverse helpers) — artifact only, no action, but it is a second stale-plan mention to be aware of under the 'never leave stale files' rule. (2) help/examples/collection-features/sprint-board.collection:43 contains `formula.isOverdue` — verified coincidental (user-defined formula column name, data-driven; collection feature source has zero `isOverdue` occurrences), record it so a future grep doesn't mistake it for a caller.

**Risk of applying as written:**

Applied literally (delete only the 241-line source file), both frontend gate commands fail immediately: `pnpm check` errors on two unresolved `$lib/features/tasks/task-metadata.logic` imports, and `pnpm vitest run` fails the whole task-metadata suite plus every test in tasks.logic.test.ts (makeTask throws on the missing symbol). Done correctly the risk is near zero: no production code path touches these symbols, all runtime task metadata arrives from Rust via get_all_tasks_v2 / get_tasks_in_section_v2, and ADR 0025 explicitly targets "zero TS metadata indexers". Required extra work: delete src/tests/lib/features/tasks/task-metadata.logic.test.ts (388 lines); patch makeTask in src/tests/lib/features/tasks/tasks.logic.test.ts:9,17; rewrite or drop the 8 parity citations in src-tauri/src/vault/parsing.rs; remove the L38 line from tasks/todo/bug-hunt-fixes.md AND re-file that dependsOn regex bug against src-tauri/src/vault/parsing.rs:1310-1316 before it disappears. No Rust rebuild needed (comments only), no e2e impact, no config or capability impact.

### #4 — CONFIRMED — `codemirror` meta-package dependency — never imported anywhere; the app imports the individual `@codemirror/*` packages directly

- Path: `package.json:57` | tag `delete` | claimed 1 +1dep lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

None to runtime or types: nothing resolves the bare specifier, and @codemirror/lint stays in the tree via language-data->lang-javascript so no phantom-dependency breakage. Prerequisites the entry does not spell out: the edit requires a `pnpm install` to regenerate pnpm-lock.yaml (the -1 line figure counts package.json only; the lock loses ~6 lines across the importer/package/snapshot blocks), and per CLAUDE.md Quick Reference rule 6 a package.json/pnpm-lock change is a frontend change, so `pnpm check` + `pnpm vitest run` + `pnpm build` must all run before the commit. ADR 0014 layer 2 (scripts/pre-commit-dep-age.sh) only inspects newly added/changed lock entries, so a pure removal cannot trip the 7-day quarantine. No test or doc needs updating.

### #5 — CONFIRMED — `@fortawesome/fontawesome-svg-core` dependency — the three FA icon-set packages are imported by file-icons.icon-data.ts, the core runtime never is

- Path: `package.json:31` | tag `delete` | claimed 1 +1dep lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Safe: the three icon packs are self-contained data modules and stay declared, and svg-core supplies nothing else in the tree. Same unstated prerequisites as #4 — `pnpm install` to regenerate pnpm-lock.yaml (importer lines 43-45 plus package entry 441-443 and snapshot 2667-2669 disappear; @fortawesome/fontawesome-common-types stays, pulled by the three packs) and the full frontend gate (`pnpm check` + `pnpm vitest run` + `pnpm build`) per CLAUDE.md rule 6. No test file references FA, no doc update needed.

### #6 — CONFIRMED — `@types/katex` devDependency — katex 0.18.1 ships its own declarations, so the @types stub is never consulted

- Path: `package.json:93` | tag `delete` | claimed 1 +1dep lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Zero type-resolution risk: the stub is already unreachable, so `pnpm check`/`svelte-check`/vitest behaviour is unchanged, and the dep genuinely leaves node_modules (nothing else installs it). Prerequisites the entry omits, same as #4/#5: regenerate pnpm-lock.yaml with `pnpm install` (importer 224-226, package entry 1031-1032, snapshot 3233 drop) and run the full frontend gate `pnpm check` + `pnpm vitest run` + `pnpm build` per CLAUDE.md rule 6. One forward-looking caveat: if anyone later imports `katex/contrib/auto-render` or `katex/contrib/mhchem`, katex 0.18.1's exports map has no `types` condition for those subpaths, so the @types/katex/contrib stubs would then be the only declarations available — today no such import exists.

### #7 — PARTIAL — `search_vault` Tauri command and its only dependency `search/text_search.rs` — the frontend does full-text search via `search_fts` (FTS5) and never in

- Path: `src-tauri/src/commands/search.rs` | tag `delete` | claimed 184 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1. EVIDENCE ERROR (material): the audit's "3 hits total" grep skipped `src-tauri/tests` and `e2e/`. A dedicated 306-line integration test file exists — src-tauri/tests/commands/search_test.rs (`wc -l` = 306), wired in by `mod search_test;` at src-tauri/tests/commands/mod.rs:3, opening with `use kokobrain_lib::commands::search::search_vault;`. It must be deleted along with that mod line, and the finding never mentions it.
2. REPLACEMENT INCOMPLETE: it says to drop `pub mod search` from commands/mod.rs but omits `pub mod text_search;` at src-tauri/src/search/mod.rs:4. Deleting text_search.rs without that line is an immediate compile error.
3. E2E CASCADE UNMENTIONED: e2e/mocks/tauri-core.ts:33-41 (`handleSearchVault`) + :253 (`search_vault: handleSearchVault`) + e2e/mocks/virtual-fs.ts:300-350 (`searchVault`, ~50 lines) + its local `VaultSearchMatch` interface at virtual-fs.ts:32 all go dead. Extra proof they are already dead: the mock takes (path, query, caseSensitive, wholeWord, useRegex) while the real command is `search_vault(vault_path: String, query: String)` — the signatures have diverged and nobody noticed.
4. DOC UPDATE UNLISTED: docs/adr/0011-sqlite-fts5-wal-local-search.md:41 names `search.rs` in "Commands (src-tauri/src/commands/): search.rs, search_index.rs, history.rs, semantic.rs, db.rs". The audit's Verification notes the ADR is stale but the Replacement line never lists the ADR edit as a step. (Not protective — the ADR is about SQLite/FTS5 and search.rs does a raw filesystem scan, so it does not save the finding.)
5. LINE-COUNT UNDERSTATEMENT: -184 counts production lines only. Real repo deletion is 85 + 262 + 306 = 653 lines plus 3 declaration lines. The headline number hides that an entire 306-line test suite disappears.
6. UNSTATED COUPLING: src/lib/core/filesystem/fs.types.ts:39-53 (`/** A single search match from the Rust search_vault command */ export interface VaultSearchMatch`) is the TS mirror; the audit files it separately as #33 but #7 does not cross-reference it.
7. CONTEXT: tasks/done/test-gap-closure-phase1.md documents search.rs as a coverage gap that a prior task closed — search_test.rs was written deliberately. It is a task artifact, not a protective doc, but the finding deletes work that was consciously added.

**Corrections (fable-5 cross-exam):**

One imprecision in the first agent's own evidence: their claim that `is_markdown_filename` is 'still used by commands/vault.rs, files.rs, search_index.rs, db.rs' is wrong — `rg -nw is_markdown_filename src-tauri` shows its only other production callers are inside utils/fs.rs itself (:166, :216, :272, within collect_markdown_paths* helpers); the command files they listed only call validate_vault_path. Conclusion unchanged (deleting search.rs orphans no util). Also confirmable context nuance: tasks/done/test-gap-closure-phase1.md:73 claims search_vault 'Has E2E tests', but since no frontend invoke exists, the e2e mock dispatch entry can never fire — that claimed e2e coverage was already illusory, reinforcing (not weakening) the finding.

**Risk of applying as written:**

Applying the finding EXACTLY AS WRITTEN does not compile, twice over: (a) `pub mod text_search;` at src-tauri/src/search/mod.rs:4 points at a deleted file → `cargo build` fails; (b) src-tauri/tests/commands/search_test.rs:1 `use kokobrain_lib::commands::search::search_vault;` → the integration-test crate fails to compile, so `cargo test --manifest-path src-tauri/Cargo.toml` (the mandatory Rust pre-commit gate in CLAUDE.md rule 6) errors out before running anything. Correct step list: delete src-tauri/src/commands/search.rs, src-tauri/src/search/text_search.rs, src-tauri/tests/commands/search_test.rs; remove src-tauri/src/lib.rs:316, src-tauri/src/commands/mod.rs `pub mod search;`, src-tauri/src/search/mod.rs:4, src-tauri/tests/commands/mod.rs:3; then optionally e2e/mocks/tauri-core.ts:33-41+253 and e2e/mocks/virtual-fs.ts:32+300-350. Docs to update: docs/adr/0011:41. No runtime/user-facing regression — no production code path reaches search_vault, and text search stays on search_fts.

### #8 — PARTIAL — audio and video are three copy-pasted file pairs (parser, plugin, widget) differing only in the string "audio"/"video"

- Path: `src/lib/core/markdown-editor/extensions/live-preview/parsers/audio.ts` | tag `shrink` | claimed 165 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

Evidence-block arithmetic is wrong: widgets are 25+25 = 50 lines (AudioWidget 189-213, VideoWidget 216-240), not '27+32'; total is 162+148+50 = 360, not 369 (the audit's own Verification block already corrected this, but the header -165 was left). The headline 'differing only in the string audio/video' is false one layer down: `.cm-lp-audio` uses width:100% while `.cm-lp-video` uses maxWidth:100%. The Replacement omits the 376 lines of asymmetric duplicated tests that must be reworked, and omits the e2e spec that seeds an audio tag.

**Corrections (fable-5 cross-exam):**

Three additions beyond the first agent. (1) `extractSrcAttr` is not one shared helper: each parser file carries its own module-private copy (parsers/audio.ts:16 and parsers/video.ts:16, identical bodies) — the first agent's 'both use the same extractSrcAttr' implied sharing; the duplication actually strengthens the merge case (one copy dies). (2) `rg -n 'AudioWidget|VideoWidget' src/tests` → 0 hits: no test constructs either widget class, so the widget half of the merge forces no test rewrite — the 376 flagged test lines are exclusively the parser/plugin suites. (3) The concrete audio-only test cases a parameterized merge must preserve are: parsers 'handles single quotes for src attribute', 'returns correct positions for multi-line block', 'returns null for multi-line audio without src anywhere'; plugins 'does not decorate when cursor is inside multi-line block' (verified by listing every it() in the four files).

**Risk of applying as written:**

Low if implemented tag-parametrically; three unstated items. (1) CSS is NOT a pure audio/video rename: styles.ts:266 `.cm-lp-audio { width: '100%' }` vs styles.ts:277 `.cm-lp-video { maxWidth: '100%' }`. The merged widget must keep deriving `cm-lp-${tag}` / `cm-lp-${tag}-wrapper`; collapsing to one `cm-lp-media` class silently changes the audio player's width behavior. (2) Four near-duplicate test files must be rewritten and they are asymmetric: `wc -l` -> parsers/audio.test.ts 122 vs video.test.ts 93, plugins/audio-plugin.test.ts 87 vs video-plugin.test.ts 74; the audio suites carry cases video lacks (self-closing `endIdx`, `closeTo` assertion, 'does not decorate when cursor is inside multi-line block'). A naive parameterized merge drops that coverage; CLAUDE.md rule 11 makes updating them mandatory. The audit's Replacement line never mentions tests. (3) e2e/specs/embed-widgets.spec.ts:33-35 seeds `<audio src="clip.mp3"></audio>` in the host note (no assertion on `.cm-lp-audio`), so `bash scripts/e2e.sh` should be re-run. Doc drift: docs/LIVE-PREVIEW.md:11 ('plugins/ # ViewPlugin implementations (one per feature)') and its 'Adding a New Plugin' list, plus ADR 0008 Consequences ('Adding a block construct = adding a file + a registration line') describe the per-file shape — neither forbids a factory, so this does not refute the finding.

### #9 — PARTIAL — Five `#[tauri::command]`s registered in `invoke_handler` that the frontend never invokes — `get_notes_with_tag_v2`, `get_tasks_in_path_v2`, `query_not

- Path: `src-tauri/src/commands/vault.rs:566` | tag `delete` | claimed 146 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1. "ADR 0025 ... do name these commands" is false — ADR 0025 never names any of the five (rg over docs/ = 0 hits); it only says "tag/task/property reads and writes" at 0025-rust-vault-index.md:72-74. Only CLAUDE.md:262 enumerates them.
2. Evidence scope omits e2e/: e2e/mocks/tauri-core.ts:84-85,265 registers a mock handler for get_notes_with_tag_v2 backed by e2e/mocks/vault-index.ts:341; e2e/mocks/vault-index.ts:409 getNoteProperties is a further orphan. Neither is a production caller, but both must be removed with the finding.
3. Replacement step list is incomplete. Beyond the 5 commands + 5 lib.rs lines + 5 lookups it must also: (a) drop `Task` from the use at commands/vault.rs:6, `JsonValue` at :9, `BTreeMap` at :11 — all three are used only by the deleted commands; (b) edit CLAUDE.md:262 to drop the five names from the enumerated read surface; (c) edit the stale doc comment at src/lib/types/vault-v2.types.ts:65 (the Verification prose mentions it, the action list does not); (d) drop the index.rs:175 doc comment naming query_notes_by_property / get_property_values; (e) delete the e2e mock handler + helpers.
4. Line-count components are wrong although the 146 total is right: commands are 13/13/14/13/13 = 66 (not 64) and lookups are 17/8/21/19/10 = 75 (not 77).
5. Test caveat wrong: 12 test fns in src-tauri/tests/vault_index_test.rs (not "~30 tests"), and the audit misses 7 more #[cfg(test)] fns inside src-tauri/src/vault/index.rs (1701,1713,1762,1773,1786,1792,1806) — 19 total, and the in-file 7 break compilation of the lib test target.
6. Unstated consequence: removing lookup_notes_by_property + lookup_property_values leaves `properties_index` (index.rs:183) with no production reader — the accessor at :213 is test-only — while it is still maintained on every build/update/remove. The real removable surface is larger than -146; either take it in the same change or accept a write-only reverse index.
7. Doc-intent tension (the parent asked for this explicitly): CLAUDE.md:262 does enumerate all five names, but the sentence's asserted decisions are "metadata lives in Rust VaultIndex" and "the TS side does not mirror it — every read goes through a *_v2 command". Both survive the cut untouched; the list is a description of the surface as built, not a decision to retain uninvoked endpoints, and the other 9 names in the same list all have live callers. So the doc does not protect the commands — but it becomes factually wrong the moment they are deleted, which makes updating it a mandatory part of the change rather than optional cleanup.

**Corrections (fable-5 cross-exam):**

Beyond the first agent's corrections (all of which I re-verified as accurate), three additional caveats it missed: (a) src-tauri/src/vault/index.rs:442 — the comment inside the entry-removal logic reads '(so `lookup_property_values` for a removed key returns [])' and becomes a stale reference to a deleted method; (b) the 'Phase 8 — Property lookups' section header at index.rs:892-894 becomes orphaned, since all three methods under it (lookup_notes_by_property, lookup_property_values, lookup_note_properties) are deleted, while the Phase 7 header at 810-811 survives via lookup_all_tags/lookup_all_tasks; (c) tasks/done/test-gap-closure-phase1.md:78 asserts src-tauri/tests/commands/vault_test.rs has Rust-side tests for these five commands — that is stale (zero hits for any of the five names anywhere under src-tauri/tests/), so no command-level Rust tests need deleting, only the 19 lookup_* test fns the first agent listed. Also note the first agent's ERROR 5 phrasing 'the in-file 7 break compilation of the lib test target' is correct and I confirmed the fns call the deleted methods directly (e.g. index.rs:1708 idx.lookup_notes_with_tag("RUST")).

**Risk of applying as written:**

Moderate, entirely from the unlisted extra steps rather than from the deletion itself.

Guaranteed breakage if applied literally as written: `cargo test --manifest-path src-tauri/Cargo.toml` fails to COMPILE. The 7 #[cfg(test)] fns inside src-tauri/src/vault/index.rs (1701, 1713, 1762, 1773, 1786, 1792, 1806) call the deleted lookups from inside the same crate, and the 12 fns in src-tauri/tests/vault_index_test.rs (1052-1088, 1134-1144, 1407-1434, 1441-1450, 1454-1468) do the same across the integration boundary. All 19 must be deleted in the same commit. This is required by CLAUDE.md Quick Reference rule 6 and rule 11 (test files must be updated alongside source).

Build warnings, not failures: three now-unused imports at src-tauri/src/commands/vault.rs:6 (Task), :9 (JsonValue), :11 (BTreeMap). No [lints] section in src-tauri/Cargo.toml and no #![deny] in lib.rs, so unused_imports stays warn-level; still leaves a dirty build.

Docs needing updates: CLAUDE.md:262 (enumerates all five in the v2 read surface), src/lib/types/vault-v2.types.ts:65 (doc comment naming get_notes_with_tag_v2), src-tauri/src/vault/index.rs:175 (doc comment naming query_notes_by_property and get_property_values). Skipping these leaves the project's own architecture doc asserting commands that no longer exist — a live-ammunition trap for the next agent that reads CLAUDE.md as authoritative.

Orphaned test infrastructure: e2e/mocks/tauri-core.ts:84-85 and :265 plus e2e/mocks/vault-index.ts:341 (and :409, already orphaned). Harmless if left — the mock dispatches by command name and the frontend never sends these — but it is dead code the finding creates.

No runtime or user-facing risk: no production TS path invokes any of the five, no dynamic invoke exists, no capability grant or generated schema references them, and every UI surface they nominally serve is already fed by a different live command (get_all_tags_v2 aggregates carry filePaths; get_all_tasks_v2 / get_tasks_in_section_v2 carry tasks; get_all_property_records carries properties). Frontend gate (pnpm check / vitest / build) is unaffected — no src/ file changes except the one doc comment.

Residual debt if applied as written: properties_index stays maintained with no production reader.

### #10 — PARTIAL — dead halves of two surviving parser files: findInlineMathRanges/InlineMathRange in math.ts and findMarkdownLinkRanges/MarkdownLinkRange + findAutolink

- Path: `src/lib/core/markdown-editor/extensions/live-preview/parsers/link.ts:4` | tag `delete` | claimed 115 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) LINE COUNT WRONG (understated). Measured by reading the files: math.ts dead block = lines 4-13 (interface+jsdoc+blank, 10) + lines 28-53 (jsdoc+fn+blank, 26) = 36 lines → math.ts 102→66. link.ts dead = 4-12 (9) + 13-52 (40) + 53-62 (10) + 63-90 (28) = 87 lines → link.ts 198→111. TOTAL 123 lines, not 115.

2) MAJOR OMISSION — the audit says nothing about test churn, but 14 test files import/call the three dead functions and will fail `pnpm vitest run` the moment they are removed:
  - findInlineMathRanges (12 files): math.test.ts + combined-{footnote,math-block-ref,highlight-comment,heading-inline,inline-in-containers,block-structures,link-image-wikilink,callout,blockquote-inline,list-inline,inline-nesting}.test.ts
  - findMarkdownLinkRanges (13 files): link.test.ts + the 12 combined-*.test.ts (adds combined-table-meta-bind)
  - findAutolinkRanges (6 files): link.test.ts + combined-{heading-inline,list-inline,callout,link-image-wikilink,blockquote-inline}.test.ts
  Whole describe blocks die: math.test.ts `describe('findInlineMathRanges')` lines 5-88; link.test.ts `describe('findMarkdownLinkRanges')` lines 10-100 and `describe('findAutolinkRanges')` lines 154-193 (~215 test lines), plus scattered assertions in the 12 combined-* files. CLAUDE.md Quick Reference rule 11 makes updating them mandatory before commit.

3) The Path field cites only `parsers/link.ts:4`; `parsers/math.ts` appears only in the title and has no Path entry.

4) Overlap not flagged: the combined-*.test.ts files are built almost entirely on the 11 legacy parsers that finding #1 proposes deleting, so #10 and #1 share the same test-corpus cleanup. Applying #10 alone means surgically editing 12 combined-* files that #1 would delete outright — sequence #1 first to avoid doing the work twice.

**Corrections (fable-5 cross-exam):**

None beyond the first agent's list — I verified all four of their corrections (123-line count, 14-file test churn with the per-symbol file counts matching exactly, Path field citing only link.ts, and the sequencing overlap with finding #1 since the combined-*.test.ts corpus is shared).

**Risk of applying as written:**

No production regression: inline math, markdown links and `<url>` autolinks all render through the unified inline registry handlers listed above, and none of the three dead functions is reachable from any plugin, widget, click handler, dynamic import or barrel. Risk is entirely in the test suite — deleting the exports without touching the 14 test files breaks `pnpm vitest run` immediately (14 files fail at import resolution / type-check, and `pnpm check` fails too). Also expect the `-115` figure in any plan derived from this entry to be wrong by 8 lines. No docs or ADRs need updating; ADR-0008 already describes the surviving architecture.

### #11 — CONFIRMED — `scripts/settings-watcher.py` — orphan script, nothing invokes or documents it

- Path: `scripts/settings-watcher.py` | tag `delete` | claimed 94 lines | opus-5: PARTIAL, fable-5: CONFIRMED, arbitrated

**Corrections (opus-5 deep dive):**

1) "Orphan script" overstates it. `git log -- scripts/settings-watcher.py` shows exactly one commit, b4a2d6b (2026-05-27, 2.5 months old), authored deliberately with the project's full Context/Solution/Behavior/Files commit format: it is an intentionally added maintainer debugging tool whose documentation was simply never written, not accumulated cruft. 2) The recommendation is questionable at the same laziness rung the audit is arguing from: the file costs nothing at runtime, in the bundle, in CI or in dependency surface (94 lines of stdlib-only Python nobody imports), and the cheaper fix for the stated defect ("nothing documents it") is one line in CLAUDE.md's Debugging section next to the existing log-watcher.py line, not deleting a working tool. 3) The entry's Area tag "deps/build" is a mislabel — the file touches neither dependencies nor the build. 4) The audit's own sibling-reference scoring cuts both ways: it proves the file is undocumented, not that it is unused (a manual dev tool run by hand leaves no repo references by construction).

**Corrections (fable-5 cross-exam):**

Carry one owner-judgment note (not an entry error): the script is a working manual dev tool restorable from git (b4a2d6b); an owner who uses it by hand may prefer adding a doc line next to log-watcher.py in CLAUDE.md's Debugging section instead of deleting — but that is a preference against the audit's deletion charter, not a defect in the finding.

**Risk of applying as written:**

Nothing in the build, CI, tests or runtime touches this file, so deletion cannot break anything — the risk is purely judgment. Corrections below are why this is PARTIAL rather than a clean confirm; if the owner still wants it gone, no accompanying test or doc edit is required (nothing references it) and `git rm scripts/settings-watcher.py` is the whole change, with no dependency or gate implications (Python file, outside the pnpm/cargo test matrix).

**Arbitration:**

Agent B was right; every one of Agent A's PARTIAL-justifying corrections fails under my own re-verification. (1) Reference sweep, run myself from the repo root with only node_modules/.git/.svelte-kit/target/dist/build/.scratch/tasks excluded (so hidden dirs .github, .vscode, .claude, plus package.json and src-tauri/tauri.conf.json were all traversed): `grep -rn "settings-watcher|settings_watcher"` returns exactly 2 hits, both in the file's own usage docstring (scripts/settings-watcher.py:9-10). No Makefile/justfile/husky/lefthook exists and no glob-style `scripts/*` invocation appears anywhere, so no runner can reach it without its literal name. `wc -l` = 94, exact. Deletion as written (`git rm scripts/settings-watcher.py`) breaks nothing: the script is stdlib-only (sys/time/json/difflib/pathlib), imports nothing from the app, and nothing imports or executes it. (2) The Area-tag "mislabel" — Agent A's only concrete factual correction — is wrong: findings.md's header states the Areas are the 6 finder-agent beats (deps/build, core, live-preview, features, plugins, rust+utils), my uniq count over the file confirms exactly those 6 values exist, and the other deps/build entries are package.json:31/57/93, Cargo.toml:24/36/51/68, and vite.config.js:22 — i.e., the beat that owns the whole build/tooling surface. scripts/ has no other possible home in the taxonomy, so the tag is beat routing, not an entry error. (3) The "deliberately authored maintainer tool" point is factually true (single commit b4a2d6b, 2026-05-27, co-authored by Claude Opus 4.7) but immaterial under the ground rules: documented intent means CLAUDE.md or docs/adr/, and both have zero mentions of settings-watcher (verified: grep of CLAUDE.md, docs/, README, .github → exit 1), in sharp contrast to sibling log-watcher.py (CLAUDE.md:165, docs/adr/0015-dual-logging-frontend-rust.md:45). A well-formatted commit message cannot protect a finding in a repo where docs/COMMITS.md mandates that format for every commit. (4) "Document it instead of deleting" is a recommendation preference, not an error, omission, or unstated prerequisite in the entry — and the entry's literal claims ("orphan script, nothing invokes or documents it", -94, replacement: nothing) are all true and acting on them exactly as written is safe. That meets the CONFIRMED bar precisely.

### #12 — PARTIAL — `debug_semantic_embeddings` — a hardcoded diagnostic that embeds six fixed Portuguese/English query-passage pairs and returns a formatted score report

- Path: `src-tauri/src/commands/semantic.rs:1067` | tag `delete` | claimed 83 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1. OMITTED CASCADE ("Replacement: nothing" is incomplete): semantic.rs:1129 `let sample = db::with_db(|conn| db::semantic_repo::get_sample_chunks(conn, 5))?;` is the ONLY production caller of `db::semantic_repo::get_sample_chunks` (defined src-tauri/src/db/semantic_repo.rs:307) and the only production consumer of the `ChunkSample` struct (semantic_repo.rs:29). After the deletion both are referenced solely by their own in-file `#[cfg(test)]` tests at semantic_repo.rs:548-575 (`get_sample_chunks_returns_limited_results`, `get_sample_chunks_empty`). No compile error (they are `pub` in a lib crate, so no dead_code warning fires), but for an over-engineering audit that is a newly-created ~35-line orphan the entry should have named. True cut is ~83 or ~118 lines depending on whether the cascade is taken.
2. The -83 count covers the fn body only; the lib.rs:333 registration line is a 84th line to remove (the audit does mention the registration in its evidence, just not in the count).
3. Minor: the audit says "Body spans lines 1063-1145" but the Path field says `semantic.rs:1067` — the fn keyword line, not the block start. Cosmetic inconsistency only.

**Corrections (fable-5 cross-exam):**

Sharpening the first agent's correction 2, which undersold the registration line as merely 'a 84th line to remove': the audit's Replacement field says 'nothing' and, unlike finding #7, lists NO step to drop the lib.rs registration. Deleting the fn while leaving `commands::semantic::debug_semantic_embeddings` inside generate_handler! at lib.rs:333 is a compile error (the macro references a now-missing path), so this finding has the same class of compile-breaking unstated prerequisite as #7 and #46 — it is only recoverable because the Evidence block happens to name lib.rs:333 as a grep hit.

**Risk of applying as written:**

Low. Applying the finding exactly as written compiles and leaves every test green: no test, no frontend invoke, no doc and no dynamic dispatch reaches it, and all three shared helpers it uses have other callers. Two soft costs: (a) it removes the only manual embedding-quality sanity check against known-good/known-bad pairs, with no replacement diagnostic — a judgement call, not a defect; (b) it silently creates dead code in db/semantic_repo.rs (get_sample_chunks + ChunkSample) unless that cascade is taken in the same change. Nothing user-facing regresses.

### #13 — PARTIAL — Five exports in periodic-notes.logic.ts plus the two lookup tables that feed them have zero production callers - referenced only from src/tests

- Path: `src/lib/plugins/periodic-notes/periodic-notes.logic.ts:13` | tag `delete` | claimed 72 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

(a) HEADLINE COUNT IS WRONG: the entry says "Five exports", but only FOUR exports are dead and only four are enumerated in its Evidence/Verification and in its removal spans (buildPeriodicNotePathForToday, buildAdjacentPeriodPath, getTodayPeriodicNoteTitle, buildPeriodicNotePathForDate). The "two lookup tables" are module-private consts, not exports, and are counted separately by the entry itself.
(b) FALSE LINE-SAVING CLAIM: the Verification says the removal frees "the now-unused `today` import from utils/date". It does not free a line. Line 2 is `import { today, formatToCapturingRegex } from '$lib/utils/date';` and formatToCapturingRegex is still used at logic.ts:316 (detectPeriodicNoteType), so the import statement survives with one identifier removed. Net line delta from that edit: 0.
(c) MISSED A FIFTH GENUINELY DEAD EXPORT: `export { processTemplate, evaluateExpression } from '$lib/utils/template';` at logic.ts:5-6 ("Re-export ... for backward compatibility"). `rg -n "processTemplate|evaluateExpression" src | grep -v utils/template | grep -v '^src/tests'` shows the only production consumers import straight from '$lib/utils/template' (note-creator.service.ts:8, deep-link.service.ts:25); features/collection/expression/evaluator.ts declares an unrelated same-named function. No file imports either symbol from periodic-notes.logic, and the logic test's import block (lines 3-21) does not list them either. That is 2 more dead lines the entry's spans omit.
(d) MISCOUNT IN THE VERIFICATION PROSE: it calls the importers "the 5 real importers" and then names six files.
(e) Line count 72 vs my re-measure of 73 (immaterial).

**Corrections (fable-5 cross-exam):**

Two supporting details beyond the first agent: (i) src/tests/lib/utils/template.test.ts:3 imports processTemplate/evaluateExpression directly from '$lib/utils/template', so deleting the dead re-export at logic.ts:5-6 breaks no test at all — the fifth-export deletion is even safer than the agent stated; (ii) the audit's span "85-91" for getTodayPeriodicNoteTitle includes blank line 85; the actual block is 86-91 (immaterial to the ~72-line total).

**Risk of applying as written:**

Low functional risk: the four exports and both consts are provably unreachable from production code, including the one dynamic `import()` path (wikilink-navigation.ts:28 destructures only detectPeriodicNoteType). Applying it as written leaves the build red until tests are updated, which the entry does not spell out: src/tests/lib/plugins/periodic-notes/periodic-notes.logic.test.ts must drop 4 named imports (lines 5, 6, 8, 12) and 4 describe blocks (line 59 buildPeriodicNotePathForToday, 67 buildAdjacentPeriodPath, 105 getTodayPeriodicNoteTitle, 176 buildPeriodicNotePathForDate) — ESM named imports of deleted exports fail at link time, so `pnpm vitest run` and `pnpm check` both break otherwise. Also, `today()` in $lib/utils/date loses its last consumer in this file but keeps others, so do not delete it. Per CLAUDE.md Quick Reference rule 6 the gate for this frontend-only change is `pnpm check` + `pnpm vitest run` + `pnpm build`; per rule 11 the test edit is mandatory, not optional. No docs/ADR update needed (no doc names these symbols).

### #14 — PARTIAL — KBDateTime hand-rolls a 269-line date library (quarter, ISO weekNumber, plus/minus, startOf/endOf, month-name arrays, token formatter) while dayjs is

- Path: `src/lib/plugins/queryjs/kb-datetime.ts` | tag `shrink` | claimed 70 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) "Public API (kb.date()) unchanged" is false as written. toFormat cannot be swapped for a token map + dayjs.format: dayjs's REGEX_FORMAT reinterprets D/d/a/A/H/h/m/s/M/Y/Z/S characters inside literal text and turns [..] into an escape, whereas the current replaceAll chain leaves all non-token text verbatim. Keep toFormat (and therefore MONTHS_LONG/MONTHS_SHORT, ~10 of the claimed 70 lines) hand-rolled, or escape non-token runs explicitly.
2) Unstated prerequisite: kb-datetime.ts must import and extend dayjs itself (dayjs + quarterOfYear + isoWeek). Verified that no queryjs file imports $lib/utils/date, so "already extended" does not hold inside this module graph; relying on it makes kb-datetime.test.ts throw. That is ~5 added lines the -70 estimate does not budget.
3) The month/year clamping delta appears only in the Verification block, not in the headline/Replacement, and no existing test pins the current overflow behaviour, so the change would ship silently.
4) -70 is optimistic. Block by block with dayjs: weekNumber -6, quarter 0, plus ~-3, minus ~-1, toISODate -3, hasSame -2, startOf ~-17, endOf ~-20 = ~-52; +5 for import/extend; and if toFormat + MONTHS stay (see 1) the realistic net is ~-45.
5) Minor: advancedFormat is cited as relevant but nothing in the proposal needs it. QueryjsSection.svelte:51 tells users the KBAPI "is documented in the README" while the README never mentions KBDateTime.

**Corrections (fable-5 cross-exam):**

1) Runtime nuance the first agent overstated slightly: calendar.logic.ts:2 does a bare side-effect `import '$lib/utils/date';`, so in the running app the dayjs plugins are normally registered globally before any queryjs block executes — the failure is guaranteed only in vitest and in module-graph terms; the 'add local extends' prerequisite still stands (relying on an unrelated plugin's side-effect import is load-order roulette). 2) kb-datetime.ts:2 docblock advertises 'Luxon-compatible', and real Luxon's plus() clamps at month ends exactly like dayjs — so the clamp-vs-overflow delta is a change TOWARD the advertised contract, not purely a regression; it still needs a pinning test and an explicit decision before shipping. 3) The Luxon-token choice (yyyy/dd lowercase, kb-datetime.ts:168-181) confirms the token map is intentional API surface, reinforcing that toFormat + MONTHS arrays should stay hand-rolled.

**Risk of applying as written:**

Medium-high if applied exactly as written, because the two behaviour breaks are invisible to the test suite. (1) Swapping toFormat to a token map + dayjs.format silently corrupts every user script whose format string contains literal text (any D/d/a/A/H/h/m/s/M/Y/Z/S character) or square brackets; all 7 existing toFormat assertions still pass, so CI gives false confidence. (2) plus/minus switch from Date overflow to dayjs clamping at month/year boundaries with zero test coverage of that case. (3) Without adding `dayjs.extend(quarterOfYear)`/`dayjs.extend(isoWeek)` inside kb-datetime.ts, `.quarter()`/`.isoWeek()`/`.startOf('isoWeek')` are undefined - verified no queryjs file imports $lib/utils/date - so kb-datetime.test.ts throws immediately (that one fails loudly, at least). Docs/tests to update: src/tests/lib/plugins/queryjs/kb-datetime.test.ts (448 lines) needs new cases pinning month-end clamping and literal-text formatting before any swap; QueryjsSection.svelte:51 claims the KBAPI is "documented in the README" but `grep -rn "KBDateTime" README.md` returns nothing, so there is no external contract - the only guard is the test file. Safe subset: startOf/endOf/weekNumber/quarter/hasSame/toISODate (~50 lines) can be delegated to dayjs with no observable change; toFormat + MONTHS arrays and plus/minus should stay hand-rolled unless the behaviour change is intentional.

### #15 — PARTIAL — The file read/write transform hook system in editor.hooks.ts: `FileReadTransform` + `FileWriteTransform` types, the `readTransform`/`writeTransform` m

- Path: `src/lib/core/editor/editor.hooks.ts:12` | tag `delete` | claimed 65 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

New evidence the first agent did not report: `git log --oneline -S setFileReadTransform -- src/lib` → 80abf50 'feat!: remove encrypted-notes feature and macOS security stack'. The encrypted-notes plugin was the historical registrant, and 80abf50's commit message explicitly records a deliberate decision to KEEP the hooks: 'The generic read/write transform hooks in editor.hooks.ts stay — they are pure infrastructure (currently no registrants) and removing them would touch every save/open path; they cost nothing at runtime when no transform is set.' This is a recorded intentional retention (in a commit message, not CLAUDE.md/an ADR, so it does not refute), and any commit applying #15 should state it reverses that 2026-05-19 decision. Also: docs/perf/baseline-template.md:23 ('read disk + apply transform + add tab') confirmed as the only doc needing rewording.

**Risk of applying as written:**

Applying as written is functionally safe (no behavior change: both transforms are provably null). Corrections to the entry: (1) Line count is UNDER-stated, not over: real deletion is ~85 lines, not 65 — editor.hooks.ts 12-32 (21) + 40-45 (6) + 96-107 (12) + 119-149 (31) + the now-orphaned `import type { EditorTab }` at :2 (1) + the two resetHooks assignments at :208-209 (2) = 73; editor.service.ts ~12 (drop :88-89 incl. the FE-STARTUP-PROBE line that measures applyReadTransform, the :93-95 `if (transformed)` debug block, collapse :139-145 to a bare `await writeTextFile(path, content)`, simplify :91/:111/:371-372). (2) The audit's stated applier range "118-155" is WRONG and dangerous if followed literally: 150-153 is notifyAfterSave's docblock, 154 its signature, 155 its `markRecentSave(filePath);`. Correct range is 119-148 (+blank 149). (3) Unstated prerequisite: `import type { EditorTab } from './editor.types'` (editor.hooks.ts:2) becomes orphaned — tsconfig has no noUnusedLocals so `pnpm check` will not catch it. (4) Test churn omitted from the Replacement: src/tests/lib/core/editor/editor.hooks.test.ts loses 4 describe blocks (applyReadTransform :40-74, applyWriteTransform :76-99, setFileReadTransform(null) :258-276, setFileWriteTransform(null) :278-294) plus imports :18,:19,:21,:22; src/tests/lib/core/editor/editor.service.test.ts loses the transform tests at :1001, :1017, :1028, :1042, :1067, :1079, :1248 plus the import at :53; src/tests/lib/core/app-lifecycle/app-lifecycle.service.test.ts must drop the stale mock entries at :16, :17, :21, :22. (5) Doc caveat (not a blocker): docs/perf/baseline-template.md:23 describes the `openFileInEditor:fresh` marker as "read disk + apply transform + add tab" and should be reworded.

### #16 — PARTIAL — block-math-widget.ts and inline-math-widget.ts are near-identical KaTeX widgets, each carrying its own Map cache and its own clear* export (CLAUDE.md

- Path: `src/lib/core/markdown-editor/extensions/live-preview/widgets/inline-math-widget.ts` | tag `shrink` | claimed 45 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

The finding title's parenthetical ('CLAUDE.md counts these as one "math HTML string" cache; there are two') mischaracterizes the doc: CLAUDE.md:231 lists both filenames in one bullet and describes the caching strategy, it never asserts a single cache. The audit omits the cache-key collision (displayMode must enter the key), omits that the empty-formula guard must remain displayMode-conditional to keep block-math-widget.test.ts:35-40 green, and omits the two 141-line test files that must be rewritten. The -45 estimate is roughly right but optimistic once the keyed cache plus the conditional guard are written back in (118 lines -> ~70, i.e. ~48 including the 2 lines dropped in app-lifecycle).

**Corrections (fable-5 cross-exam):**

One quibble with the first agent's own correction: their charge that the audit title 'mischaracterizes the doc' is half-fair — CLAUDE.md:231 uses the singular ('cache the sanitized KaTeX HTML string') across both filenames, so the audit's parenthetical is a defensible reading of the sentence; either way the doc neither asserts one cache nor protects two, so nothing changes. No other material additions.

**Risk of applying as written:**

The Replacement as written ('one cache and one clearMathCache()') is a correctness regression. Both existing maps key on the bare formula string; a single merged map makes `$E=mc^2$` and `$$E=mc^2$$` in the same vault collide — whichever renders first wins and the other gets KaTeX output for the wrong displayMode (displayMode:true emits the `katex-display` wrapper). The key MUST include displayMode (or two maps must be kept). Second, the empty-formula guard must stay conditional on displayMode: applying it unconditionally changes inline behavior for whitespace-only inline math; dropping it fails block-math-widget.test.ts:35-40. Third, the span/div swap is load-bearing, not cosmetic: styles.ts:819-829 gives `.cm-lp-math-inline` display:inline + verticalAlign:baseline and `.cm-lp-math-block` display:block + padding + textAlign:center + overflow:auto. Also required: rewrite both 141-line test files (clearInlineMathCache disappears from the API), update the two imports/calls at app-lifecycle.service.ts:75-76/414-415, and update CLAUDE.md:231 which names both files.

### #17 — CONFIRMED — DataArray's Proxy gates on KNOWN_PROPS, a hand-maintained 42-entry Set that must be kept in sync by hand with every method on the class

- Path: `src/lib/plugins/queryjs/data-array.ts:12` | tag `shrink` | claimed 45 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

1) The Evidence block says "Block spans lines 12-51" - wrong; it is 12-56. The Verification block already corrects this to 45 lines, so the headline -45 is right (45 removed plus the 3-line branch at :67-69 collapsing to 1 line).
2) Unstated but implied prerequisite: the symbol branch at :63-65 MUST stay. The proposed cut range (12-56) leaves it intact, but anyone "finishing the cleanup" by folding symbols into Reflect.has would send Symbol.toPrimitive/Symbol.toStringTag into `Number(prop)` and throw.
3) Five comment/test-name references to KNOWN_PROPS in src/tests/lib/plugins/queryjs/data-array.test.ts (91, 148, 205, 679-700) become stale wording; the assertions themselves need no change.
4) The Object.prototype delegation delta is named correctly but its scope is wider than "valueOf, hasOwnProperty, toLocaleString, __proto__": it also covers isPrototypeOf, propertyIsEnumerable, __defineGetter__/__lookupGetter__ etc. Traced through - nothing depends on the old mapping.

**Corrections (fable-5 cross-exam):**

One residual delta worth naming beyond the first agent's list: after the swap, `arr.constructor`-adjacent Object.prototype-chain names change failure mode, e.g. today `arr.valueOf()` THROWS ('not a function' — to('valueOf') returns a DataArray, not callable) while after the change it returns the proxy; same for hasOwnProperty()/isPrototypeOf() which start WORKING instead of throwing/mapping. All unreachable in production (no test or caller touches them) and strictly less surprising, but it is a behavior change in the user-scripting surface, not a pure no-op. Also the doc comment at data-array.ts:12 becomes stale alongside the test wording the first agent flagged.

**Risk of applying as written:**

Low. The one-line replacement is exactly equivalent for all 42 entries; numeric-index access, the symbol branch and the unknown-prop `to()` fallthrough are all preserved, and the existing prototype-completeness test at data-array.test.ts:679 fails loudly if delegation ever regresses. Follow-ups needed: keep the symbol branch at :63-65 (DataArray has [Symbol.iterator] at :399, but Symbol.toPrimitive/toStringTag are not members and would hit `Number(symbol)` -> TypeError), and reword the 5 stale KNOWN_PROPS mentions in the test file. Residual exposure is a note whose frontmatter key is an Object.prototype method name (valueOf, hasOwnProperty, ...), which would stop being mapped by kb.pages().<key> - untested today and practically unreachable, and arguably a safety improvement. No docs, ADRs, IPC or Rust surfaces involved.

### #18 — PARTIAL — canvas.logic.ts:373-411 — addNode, removeNode, updateNode, createEdge, addEdge, removeEdge (immutable CanvasData helpers). CanvasInner drives @xyflow/

- Path: `src/lib/features/canvas/canvas.logic.ts:373` | tag `delete` | claimed 35 lines | opus-5: CONFIRMED, fable-5: PARTIAL, arbitrated

**Corrections (opus-5 deep dive):**

Header line range "373-411" is wrong; the real block is 374-408. The audit's own Verification block already corrects this to 374-408/35 lines, and 35 is exact. No other correction.

**Corrections (fable-5 cross-exam):**

One refinement to the first agent's risk note: its worry that CanvasData/CanvasTextNode type imports in the test might become unused after deleting the five describe blocks does not materialize — `rg -n --word-regexp 'CanvasTextNode|CanvasData' canvas.logic.test.ts | awk -F: '$2<178 || $2>247'` shows CanvasData live at test lines 90/100 and CanvasTextNode at 142/148, so the type import line stays untouched.

**Risk of applying as written:**

Low. Nothing in production reads them. Prerequisite the entry states only in its evidence line, not in the fix: src/tests/lib/features/canvas/canvas.logic.test.ts must be edited in the same commit — drop the six names from the import block (lines 13-18) and delete describe('addNode') 178-186, describe('removeNode') 188-217, describe('updateNode') 219-228, describe('addEdge') 230-238, describe('removeEdge') 240-247 (~70 test lines). Note removeNode's "also removes connected edges" test at 200-206 also consumes createEdge, so createEdge cannot be kept "just for tests" once removeNode's tests go. Check whether `CanvasData`/`CanvasTextNode` type imports in the test become unused after the deletion (pnpm check would flag). CLAUDE.md rule 11 makes the test edit mandatory, not optional.

**Arbitration:**

Agent B was right. Both agents agree on the facts; the dispute is verdict classification, and the rubric settles it. Core claim re-verified true by my own sweeps: rg --word-regexp for all six symbols across src, src-tauri, e2e, scripts, and configs hits only the definitions (canvas.logic.ts:375/380/388/396/401/406) and src/tests/lib/features/canvas/canvas.logic.test.ts; zero internal callers; no barrel re-exports or dynamic/string usage in the canvas folder; CanvasInner's @xyflow/svelte import (lines 3-14) is SvelteFlow/Background/Controls/MiniMap/useSvelteFlow/types/MarkerType, so no addEdge collision; ADR 0023 protects only the JSON⇄xyflow transforms, not these helpers. But two entry defects block CONFIRMED under the given rubric ('acting on the finding exactly as written is safe'; wrong line count and unstated prerequisites are explicit PARTIAL triggers). (1) The headline and Path field say 373-411/:373 while the real block is 374-408 (35 lines, matching the -35 figure); I read lines 368-415 directly: 409 is blank, 410 is duplicateNode's docblock, 411 is `export function duplicateNode(...)` — and duplicateNode is LIVE (CanvasInner.svelte:38 import, :350 call), so deleting per the headline strips a live function's signature and breaks the build; only the buried Verification block corrects the range, leaving the entry internally inconsistent. (2) Unstated mandatory prerequisite: canvas.logic.test.ts imports all six at lines 13-18 and runs describe('addNode') 178, describe('removeNode') 188 (whose 200-206 test also consumes createEdge), describe('updateNode') 219, describe('addEdge') 230, describe('removeEdge') 240; deleting the exports without a same-commit test edit fails pnpm check and pnpm vitest at import time, and the entry never states this step (CLAUDE.md rule 11 makes it mandatory). Agent A itself listed both defects in its corrections yet still returned CONFIRMED, which contradicts the rubric it was applying. Verdict: PARTIAL — delete is correct with corrections: range 374-408 (35 lines, keep 409-411), plus the same-commit test-file edit (drop the six imports, delete the five describe blocks, and check whether CanvasData/CanvasTextNode type imports become unused).

### #19 — PARTIAL — properties.logic.ts dead exports: detectPropertyType (L31-39), serializePropertyValue (L192-205), formatRelationshipLabel (L428-430)

- Path: `src/lib/features/properties/properties.logic.ts:31` | tag `delete` | claimed 34 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) Omits the mandatory test work entirely: 3 import names + 3 describes (~150 lines) in src/tests/lib/features/properties/properties.logic.test.ts must be deleted, and 5 `it`s at lines 668/674/680/692/698 inside the surviving yaml-quoting-integration describe must be ported to `serializeProperties`, not dropped. 2) The summary header's ranges (L31-39, L192-205, L428-430) are export-statement ranges; the -34 figure only reconciles if the JSDoc blocks (28-30, 188-191, 427) are included — the verification block states this correctly but the summary row does not. 3) `formatRelationshipLabel` is explicitly acknowledged as caller-less in tasks/done/relationship-aliases-has-many.md:24 ("formatRelationshipLabel has no non-test callers; BacklinksPanel shows relationshipType.replace('_',' ')") — a task note, not an ADR, so it does not protect the symbol, but it shows the author knowingly kept it after a 2026 change that added `has_many` coverage.

**Risk of applying as written:**

Applying as written breaks `pnpm vitest run` unless the test file is edited in the same change — the audit never mentions it. `grep -n "^describe(" src/tests/lib/features/properties/properties.logic.test.ts` shows `detectPropertyType` (75-126), `serializePropertyValue` (331-411), `formatRelationshipLabel` (1075-1088), plus import names on lines 11, 13, 25. That is ~150 test lines. Additionally `sed -n '655,705p'` shows 5 `it`s inside the SURVIVING describe `serialize — backslash and quote sequences (yaml-quoting integration)` (662-731) that call `serializePropertyValue` directly (test lines 668, 674, 680, 692, 698) — these are emitter-level guards for ADR 0029's leading-quote rules and must be PORTED to `serializeProperties([...])`, not deleted, or ADR 0029's pinning surface shrinks (`yaml-quoting.logic.test.ts` contains zero `serializePropertyValue`/`serializeProperties` references, so it does not cover the emitter). No coverage thresholds exist (`grep -n coverage vitest.config.ts vite.config.js` → no output), so nothing else gates. CLAUDE.md rule 11 requires the test change in the same commit; frontend-only gate = pnpm check + pnpm vitest run + pnpm build.

### #20 — PARTIAL — type-sidebar.logic.ts dead exports: isInsideSystemFolder (L57-68), countInbox (L361-368, a byte-identical copy of inbox-workflow.logic.ts::getInboxCou

- Path: `src/lib/features/type-definitions/type-sidebar.logic.ts:57` | tag `delete` | claimed 30 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) LINE COUNT IS UNDER: the audit's -30 counts isInsideSystemFolder as 57-68 (12) but omits its 5-line JSDoc at 52-56; the real deletable block is 52-68 = 17. Real total is 17 + 8 (360-367) + 9 (392-400) = 34, ~37 with separating blank lines — not 30. 2) Off-by-one in the summary header: countInbox is L360-367 (header says L361-368) and formatDatePair is L392-400 (header says L393-401); the verification block has these right, the headline row does not. 3) The finding as relayed in the task summary drops `formatDatePair`; the audit file's section 20 does include it — all three must be deleted for the stated line count. 4) Omits the test work, including the in-place edit of test line 1159 described above. 5) Worth noting: `countNavItems` (LIVE, :316+) computes the same inbox count a third time inline, so removing countInbox leaves two implementations, not one.

**Corrections (fable-5 cross-exam):**

One cite error in the first agent's correction 5: countNavItems is defined at type-sidebar.logic.ts:88 (JSDoc :87), not ':316+' — 315 is its test describe line. The substance (countNavItems counts inbox inline as a third implementation) is correct: its body opens with `let inbox = 0` at :89.

**Risk of applying as written:**

Two concrete breakages if applied literally. (a) src/tests/lib/features/type-definitions/type-sidebar.logic.test.ts:2 imports all three names in one line, and describes at 303-313 (countInbox), 680-703 (formatDatePair), 1070-1100 (isInsideSystemFolder) must go (~66 lines). (b) Non-obvious: `sed -n '1140,1166p'` shows test line 1159 `expect(countInbox(filtered)).toBe(1);` living inside the SURVIVING `excludeSystemFolder` describe (1102-1166) — that single line must be deleted in place, not as part of a describe removal. It is redundant with line 1160 `expect(countNavItems(filtered).inbox).toBe(1);`, so removing it loses no coverage. Nothing else breaks; no prod behavior change. Frontend-only gate applies.

### #21 — PARTIAL — view-parse-cache.ts: getCachedViewYaml, getViewQueryResult, clearViewParseCache, clearAllViewParseCache are dead — which makes queryResultCache write-

- Path: `src/lib/features/type-definitions/view-parse-cache.ts:38` | tag `delete` | claimed 30 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) MISSES the clearAllViewParseCache-as-test-reset dependency (view-parse-cache.test.ts:23) — the single most important prerequisite in this finding. 2) LINE COUNT IS UNDER: counting survivors line by line, the file keeps lines 1-5, 8-11, 13-36 = 33 of 67, so 34 lines leave view-parse-cache.ts, plus TypeSidebar.svelte:105 = 35 — not 30. Line 27 (`parseCache.set(path, { contentHash, yaml: content, definition: parsed })`) is EDITED, not deleted, and the audit does not mention it. 3) Omits that ~85 of the 174 lines in view-parse-cache.test.ts (describes at 92-127, 129-139, 141-159, 161-172, plus 4 import names on line 13) die with the change. 4) Omits the stale mock at TypeNoteList.perf.test.ts:75. 5) Does not state that `matchingPaths` in TypeSidebar.svelte survives (used at :106) — only the :105 call goes; the audit's phrasing ("drop … the setViewQueryResult call site") is correct but a reader could over-delete.

**Corrections (fable-5 cross-exam):**

Two additions. (1) The audit's replacement names only 'the 4 functions', but dropping the queryResultCache Map forces deleting a FIFTH export, setViewQueryResult itself (:47-50) — it references the Map at :49 and its sole caller (TypeSidebar.svelte:105) is also being cut; the audit implies it, never states it. (2) The first agent's 'LINE COUNT IS UNDER … 34 not 30' is softer than stated: 4 of the 34 departing lines are blank separators (46/51/56/62); the audit's 30 counts content lines, so the two figures reconcile rather than contradict.

**Risk of applying as written:**

HIGH-VALUE CAVEAT the audit missed: `sed -n '1,40p' src/tests/lib/features/type-definitions/view-parse-cache.test.ts` shows lines 21-24 `beforeEach(() => { vi.clearAllMocks(); clearAllViewParseCache(); });`. `clearAllViewParseCache` is the test harness's ONLY reset for the module-level parseCache. Deleting it without a replacement leaves the two SURVIVING suites (`refreshViewDefinition` 26-65, which asserts "skips parse on second call with same content", and `getCachedViewDefinition` 67-90) sharing cache state across tests — they will pass or fail depending on ordering. The change must either keep the export as a test hook or switch the file to `vi.resetModules()` + dynamic re-import in beforeEach. Second risk (latent, pre-existing): with `clearViewParseCache` deleted there is no invalidation path at all for the parse cache on .view delete/rename, so a stale definition can survive a delete-and-recreate; the function is dead today, so this is not a regression, but deleting it removes the tool. Third: src/tests/lib/features/type-definitions/TypeNoteList.perf.test.ts:75 lists `setViewQueryResult: vi.fn()` in a `vi.mock` factory — harmless leftover (extra factory keys do not error) but should be cleaned. Fourth: TypeSidebar.svelte:32 must be narrowed to `import { refreshViewDefinition } from './view-parse-cache';` or `pnpm check` fails on the unresolved name.

### #22 — CONFIRMED — `components/ui/label` — a shadcn-svelte component nobody imports, plus its one-symbol barrel

- Path: `src/lib/components/ui/label/label.svelte` | tag `delete` | claimed 27 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Effectively zero. Deleting `src/lib/components/ui/label/` breaks no import, no template, no barrel, no CSS selector, no test. No test file references it, so nothing to update. Reversible in one CLI command per ADR-0002 (`pnpm dlx shadcn-svelte@next add label`). Only forward-looking caveat: any future shadcn form-field pattern (Input/Switch + Label) would need the component re-added; the repo currently pairs its inputs with plain HTML `<label>` (e.g. widgets.ts:916 `document.createElement('label')`).

### #23 — PARTIAL — Scattered single dead exports: tags.service.ts:103 flushScheduledTagIndexRebuild, collection.service.ts:108 removeNoteFromIndex, toolbar/sort.logic.ts

- Path: `src/lib/features/tags/tags.service.ts:103` | tag `delete` | claimed 26 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) flushScheduledTagIndexRebuild MUST be removed from the cut — it is a documented "Test-only convenience" with 4 live references in tags.service.test.ts; the finding's title and Path anchor still name it. 2) Line total for the remaining four is 27 (6+7+6+8), not 26 — isDurationString's docblock is 4 lines, not 3. 3) Unstated cascade: collection.store.svelte.ts:27 removeRecord becomes dead once removeNoteFromIndex goes. 4) Unstated prerequisite: four separate test suites must be edited.

**Risk of applying as written:**

The entry's headline and its `Path:` anchor both point at src/lib/features/tags/tags.service.ts:103 — the ONE symbol that must not be deleted. An implementer acting on the title (or on the summary row, which lists flushScheduledTagIndexRebuild first) breaks src/tests/lib/features/tags/tags.service.test.ts at 4 sites and removes a deliberately documented test seam. The audit's Verification block does say to drop it from the cut, so the entry contradicts itself.
For the four genuinely dead ones: each deletion requires deleting the matching test block in the same commit (sort-logic.test.ts describe('addSort') and its 4 cases; formula-logic.test.ts describe('finishAllEditing'); duration.test.ts describe('isDurationString'); collection.service.test.ts describe('removeNoteFromIndex')). Deleting removeNoteFromIndex should also remove collectionStore.removeRecord (and its store test) or it becomes newly dead code. Separately worth flagging to the owner, not fixing here: with removeNoteFromIndex dead, nothing ever evicts a deleted note from the collection panel's record map — that is a latent product bug the deletion makes permanent rather than causes.

### #24 — PARTIAL — MetaBindNumberWidget and MetaBindDateWidget are identical classes whose only difference is the 3-field options object handed to buildMetaBindTextInput

- Path: `src/lib/core/markdown-editor/extensions/live-preview/widgets.ts:838` | tag `shrink` | claimed 25 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

The audit claims 'No behavior lost — both would still produce the same input element'; that is only true for the first render. It misses that `eq()` is what makes the two types non-interchangeable to CodeMirror, so the merged class needs `type` added to `eq`. Everything else checks out: line ranges (838-861 / 876-899), the two construction sites, the shared builder at :941, and the ~25-line estimate.

**Corrections (fable-5 cross-exam):**

None beyond the first agent. Independently confirmed their node_modules citation, the two construction sites, the test-file construction at meta-bind-input-widgets.test.ts:25-26/:73+/:105+, and that MetaBindToggleWidget/MetaBindSelectWidget are correctly outside the finding's scope (toggle builds its own checkbox DOM, select takes an options list).

**Risk of applying as written:**

Merging as written breaks widget identity. Today the two distinct classes make `constructor == constructor` false, so a number widget can never be reused as a date widget. After collapsing into one `MetaBindTextWidget` whose `eq` still compares only bindTarget + currentValue, a `number()` -> `date()` change at the same position with the same value returns eq=true, CodeMirror keeps the existing DOM, and the input stays `type="number"` with `isNumericString` as the validator (and the wrong invalidMessage). The merged `eq` must also compare the input type/opts identity. Reachability is narrow because the plugin drops the decoration while the cursor touches the range, but doc changes made without the cursor there — watcher-driven reload of the open tab, undo/redo, a replace-all across the file — reach it. Also required: `isNumericString` (826) and `isDateString` (864) are separately exported and unit-tested and must stay exported, and src/tests/.../meta-bind-input-widgets.test.ts constructs both classes at :25-26, :73-144 and must be updated.

### #25 — PARTIAL — `extract_wikilinks_from_str` in entry.rs — a second hand-rolled `[[target]]` byte scanner living in a file that already imports the real one

- Path: `src-tauri/src/vault/entry.rs:406` | tag `delete` | claimed 23 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

New evidence the first agent did not report, which STRENGTHENS the swap: the TS-side consumers of relationship/property wikilinks already use the canonical semantics, not the local scanner's. `grep -rnF '[[' src/lib/features/{properties,type-definitions}/*.ts` → properties.logic.ts:370 `extractWikilinks` and type-sidebar.logic.ts:422 `splitPropertyIntoPills` both match `/\[\[([^\]]+)\]\]/g` — the reject-`]`-inside rule of `extract_outgoing_links`. So the local Rust scanner is currently OUT of parity with the TS frontend that renders these very relationship values as pills; the swap is a parity fix, not just dedup. All first-agent corrections re-verified true: 28-line block (net -24 with a 4-line wrapper), `.flat_map(extract_wikilinks_from_str)` by-name at :365 and :393 makes the wrapper the right shape, the empty-target filter is load-bearing, divergence class is every single-`]`-before-`]]` input.

**Risk of applying as written:**

Low, and it is a bug fix rather than a no-op. Real risk surface: `relationships` / `belongs_to` / `related_to` / `has_many` are emitted over IPC through `get_all_vault_entries_v2` and consumed by the type-definitions feature (ADR 0026), so for any user whose frontmatter puts a `]` inside a link (`_belongs_to: "[[a] b]]"`) the extracted targets change — they get *fewer, saner* targets. Nothing in src/ or src-tauri/tests asserts the old garbage output. Corrections: (1) the block is entry.rs:405-432 = 28 lines, not 27, so net after the 4-line delegating wrapper is -24 (or -25 collapsing the blank), not -23; (2) the divergence is not one edge case — it is every input where a single `]` precedes the closing `]]` (audit only cites `[[a]b]]`); (3) keep the wrapper rather than inlining, because two of the four call sites pass the fn by name to `.flat_map()`; (4) `.filter(|t| !t.is_empty())` is load-bearing — canonical emits empty targets for `[[ ]]` / `[[#h]]` that the local scanner drops. No test or doc updates required.

### #26 — PARTIAL — inbox-workflow.logic.ts: isInboxEnabled and shouldNewNoteBeUnorganized are identity functions (`return explicitOrganization`) and getInboxEntries is u

- Path: `src/lib/features/type-definitions/inbox-workflow.logic.ts:6` | tag `delete` | claimed 22 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) -22 is right for production lines but omits ~38 test lines that must go with them. 2) The "identity functions" framing implies call sites to simplify; there are none — say "three dead exports", not "identity functions", so nobody goes hunting for inline sites. 3) Omits ADR 0026:41, which names inbox-workflow.logic.ts and documents an `explicitOrganization`-gated Inbox that no production code implements; the finding should carry that as a follow-up, since the delete removes the last code artifacts pointing at the documented behavior. 4) "file shrinks to the single 8-line getInboxCount" is off by the JSDoc: 19-28 is 10 lines, and with the `NoteEntryV2` import and blanks the file lands at ~13 lines, not 8.

**Corrections (fable-5 cross-exam):**

Precision on the first agent's correction 3: the ADR drift is narrower than 'an Inbox that no production code implements' — the Inbox section itself IS implemented (TypeSidebar.svelte:66 + countNavItems inbox count) and ADR 0026:59's settings-toggle claim IS implemented (TypesSection.svelte:18-20); what no code implements is specifically line 41's claim that the Inbox is conditional on explicitOrganization. The ADR fix is to line 41's gating clause only.

**Risk of applying as written:**

Applying as written is runtime-safe (no callers), but: (a) src/tests/lib/features/type-definitions/inbox-workflow.logic.test.ts loses describes at 10-18, 20-39 and 52-60 plus 3 import names — the 60-line file drops to ~22 lines covering getInboxCount only. CLAUDE.md rule 11 requires that in the same commit. (b) Documentation drift: after the delete, `explicitOrganization` is a settings toggle wired to nothing at all in production, while ADR 0026:41 asserts the Inbox section is conditional on it. The delete is what finally makes that gap visible, so ADR 0026 line 41 should be corrected (or the missing wiring tracked as an issue) rather than left asserting behavior no code implements. (c) Note the cross-finding dependency: finding #20 deletes `countInbox`, the byte-identical twin of the `getInboxCount` this finding preserves — apply #26 and #20 together and the surviving implementation is the one dock-badge.logic.ts already imports, which is correct.

### #27 — PARTIAL — Three byte-identical copies of getRelativePath in features (search.logic.ts:146, quick-switcher.logic.ts:81, file-history.logic.ts:108), all duplicati

- Path: `src/lib/features/search/search.logic.ts:146` | tag `shrink` | claimed 21 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) "Three byte-identical copies" is false — only search.logic.ts:146-152 and quick-switcher.logic.ts:81-87 are byte-identical (diff rc=0); file-history.logic.ts:104-117 has a docblock, flipped args and slice-vs-substring. 2) "Arg order must be flipped at 5 call sites" is wrong on both counts: there are 7 call sites (search.logic.ts:171 and file-history.logic.ts:128 were missed), and only 4 need flipping. 3) A second behavior delta is unmentioned: filePath === vaultPath yields '' under the feature copies and the full path under fs.logic. 4) Net line saving is ~25, not 21 (28 gross). 5) ADR-0004:53 points at src/lib/utils/ as the intended home for a thrice-duplicated .logic helper, which also avoids colliding with finding #51's proposed utils/path.ts home.

**Corrections (fable-5 cross-exam):**

One small addition: RelationshipSearch.svelte:7 (features/properties) already imports getRelativePath cross-feature from quick-switcher.logic, so the repo has a second existing precedent that further weakens any layering objection to consolidation; it is also a call site in a third feature the migration must not miss.

**Risk of applying as written:**

Real behavior change at 7 call sites, two of which the audit never counted. fs.logic's predicate differs from the feature copies in two ways, not one: (a) sibling-prefix — vaultPath `/a/b` + file `/a/bc/x.md`: feature copies return `c/x.md` (wrong), fs.logic returns the absolute path (the audit caught this one); (b) exact equality — filePath === vaultPath: feature copies return `''`, fs.logic returns the full absolute path (the audit missed this); (c) a vaultPath carrying a trailing slash silently stops matching under fs.logic. The file-history call sites are the dangerous ones: file-history.service.ts:27 and :115 feed the result straight into `invoke('get_file_history', { filePath: relativePath })` as the SQLite snapshot key (ADR-0021), so any output change orphans stored snapshot rows for affected paths.
Arg-order flips are needed at only 4 of the 7 sites (search.logic.ts:171, SearchResult.svelte:64, QuickSwitcher.svelte:82, RelationshipSearch.svelte:79); the three file-history sites already pass (vaultPath, absolutePath) and must NOT be flipped — flipping all 5/7 blindly inverts file-history.
Three test describe blocks must be deleted or migrated to fs.logic.test.ts (which already has its own getRelativePath suite at :688-702 asserting the stricter semantics).

### #28 — PARTIAL — `watch` feature on tauri-plugin-fs plus the `fs:allow-watch` / `fs:allow-unwatch` capability grants — the vault watcher is native Rust `notify`, the J

- Path: `src-tauri/Cargo.toml:24` | tag `delete` | claimed 20 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1. depsSaved 0 is WRONG and the audit's own Verification text contradicts itself ("depsSaved 0 is right ... so only notify-debouncer-full would drop"). kokobrain is the sole enabler of `tauri-plugin-fs feature "watch"`; dropping it removes notify-debouncer-full v0.6.0 AND file-id v0.2.3 from the build (each has exactly one dependent). Correct depsSaved = 2. notify v8.2.0 stays because kokobrain declares it directly at Cargo.toml:38.
2. Missing docs consequence: ADR 0017 is `status: active` and still describes the plugin-fs watch() design as the live implementation (Decision section, alternatives line 37, stale fs.watcher.ts line refs). It must be superseded/updated as part of this change. The audit does not mention ADR 0017 at all.
3. Unstated prerequisite: the Cargo feature edit and the default.json:223-242 deletion are coupled - if the plugin gates its `allow-watch`/`allow-unwatch` permissions behind the `watch` feature, doing only one half breaks the build. Requires a real Tauri/cargo build to validate; not verifiable read-only.
4. Unstated step: src-tauri/Cargo.lock (tracked) must be regenerated and committed.
5. Minor evidence error: the audit's Evidence grep claims a hit for `watch(` at e2e/mocks/tauri-fs.ts:35 as the "single hit" - there are actually three watch-related symbols in e2e (tauri-fs.ts:35, :40 comment, :49 UnwatchFn), all dead mocks.

**Risk of applying as written:**

Low functional risk - nothing in production calls the JS watch API, so the runtime behaviour is unchanged. Real risks/extra steps: (1) the Cargo.toml feature edit and the capability deletion must land in the SAME commit; whether tauri-plugin-fs's `fs:allow-watch`/`fs:allow-unwatch` ACL identifiers still exist when the crate's `watch` feature is off cannot be determined read-only (the plugin source is outside the repo), so if they are feature-gated, removing the feature alone would fail codegen with "permission not found". (2) src-tauri/Cargo.lock is tracked and must be regenerated and committed. (3) This is a Rust+capabilities change, so CLAUDE.md Quick Reference rule 6 requires `cargo test --manifest-path src-tauri/Cargo.toml` plus a Tauri build to prove ACL codegen still passes - the only real proof. Docs needing updates: docs/adr/0017-file-watcher-incremental-hidden-filter.md must be marked superseded (or rewritten) - it is `status: active` and documents the plugin-fs watch() implementation as current, contradicting ADR 0025 and CLAUDE.md:268; it is already stale but this cut makes the contradiction load-bearing. Optional cleanup not in the finding: e2e/mocks/tauri-fs.ts:35 `watch()` and :49 `UnwatchFn` become the last watch symbols in the repo.

### #29 — PARTIAL — Seven single-range clamp wrappers in settings.logic.ts (`clampFontSize`, `clampLineHeight`, `clampContentWidth`, `clampParagraphSpacing`, `clampHeadin

- Path: `src/lib/core/settings/settings.logic.ts:3` | tag `shrink` | claimed 20 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) linesSaved -20 is wrong/understated. Deleting lines 3-38 removes 36 source lines; the new shared helper is ~5 lines. Real net is ≈ -30 source lines, not -20. The audit's arithmetic ("six 4-line blocks (24) minus a 4-line helper") silently drops clampContentWidth's own 5-line block and all 7 separating blank lines. 2) OMITTED PREREQUISITE: 138 lines of tests must be deleted/rewritten — src/tests/lib/core/settings/settings.logic.test.ts:1-152 is seven `describe` blocks (clampFontSize:15, clampLineHeight:34, clampContentWidth:48, clampParagraphSpacing:75, clampHeadingFontSize:94, clampHeadingLineHeight:113, clampHeadingLetterSpacing:132) plus the 7 import names at :3-9. 3) OMITTED PREREQUISITE: a new file `src/lib/utils/clamp.ts` must be created AND paired with `src/tests/lib/utils/clamp.test.ts` — all 17 existing files in src/lib/utils have a matching test file in src/tests/lib/utils/, and CLAUDE.md rule 11 / ADR-0007 make this mandatory. The audit's "one clamp in src/lib/utils" hides a 2-file addition. 4) UNDERSOLD: the same nested pattern exists at 14 more production sites outside settings (`grep -rn "Math.max(.*Math.min(\|Math.min(.*Math.max(" src` → FileHistorySection.svelte:10, AutoMoveSection.svelte:31, kb-ui.ts:126,164,731,907,987, kb-api.ts:182,224, kanban.logic.ts:249,268, KanbanView.svelte:246,253, KanbanLane.svelte:203, toc.service.ts:27, evaluator.ts:389) — a shared clamp is more justified than the entry claims.

**Corrections (fable-5 cross-exam):**

First agent errors found: (1) Their risk claim 'EditorSection's lineHeight/contentWidth/paragraphSpacing Inputs have no HTML min/max attributes (only step), so the clamp call is the only place those bounds are written' is FALSE — Read of EditorSection.svelte:95-180 shows all four inputs carry min/max (fontSize min={8} max={32}, lineHeight min={1} max={3}, contentWidth min={0} max={2000}, paragraphSpacing min={0} max={2}), and HeadingTypographyEditor.svelte:50-105 shows min/max on all three heading inputs. Substantive residue of their point: HTML min/max on number inputs do not clamp typed values, so the onblur clamp remains the operative enforcement, and contentWidth's HTML min=0 does not encode the 400 nonzero floor — but the factual statement was wrong. (2) 'all 17 existing files in src/lib/utils have a matching test file' — the directory has 16 files (15 .ts + build-info.js, verified by ls + diff against src/tests/lib/utils/), each with a matching test; direction holds, count wrong. (3) 'the same nested pattern exists at 14 more production sites' — their own enumeration lists 16 line references; `rg -n "Math\.max\(.*Math\.min\(|Math\.min\(.*Math\.max\(" src --glob '!src/tests/**'` minus settings.logic = 16 hits in 9 files. (4) Additional support they missed: FileHistorySection.svelte:9 defines yet another local single-range clamp wrapper (clampRetentionDays) the shared helper would serve.

**Risk of applying as written:**

Behaviorally low. `Math.max(a, Math.min(b, v))` is byte-identical to a generic `clamp(v,a,b)` on every edge case including NaN (both propagate NaN: `Number(inputValue(e))` on an empty/garbage input yields NaN, and neither form clamps it), -0, and Infinity. clampContentWidth's zero-sentinel is only reachable from EditorSection.svelte:150, so inlining `n <= 0 ? 0 : clamp(n, 400, 2000)` there is equivalent. Real risks: (a) the numeric ranges migrate out of `.logic.ts` into `.svelte` markup where they are no longer unit-testable — ADR-0004 lists "validations" as `.logic.ts` material, so this is a live tension even though the ADR sanctions utils/ for shared helpers; (b) `pnpm check` + `pnpm vitest run` + `pnpm build` all break until the 7 test describes and the 7 imports are removed and a clamp test is added — CLAUDE.md rule 6 requires all three plus test parity before commit. Note EditorSection's lineHeight/contentWidth/paragraphSpacing Inputs have no HTML min/max attributes (only `step`), so the clamp call is the only place those bounds are written; do not lose them in the inline rewrite.

### #30 — CONFIRMED — `sanitizeMathHtml` — an exported KaTeX/MathML DOMPurify config that no caller uses; the two math widgets call `DOMPurify.sanitize(raw)` directly

- Path: `src/lib/utils/sanitize.ts:57` | tag `delete` | claimed 20 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Deleting is safe and I judge the recommendation correct, not just the dead-code claim. On the security angle the parent flagged: bare `DOMPurify.sanitize(raw)` is NOT the weaker path here. DOMPurify's default profile already covers html+svg+mathml and strips `<script>`, event handlers and javascript: URLs, so the widgets are XSS-safe today. Adopting `sanitizeMathHtml` instead would be a functional REGRESSION: its ALLOWED_ATTR is only ['class','style','aria-hidden','viewBox','d','xmlns','width','height'], which drops attributes KaTeX legitimately emits — `encoding` on `<annotation>`, `preserveAspectRatio` on the stretchy-delimiter `<svg>`, `display` on `<math>`, and MathML token attributes such as `mathvariant`/`stretchy` — degrading MathML semantics and delimiter rendering. (This attribute analysis is analytical: node_modules is excluded by the ground rules, and the math widget tests only assert `dom.querySelector('.katex')`, never attributes, so it is not grep-verified.) Residual note: after deletion the repo loses its only written record of a tighter math allowlist; if a hardened config is ever wanted it must be re-derived against real KaTeX output. No test needs updating — sanitize.test.ts never imports the symbol.

### #31 — PARTIAL — lifecycle-filter.logic.ts: excludeArchived, onlyArchived, countArchived unused; only buildArchivedPathSet has a caller

- Path: `src/lib/features/properties/lifecycle-filter.logic.ts:3` | tag `delete` | claimed 18 lines | opus-5: CONFIRMED, fable-5: PARTIAL, arbitrated

**Corrections (opus-5 deep dive):**

None material. Only addition: the -18 counts production lines only; ~34 lines of src/tests/lib/features/properties/lifecycle-filter.logic.test.ts must be deleted in the same commit (import names on lines 3/4/6 and describes 34-50, 52-58, 75-84).

**Corrections (fable-5 cross-exam):**

Beyond the reclassification: the audit's '29-line file collapses to one 7-line function' undersells the residue — the surviving file is ~10 lines (NoteEntryV2 import at :1, JSDoc :13, body :14-20), only the function body is 7 lines. The mandatory same-commit test edit is ~37 lines: import names at lines 3/4/6 plus describes 34-50, 52-58, 75-84 of lifecycle-filter.logic.test.ts; lifecycle-filter.service.test.ts and the store tests are unaffected.

**Risk of applying as written:**

Low. The only follow-up work is the test file, which the audit does not mention: src/tests/lib/features/properties/lifecycle-filter.logic.test.ts (84 lines) loses 3 import names on lines 3, 4, 6 and the describes at 34-50, 52-58 and 75-84 (~34 lines), leaving only the buildArchivedPathSet suite at 60-73 plus fixtures. CLAUDE.md rule 11 requires that edit in the same commit; frontend-only gate = pnpm check + pnpm vitest run + pnpm build. No production behavior change, no dependency change, no ADR text to update. Note that lifecycle-filter.service.test.ts and lifecycle-filter.store.test.ts exist and touch only buildArchivedPathSet/the store, so they are unaffected.

**Arbitration:**

Agent B is right. The core claim is fully true — I independently re-verified that excludeArchived, onlyArchived, and countArchived have zero production references (word-boundary rg across src, src-tauri, e2e, scripts, docs, configs hits only the definitions at lifecycle-filter.logic.ts:4/:9/:23 plus the logic test file; no barrel in src/lib/features/properties, no string-keyed usage, no dynamic import, no docs/ADR protection, single-commit history 68951bf), buildArchivedPathSet is the sole live export (lifecycle-filter.service.ts:3 import, :9 call), and the -18 line count is exact (lines 3-6, 8-11, 22-29 plus separating blanks 7 and 21 of the 29-line file). But the audit entry is not safe to act on exactly as written: its Replacement field says "nothing" and its evidence greps excluded src/tests, while src/tests/lib/features/properties/lifecycle-filter.logic.test.ts (84 lines) imports all three names at lines 3/4/6 and exercises them in describes at 34/52/75 — deleting the exports without editing that file fails both pnpm check and pnpm vitest run, the mandatory frontend gate (CLAUDE.md rule 6/11). This is not implied by audit convention: the audit names test-file deletion where it applies (finding #1's Replacement says "delete the 11 files and their test files"; #9's verification carries a ~30-Rust-test caveat), so #31's silence is an unstated prerequisite — the exact PARTIAL criterion ("omissions or unstated prerequisites... extra step"). Agent A conceded the ~34-line test cleanup in its own corrections while calling it "None material," which is inconsistent with the CONFIRMED bar of "safe exactly as written." Correction to carry: delete the three imports (test file lines 3, 4, 6) and the describes at 34-50, 52-58, 75-84 in the same commit; the buildArchivedPathSet suite (60-73) and the service/store test files are unaffected.

### #32 — CONFIRMED — getPeriodicNoteTitle / getTemplatePathForPeriod / getDailyInlineTemplate are one-line property reads wrapped as exported 'logic' functions with a sing

- Path: `src/lib/plugins/periodic-notes/periodic-notes.logic.ts:82` | tag `yagni` | claimed 18 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

No material errors. Two small omissions worth stating: (i) src/tests/.../periodic-notes.service.test.ts imports getTemplatePathForPeriod (:25) and getDailyInlineTemplate (:26) but never calls them — they are dead test imports that still break the module link when the exports go; (ii) the same file DOES call getPeriodicNoteTitle at :75 and :270 to derive `expectedTitle`, so those two assertions must be rewritten (e.g. `now.format(format)` / `date.format(format)`), not merely have an import dropped. The entry's Verification lists those import line numbers but never mentions the two live usages.

**Corrections (fable-5 cross-exam):**

The true line saving is ~21, not 18: deleting the three wrappers also frees their three one-name-per-line entries in periodic-notes.service.ts's multi-line import block (:11, :12, :13). Undercount is in the finding's favor, not a safety issue.

**Risk of applying as written:**

Behaviourally inert — three one-line property reads inlined into their single caller, no reachable-path change, no type change. The real cost is test churn the entry understates: periodic-notes.logic.test.ts must drop imports at lines 7, 10, 11 and the describe blocks at :94, :141, :162; periodic-notes.service.test.ts must drop imports at lines 25-27 and inline the two `getPeriodicNoteTitle(...)` expectation helpers at :75 and :270. Without that, `pnpm vitest run` and `pnpm check` fail on missing named exports. Applying #13 and #32 together is fine — the spans do not overlap (79-112 vs 8-29/49-78/86-92/114-127; note 86-92 belongs to #13's getTodayPeriodicNoteTitle and sits between #32's blocks, so delete by symbol, not by line range). Gate: `pnpm check` + `pnpm vitest run` + `pnpm build`.

### #33 — PARTIAL — `VaultSearchMatch` interface — the TS mirror of the dead `search_vault` command's return type

- Path: `src/lib/core/filesystem/fs.types.ts:40` | tag `delete` | claimed 17 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Risk of applying as written:**

Applying the deletion is safe — no production or test consumer, no barrel, no invoke-string or capability identifier ties to it. Corrections to apply: the realized cut is 15 lines (16 with the blank), so re-rank accordingly; do not chain this to a `search_vault` removal on the strength of this entry's wording — that command is live in generate_handler with a large Rust test suite and an e2e mock handler, and removing it is a separate, much bigger change. `e2e/mocks/virtual-fs.ts:32` keeps its own copy of the shape and must NOT be touched. No test file needs updating for this finding.

### #34 — PARTIAL — `find_triple_backtick(bytes, start)` — a 14-line manual byte loop looking for three consecutive backticks

- Path: `src-tauri/src/vault/parsing.rs:967` | tag `stdlib` | claimed 16 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

One additional audit error the first agent missed: the audit's Verification block cites the enclosing fn at 'parsing.rs:818' — it is at :918 (`grep -n 'fn strip' parsing.rs`). Also additional supporting evidence: the in-file test module :2373-2399 (`strip_non_body_replaces_fenced_code_blocks_with_spaces`, `strip_non_body_handles_multiple_fences`) asserts `stripped.len() == input.len()` and exact fence blanking — these tests pin the behavior and would catch the broken windows-form replacement (they'd fail on the shifted blank range), and stay green under the primary `find` form.

**Risk of applying as written:**

**The parenthetical alternative in the audit is wrong as written.** `bytes[start..].windows(3).position(|w| w == b"```")` returns an offset *relative to `start`*; it needs `.map(|i| i + start)` exactly like the `find` form. Applied verbatim it silently corrupts `strip_non_body_content`: `open_idx` comes back too small, `for i in open_idx..close_end` blanks the wrong byte range, and every downstream unlinked-mention offset (vault/index.rs:129, :700) shifts. Take only the primary form: `content[search_from..].find("```").map(|i| i + search_from)` at :942 and `content[after_open..].find("```").map(|i| i + after_open)` at :948. Other corrections: the fn body is 13 lines, not the 14 the title claims (the -16 total is correct); and the equivalence holds only because both call sites keep `start <= len` — the guard at :940 must not be loosened, or the slice will panic where the old loop returned None. No test or doc updates required.

### #35 — CONFIRMED — extractAliasesFromContent in the wikilink completion logic — no completion path calls it

- Path: `src/lib/core/markdown-editor/extensions/wikilink/completion.logic.ts:118` | tag `delete` | claimed 15 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

Only a mechanical addendum the audit implies but does not spell out: besides deleting the `describe('extractAliasesFromContent')` block at test lines 227-259, the symbol must also be removed from the test file's import list at `src/tests/lib/core/markdown-editor/extensions/wikilink/completion.logic.test.ts:7`, or the suite fails to resolve the import. The audit's line arithmetic (-15 = 14-line block + dead import) reproduces exactly.

**Corrections (fable-5 cross-exam):**

Two trivia only: (a) deleting the function also strands blank line 116 (the separator before the jsdoc), so the tidy diff is arguably -16, not -15 — cosmetic; (b) worth noting completion.logic.ts has an external consumer (KanbanWikilinkSuggestions.svelte) so the FILE must survive; only the one function + import go. Neither changes the verdict.

**Risk of applying as written:**

Essentially zero production risk: the function is unreachable from any completion path, and the live alias source is `getAliasesFromEntry` in completion.ts. The only way to break the build is to delete the source without the test edits (test import at line 7 + describe block 227-259) — that fails `pnpm check` and `pnpm vitest run`. Test coverage lost is coverage of dead code only. No docs or ADR updates needed.

### #36 — CONFIRMED — 5 exported Decoration constants in styles.ts with no producer (inline marks moved to the HighlightStyle), plus the `.cm-lp-codeblock-line` and `.cm-lp

- Path: `src/lib/core/markdown-editor/extensions/live-preview/styles.ts:4` | tag `delete` | claimed 14 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

Nothing material. Two cosmetic follow-ups the audit does not mention: after deleting the rules, `e2e/specs/live-preview/code-blocks.spec.ts:56` and `e2e/specs/live-preview/misc-features.spec.ts:32-34` contain prose comments naming `.cm-lp-codeblock-line` / `.cm-lp-hard-break`; neither is an assertion, so no test breaks, but both comments go stale. Also worth recording that `.cm-lp-code` has a second live producer (`widgets.ts:291`) beyond the HighlightStyle — this strengthens, not weakens, the audit's decision to keep that rule.

**Corrections (fable-5 cross-exam):**

None material — I confirm the first agent's two cosmetic notes (stale prose comments in the two e2e specs after deletion; cm-lp-code's second producer at widgets.ts:291 reinforcing the keep decision).

**Risk of applying as written:**

No visual and no test risk. The 5 constants are referenced by nothing (not even tests), so removing them cannot change rendering; the two deleted CSS rules target class names that no code path emits, so no element loses styling; the `Decoration` import in styles.ts stays needed by the surviving constants; no CSS custom property becomes unreferenced. `pnpm check`, `pnpm vitest run`, `pnpm build` and the live-preview e2e specs are all unaffected (verified the e2e hits are comments, not locators). Only stale e2e comments to tidy; no docs or ADR changes needed.

### #37 — PARTIAL — heatmapCalendar() and yearlyCalendar() open with the same ~44-line intensity-resolution preamble (year filter, min/max intensity, scale start/end, col

- Path: `src/lib/plugins/queryjs/kb-ui.ts:646` | tag `shrink` | claimed 14 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) The recommended helper signature is incomplete: `{mapped, colors, emptyColor}` is not enough. Both call sites still need `year`, `firstColorKey` and `showCurrentDayBorder` afterwards (line numbers above), so the return bag must be ~6 fields, e.g. `{year, colors, firstColorKey, showCurrentDayBorder, resolved, emptyColor}`. As literally written the change does not compile.
2) The Evidence block's sed ranges (649-692 / 816-856) are off by one on both sides and "only 4 hunks" is wrong: with the true blocks (650-697 / 817-858) diff reports 7 hunks, two of which are comment-wording-only.
3) -14 is under-counted, not over-counted. 48 + 42 = 90 lines today; the two call sites collapse to ~8 lines each and the helper is ~30 (plus a small shared entry type), so the realistic net is roughly -25 to -40. Understated in the safe direction.
4) Unstated constraints for the extraction: weekStartDay is heatmap-only and must stay caller-local; the two records key differently (number dayOfYear vs `${month}-${day}` string) so the helper must return a per-entry list, not a keyed map; the helper should return the already-parsed `new Date(e.date + 'T00:00')` so heatmap does not parse the date string twice; last-wins duplicate-date semantics must be preserved by keeping the assignment loops at the call sites.

**Corrections (fable-5 cross-exam):**

Two inaccuracies in the first agent's own evidence, neither verdict-changing: 1) their heatmapCalendar `year`-read line list includes :703, but :703 is `const todayYear = new Date().getFullYear();` which does not read `year` — the actual reads are :698, :700, :724, :726, :754. 2) They describe weekStartDay as affecting only 'heatmap's leading-empty-cell math at :699', but it is also read at :773 (the day-of-week label rendering `new Date(1970, 0, i + weekStartDay + 4)`), so folding it into the helper would corrupt two things, not one — their keep-it-caller-local constraint is right for an extra reason.

**Risk of applying as written:**

Medium-low, and mostly compile-time once the return bag is fixed. Hazards when applying it: (a) the audit's 3-field bag omits year/firstColorKey/showCurrentDayBorder, so the code as recommended does not compile - TypeScript catches it immediately; (b) folding weekStartDay into the shared helper would change heatmap's leading-empty-cell math at :699 and hand yearlyCalendar an option KBYearlyCalendarOptions does not expose; (c) collapsing the two loops into one keyed map breaks either heatmap's dayOfYear indexing or yearly's `${month}-${day}` indexing, and would need to preserve last-wins on duplicate dates; (d) `colors[e.color ?? ''] ?? colors[firstColorKey]` throws today when a user passes `colors: {}` - the helper must reproduce that, not 'fix' it, since it is observable behaviour of a public API. Both functions are public KBAPI surface (kb.ui.heatmapCalendar / kb.ui.yearlyCalendar), but 29 assertions in src/tests/lib/plugins/queryjs/kb-ui.test.ts pin exact rgb output, box counts, grid columns and today borders, so a regression is caught by `pnpm vitest run`. No Rust/IPC/docs impact; KBHeatmapCalendarOptions/KBYearlyCalendarOptions in kb-ui.types.ts:168-197 stay untouched.

### #38 — CONFIRMED — calendar.service.ts openCalendarFile() is a pure rename of openFileInEditor, and openOrCreateDailyNoteForDate() a one-line partial application - each

- Path: `src/lib/plugins/calendar/calendar.service.ts:124` | tag `yagni` | claimed 14 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

Only bookkeeping nits, all self-corrected inside the entry: the `Path:` field points at :124 (openOrCreateDailyNoteForDate) while the title leads with openCalendarFile, and the Evidence lists "declarations at calendar.service.ts:124 and :131" without saying which is which; the Verification block gets the mapping right (openCalendarFile :131, openOrCreateDailyNoteForDate :124). The Verification also says "14 net, slightly above the claimed 11" while the entry header already says -14 — the -14 figure is the correct one and matches my count (14 deleted at 120-133, +1 for the orphaned import at :1, -1 for the new import in CalendarPanel).

**Corrections (fable-5 cross-exam):**

Two additions beyond the first agent: (i) calendar.service.test.ts:366-367 itself carries a comment calling openCalendarFile a "Thin orchestrator: the only effect is delegating to the (mocked)" sub-service — the file documents that its 4 tests assert pure delegation, so dropping them loses no real coverage; (ii) e2e/specs/calendar-plugin.spec.ts exercises the CalendarPanel UI (day-selection flow with fallback locators at :28-40), so any behavioral slip in the inlined replacement is additionally guarded by `bash scripts/e2e.sh` beyond the check+vitest+build gate.

**Risk of applying as written:**

Low, with one naming trap the entry does not flag. CalendarPanel.svelte:13 already imports calendar.service's own `openOrCreatePeriodicNoteForDate(periodType, dateKey: string)` (defined at calendar.service.ts:116), so replacing the :122 call site needs no new import — but periodic-notes.service exports an identically named `openOrCreatePeriodicNoteForDate(periodType, date: dayjs.Dayjs)` (aliased to `openPeriodicNote` at calendar.service.ts:8). Importing the wrong one into CalendarPanel would pass a 'YYYY-MM-DD' string where a Dayjs is expected: a type error at best, a silently wrong date at worst. Keep the './calendar.service' import. Second, openCalendarFile's replacement needs a NEW import of openFileInEditor from '$lib/core/editor/editor.service' in CalendarPanel.svelte; both call sites are un-awaited `onclick` handlers, and openFileInEditor already handles its own errors, so the fire-and-forget behaviour is unchanged. Test prerequisite (unstated in the entry): src/tests/lib/plugins/calendar/calendar.service.test.ts must drop the imports at :26 and :28 and the whole describe blocks at :327-357 (openOrCreateDailyNoteForDate) and :359-386 (openCalendarFile) — 4 tests, all of which only assert delegation, so no real coverage is lost. Gate: `pnpm check` + `pnpm vitest run` + `pnpm build`.

### #39 — PARTIAL — The entire file-tree sort-option feature: `changeSortOption`, the `sortVersion` counter, the `expectedSortVersion` optional param + staleness guard on

- Path: `src/lib/core/filesystem/fs.service.ts:333` | tag `delete` | claimed 13 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

One correction beyond the first agent: the audit's Verification sentence 'the SortOption type/sortTree stay for the store' is wrong on sortTree — `rg -n -w sortTree src | grep -v /tests/` shows sortTree (fs.logic.ts:10) has ZERO production callers anywhere (only its own recursive call at :22 and fs.logic.test.ts); sorting happens Rust-side in scan_vault. The store never calls sortTree, so it is a fourth dead remnant this finding leaves behind (out of scope to delete under #39, but the entry's claim that it 'stays for the store' is false).

**Risk of applying as written:**

Behaviorally safe — sortBy is provably the literal 'name' in production. Corrections: (1) Line count 13 is short by ~2: the deletable block is fs.service.ts:333-342 (10 lines incl. the sortVersion doc, the counter, the changeSortOption docblock/body and the trailing blank), :89-90 (comment + guard, 2), the doc line :75 "When called with an expectedSortVersion..." (1), `sortVersion = 0;` at :345 (1), fs.store.svelte.ts:46 setSortBy (1) = ~15. (2) The headline claims "the entire file-tree sort-option feature" but the proposed cut leaves three dead remnants: `let sortBy = $state<SortOption>('name')` (fs.store.svelte.ts:18), the `get sortBy()` accessor (:33) and `sortBy = 'name'` in reset (:85) all become unreachable once both invokes hardcode 'name'. Either keep the store read (and drop only changeSortOption/sortVersion/the guard) or delete the store state too — the entry is inconsistent as written. (3) Unstated prerequisite: `SortOption` in the type-only import at fs.service.ts:4 becomes orphaned (fs.logic.ts and fs.store.svelte.ts still use the type, so fs.types.ts:20 stays). (4) Test churn omitted: src/tests/lib/core/filesystem/fs.service.test.ts needs the import at :77, the whole `describe('changeSortOption')` at :726+, and the tests at :244, :300, :315-320, :347-357, :363, :376, :737-739, :754-762, :875-884, :888-902, :921 reworked; src/tests/lib/core/filesystem/fs.store.test.ts:32-34 (setSortBy test — CLAUDE.md requires a test per getter, so removing setSortBy without removing the getter leaves an untestable setter-less getter) and :118/:129; src/tests/lib/core/filesystem/fs.watcher.test.ts:355 asserts `sortBy: 'name'` in the scan_vault payload (still passes). (5) e2e/mocks/virtual-fs.ts:281 `if (sortBy === 'modified')` becomes an unreachable mock branch.

### #40 — CONFIRMED — `removeThemeOverrides(theme)` in theme.service.ts — walks a theme's CSS vars and removes them from documentElement. Nothing in the app ever removes th

- Path: `src/lib/core/settings/theme.service.ts:32` | tag `delete` | claimed 12 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

None material. Minor: deleting the block also takes the preceding blank line 27, so the diff is -13 lines rather than -12; the audit's -12 counts only 28-39.

**Corrections (fable-5 cross-exam):**

None material beyond the first agent's. Added evidence strengthening the delete: app.html:2 permanently applies class="dark" with no toggle anywhere, making the docstring's fallback scenario impossible, not merely unused.

**Risk of applying as written:**

Very low. Nothing in production calls it and no code path needs it (proof above). The one required extra step, which the audit's evidence discloses but does not spell out as an action: src/tests/lib/core/settings/theme.service.test.ts:134-153 (the whole `describe('removeThemeOverrides')`, 20 lines) and the symbol in the import at :6 must be deleted in the same commit or `pnpm vitest run` and `pnpm check` fail. No ADR protects it — ADR-0008 and the theme ADR surface say nothing about removing overrides, and the function's own docstring rationale ("allowing CSS `.dark` fallback values to take effect") describes a fallback mechanism no code path exercises.

### #41 — CONFIRMED — HorizontalRuleWidget and its `.cm-lp-hr` CSS rule — horizontal rules are rendered CSS-only via `.cm-lp-hr-line`, so the widget never produces a decora

- Path: `src/lib/core/markdown-editor/extensions/live-preview/widgets.ts:17` | tag `delete` | claimed 12 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

None material. Two notes: the audit's bonus observation in the Evidence block is correct but must be applied carefully — widgets.ts:301 `export { openWikilinkTarget } from './wikilink-navigation';` IS dead (the only external consumer, KanbanView.svelte:47, imports from the source module), yet the plain import at widgets.ts:15 must stay because widgets.ts:334 calls it; only the re-export line goes, and it is not part of the -12. Also tasks/notes/css-classes-inventory.md:100 already records cm-lp-hr as deprecated and should be updated alongside.

**Corrections (fable-5 cross-exam):**

One correction to the first agent's evidence characterization, not to the verdict: they listed 'parsers/horizontal-rule.ts's isHorizontalRule' as part of the live Lezer-node path. It is not live — `rg -n -w isHorizontalRule src e2e scripts | rg -v '^src/tests'` returns only the declaration (parsers/horizontal-rule.ts:8); the live HR path dispatches on the nodeType string 'HorizontalRule' in simple-widget-handlers.ts:36/43 without ever calling isHorizontalRule. That function is additional test-only dead code adjacent to (but outside the scope of) finding #41.

**Risk of applying as written:**

Essentially none. `--lp-hr-border` is still consumed by `.cm-lp-hr-line` (styles.ts:76), so removing the `.cm-lp-hr` rule orphans no CSS variable. `WidgetType` (widgets.ts:1) remains needed by the other widget classes in the file. No test constructs the widget, so no test change is forced (CLAUDE.md rule 11 is satisfied by there being no test to update). Only stale-doc side effect: tasks/notes/css-classes-inventory.md:100 lists `cm-lp-hr` as a 'deprecated wrapper class' — a tasks/ artifact, not a caller, but it should be pruned with the change.

### #42 — PARTIAL — todoist-bridge.logic.ts:41-52 mapPriorityFromTodoist — the inbound half of a bidirectional mapping; only mapPriorityToTodoist is used

- Path: `src/lib/features/tasks/todoist-bridge.logic.ts:41` | tag `delete` | claimed 12 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) MISSED TEST COLLATERAL — the material omission. The audit's evidence grep used `-g '!src/tests/**'` and, unlike its sibling entry #43 which explicitly re-ran "tests INCLUDED this time", #42's verification never checked tests. There IS a full test block: `src/tests/lib/features/tasks/todoist-bridge.logic.test.ts` line 4 (named import), line 42 (section header comment), lines 44-72 (`describe('mapPriorityFromTodoist')` with 7 `it` cases covering 4/3/2/1/0/-1/99). Deleting only the source breaks `pnpm check` (TS2305: no exported member) and `pnpm vitest run`. The entry must also strike test line 4 and lines 42-73.

2) THE STATED RANGE LEAVES AN ORPHAN. Path/Cut say "todoist-bridge.logic.ts:41-52 | -12 lines". Cutting exactly 41-52 leaves the 10-line JSDoc at 31-40 (ending in the ASCII priority table) dangling directly above buildTodoistArgs's own JSDoc at 54-63 — two stacked doc comments, the first documenting nothing. The correct cut is lines 30-52 (23 lines) or 31-52 (22 lines).

3) The verification's own arithmetic is wrong in a way that made it accept the low number. It says "the 6-line ASCII priority table above it (35-40) goes with it, so 12 is if anything conservative". The table is at 34-39, not 35-40, and the block that must go is the whole JSDoc 31-40. Having noticed the doc comment travels with the function, it still recorded Cut = -12 instead of ~-23, so the entry is internally inconsistent.

4) Not an error but unrecorded context worth carrying: this is a deliberately symmetric bidirectional pair (mapPriorityToTodoist is 5-to-4 lossy, mapPriorityFromTodoist is its 4-to-5 inverse). Nothing documents an intent to build inbound sync, and the trivial 4-case switch is re-derivable in a minute if one ever lands, so the cut stands — but the reviewer should know it is symmetry loss, not dead-by-accident code.

**Corrections (fable-5 cross-exam):**

None material beyond the first agent. One precision: the test-file cut is line 4 plus lines 42-73 (section comment :42, blank :43, describe 44-72, trailing blank :73; next section comment starts at :74) — verified against the actual file, matching the first agent's stated range.

**Risk of applying as written:**

Applied literally (delete source lines 41-52 only), the build fails on `pnpm check` and `pnpm vitest run` because todoist-bridge.logic.test.ts:4 still imports the symbol; you also ship an orphaned JSDoc block. Done correctly the runtime risk is zero: the entire inbound Todoist surface is fetchTaskById -> `{id, checked}` -> `todoistCompleted`, which never reads priority, and SentTaskEntry has no priority field to populate. Outbound priority mapping (the half users actually see, via TodoistPopover.svelte:65 and buildTodoistArgs:69) is untouched. Required extra work: cut lines 30-52 (not 41-52) of src/lib/features/tasks/todoist-bridge.logic.ts, and cut line 4 plus lines 42-73 of src/tests/lib/features/tasks/todoist-bridge.logic.test.ts. No docs, ADRs, Rust, capabilities or config need updating. Only cost is re-writing ~15 lines if a Todoist inbound sync is ever built.

### #43 — CONFIRMED — frontmatter-icon.service.ts:49-59 setFrontmatterIconColor — writes _color to frontmatter; no caller anywhere, not even a test

- Path: `src/lib/features/file-icons/frontmatter-icon.service.ts:49` | tag `delete` | claimed 11 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Essentially zero. Nothing calls it, no test breaks, no import is orphaned. The only judgement call is product-level: `_color` is a documented system-metadata key (ADR-0027) and a future "change icon colour without touching the icon" UI would want this writer back — but it would be a 10-line rewrite, and today the code is unreachable.

### #44 — PARTIAL — `clearRecentSaves(paths)` in editor.hooks.ts — the watcher was supposed to call it after consuming self-save markers, but nothing does; the 15s RECENT

- Path: `src/lib/core/editor/editor.hooks.ts:86` | tag `delete` | claimed 10 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

Two adjustments to the first agent's reasoning: (1) their claim that the audit rationale is 'factually wrong' is too strong — clearRecentSaves' own docblock (editor.hooks.ts:85 'after the watcher consumed them') records the watcher as the intended consumer, so 'the watcher was supposed to call it' matches the code's own documentation; what watcher-handler.service.ts:43-45 adds is that the CURRENT handler deliberately declined to clear (a caveat for the commit message, not a falsification). (2) An audit-entry imprecision neither party flagged as such: 'the 15s RECENT_SAVE_TIMEOUT_MS timer is the only path that clears entries' is not strictly true — resetHooks (editor.hooks.ts:211-212) clears all markers on vault teardown, and markRecentSave (:72-73) replaces an existing timer. Test churn confirmed: drop editor.hooks.test.ts:27 import + the :333-339 case, and app-lifecycle.service.test.ts:20 mock line.

**Risk of applying as written:**

Deleting it is safe. But the entry's RATIONALE is factually wrong and must not be carried into the commit message: the watcher was NOT "supposed to call it". src/lib/core/app-lifecycle/watcher-handler.service.ts:42-44 documents the opposite as a deliberate decision — "Skip expensive full rebuild when changes are directory-only or all self-saves. // Does NOT clear the recent-save markers — the TTL handles cleanup. // This prevents later watcher batches (for the same save) from missing detection." So the function is intentionally unused dead flexibility (arguably a footgun that would break macOS multi-batch self-save detection if wired up), which strengthens the delete but invalidates the story. Corroborating doc: docs/adr/0017 §Context notes macOS emits multiple event batches per save. Unstated test work: remove the import at src/tests/lib/core/editor/editor.hooks.test.ts:27 and the `it('clearRecentSaves removes the markers')` case at :333-339, and drop the `clearRecentSaves: vi.fn()` mock line at src/tests/lib/core/app-lifecycle/app-lifecycle.service.test.ts:20.

### #45 — CONFIRMED — stripCardDate and stripCardColor are byte-identical duplicates of removeCardDate / removeCardColor, and both strip* variants are test-only

- Path: `src/lib/plugins/kanban/kanban.logic.ts:472` | tag `delete` | claimed 10 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

One trivial off-by-one that the entry itself already fixes: its Evidence block cites "removeCardColor(:511) and stripCardColor(:516)", which are the body lines; the declarations are at :510 and :515, as the Verification block correctly states.

**Corrections (fable-5 cross-exam):**

Trivial nit in the first agent's own evidence: the kanban plugin folder contains 12 files, not the 11 it reported (`rg --files src/lib/plugins/kanban | wc -l` = 12); either way there is no index.ts barrel, so the conclusion is unaffected.

**Risk of applying as written:**

Effectively zero. The twins are unreachable from production and byte-identical to functions that stay, so no behaviour, no edge case (empty string, no-token text, multiple internal spaces) can change. Test prerequisite (implied but not spelled out): src/tests/lib/plugins/kanban/kanban.logic.test.ts must drop the named imports at :33 and :38 plus the describe blocks at :1140-1152 (and its comment header at :1138) and :1250-1262, or the ESM link fails. No coverage is lost — those 6 test cases use exactly the same inputs and expectations as the surviving removeCardDate (:1124-1136) and removeCardColor (:1236-1248) blocks. Gate: `pnpm check` + `pnpm vitest run` + `pnpm build`.

### #46 — PARTIAL — `get_search_index_stats` — registered command returning the FTS document count; nothing asks for it

- Path: `src-tauri/src/commands/search_index.rs:200` | tag `delete` | claimed 10 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1. FALSE EVIDENCE (material): "No test reference either" / "zero tests, and no dynamic invoke path" — src-tauri/tests/search_fts_test.rs:231-245 is a dedicated `#[test]` that calls the command directly. Deleting the command without deleting that test breaks the build. The audit's grep scope (`src scripts src-tauri/src`) simply never looked at src-tauri/tests.
2. Step list incomplete: the change is three edits, not two — src-tauri/src/commands/search_index.rs:198-207 (10 lines), src-tauri/src/lib.rs:321 (1 line), src-tauri/tests/search_fts_test.rs:231-245 (15 lines). Total ~26 lines, not 10.
3. Coverage note: after removing that test, `db::fts_repo::count_entries` loses its only test that exercises it through the command layer; it keeps direct coverage in src-tauri/tests/fts_repo_test.rs:67/89/176/180/200/235/239/245/302, so real coverage loss is nil. Worth stating rather than discovering.
4. Context (not protective, but relevant): tasks/done/test-gap-closure-phase1.md line 78 already flags `get_search_index_stats` as one of the "Dead-but-registered commands ... [that] have Rust-side tests but no TS consumer" — the repo knew this was dead and chose to test it anyway.

**Corrections (fable-5 cross-exam):**

Trivial line-number slip in the first agent's correction 3: `rg -nw count_entries src-tauri/tests/fts_repo_test.rs` hits lines 67/89/176/180/200/235/239/247/302 — they cited 245 where the actual hit is 247. Immaterial to the conclusion (count_entries keeps ample direct coverage after the command-layer test is deleted).

**Risk of applying as written:**

Applying the finding exactly as written breaks `cargo test --manifest-path src-tauri/Cargo.toml`: search_fts_test.rs:238 calls the removed function, so the integration-test crate fails to compile and the mandatory Rust pre-commit gate (CLAUDE.md rule 6) errors before a single test runs. With the test removed too, the change is safe: no frontend invoke, no e2e mock entry, no dynamic dispatch, no doc reference. Do NOT touch `IndexStats` (search_index.rs:13) or `db::fts_repo::count_entries` (fts_repo.rs:123) — both have live production callers via build_search_index, and vault_test.rs:699-708 asserts IndexStats' camelCase wire shape. No user-facing behavior changes.

### #47 — PARTIAL — `shouldAutoCheckNow(autoCheck) { return autoCheck; }` — an identity function with one internal caller, carrying a 13-line docblock that explicitly jus

- Path: `src/lib/core/settings/update-check.service.ts:39` | tag `yagni` | claimed 9 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) The docblock size is stated wrong TWICE and both times too small: the title says "13-line docblock", the Verification says "14-line docblock". It is lines 20-38 = 19 lines. 2) linesSaved -9 is a judgement call, not a measurement — a straight delete of 20-42 is -23 source lines; -9 assumes ~10 lines of prose survive by being pasted into maybeAutoCheckForUpdates' docblock. State that as the plan, not as the count. 3) UNSTATED HAZARD in that prose move: maybeAutoCheckForUpdates' existing docblock is ALREADY stale and contradicts the surviving text. Line 44-45 reads "if the user has `updates.autoCheck` enabled and the last check is older than the throttle window", and the inline comment at :63-64 reads "Persist the timestamp so a relaunch within the throttle window does not duplicate the check" — there is no throttle window (that is precisely what the shouldAutoCheckNow docblock explains was removed). Merging the two docblocks without fixing both stale references produces a self-contradicting comment. 4) OMITTED STEP: update-check.service.test.ts:35 and :40-48 must be removed in the same commit.

**Corrections (fable-5 cross-exam):**

Two additions: (1) The first agent's risk claim that the docblock is 'the sole written record of why the 24h lastCheckedAt throttle was removed for Nightly users' is WRONG — docs/RELEASE-CHANNELS.md § 'Auto-check on launch' (lines 22-28) already records the full rationale in prose: 'The check fires on every vault open while the toggle is on — no throttle. The cost is a single HTTP request to a GitHub release-asset CDN per launch, and a Nightly user (where new builds publish multiple times a day) needs the check to actually fire on every launch', plus the lastCheckedAt display-only semantics in the following paragraph. The documentation-regression risk of dropping the docblock prose is therefore much lower than the first agent claimed. (2) New stale-doc find neither the audit nor the first agent reported: help/documentation/19-settings.md:257 still documents 'Auto-check on launch ... Throttled to once per 24h' — user-facing help contradicting both the code and RELEASE-CHANNELS.md; it should be fixed in the same change (pre-existing doc bug).

**Risk of applying as written:**

Zero behavioral risk — the call site already has the boolean in scope and the replacement is a literal inlining of an identity function. The only real risk is documentation regression: the deleted docblock contains the sole written record of why the 24h `lastCheckedAt` throttle was removed for Nightly users, and the sole note that `lastCheckedAt` is now display-only. If that prose is dropped rather than moved (and the two stale throttle references at :44-45 and :63-64 fixed), a future contributor will re-add the throttle and silently break Nightly auto-check. Gate is `pnpm check` + `pnpm vitest run` + `pnpm build` (frontend-only change).

### #48 — CONFIRMED — `split_key_value` — 9 lines to split a `key: value` line on the first colon

- Path: `src-tauri/src/vault/parsing.rs:657` | tag `stdlib` | claimed 9 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (fable-5 cross-exam):**

None material. The only soft spot (already noted by the first agent) is the -9 line claim, which depends on inlining style (-5 to -11 range); it does not affect safety or the verdict.

**Risk of applying as written:**

Effectively none. It is a private helper with one caller, the replacement is behaviorally identical on every input, and the caller's own comment at :600 ("Malformed line at top level (no colon, or empty key). Skip.") already carries the documentation the deleted doc comment held. The only soft point is the claimed -9: inlining on one line at :599 makes it -11 (10 lines + the blank separator, call site stays one 122-char line); keeping the fn with a one-line body makes it -5; wrapping the chain across lines at the call site makes it -6. -9 sits inside that range, so the number is defensible rather than wrong. No test file references `split_key_value`; `parse_yaml_lines` coverage in parsing.rs's test module stays green because behavior is unchanged. No doc or ADR mentions it.

### #49 — CONFIRMED — `isValidFolderName(name)` in settings.logic.ts — path-traversal validator for vault-relative folder settings, never called by any settings section or

- Path: `src/lib/core/settings/settings.logic.ts:40` | tag `delete` | claimed 8 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

None material. Minor: the removal also swallows the blank line at :47, so the diff reads -9 rather than -8.

**Corrections (fable-5 cross-exam):**

None beyond the first agent's (their blank-line note and the required test cleanup at settings.logic.test.ts:10 + :155-196 both re-verified accurate).

**Risk of applying as written:**

This is the one finding where the 'never delete validation' instinct fires, so state the reasoning explicitly rather than trusting the label: the function is not a guard, it is an unwired guard. No settings section, service, or Tauri invoke routes a folder-name string through it, and ADR-0020 puts the only trustworthy check in Rust. Deleting it therefore removes no active defense. The residual risk is future-facing: if someone later adds a free-text folder input in a settings section, the ready-made `../` rejector is gone and they must either re-write it or (correctly, per ADR-0020) rely on the Rust canonicalize check — which returns `FileReadResult { error: "Path is outside vault directory" }` rather than a friendly inline UI error, so UX degrades even though security does not. Required extra step: delete settings.logic.test.ts:155-196 and the `isValidFolderName` name from the import at :10, else `pnpm check` + `pnpm vitest run` fail.

### #50 — PARTIAL — tag-colors.logic.ts:30-37 getContrastTextColor — perceived-brightness contrast picker with no consumer (TagColorPicker.svelte uses only TAG_COLOR_PRES

- Path: `src/lib/features/tags/tag-colors.logic.ts:30` | tag `delete` | claimed 8 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) Line count/range wrong: the deletable block is 26-37 = 12 lines (docblock included), not 30-37 = 8. 2) The summary row I was given says "TagColorPicker.svelte uses only TAG_COLOR_PRESETS"; the actual import is TAG_COLOR_PRESET_ENTRIES (the audit body has this right). 3) Unstated prerequisite: delete tag-colors.logic.test.ts:59-91 and the import name at :6.

**Risk of applying as written:**

Low as a deletion — nothing in production calls it and no UI loses behavior (tag pills get their text colour from CSS/theme, not this function). Two things the entry gets wrong operationally: the cut is 26-37 (12 lines incl. the docblock), not 30-37 (8), and applying the stated range literally orphans the comment. The 33-line describe('getContrastTextColor') block at tag-colors.logic.test.ts:59-91 must be deleted in the same commit plus the name dropped from the import at :6, or vitest fails. Soft caveat: tasks/done/test-gap-closure-phase1.md:44 records that someone deliberately wrote those tests in a past task, so the owner may want to confirm the helper was not staged for an upcoming coloured-tag-pill feature before deleting.

### #51 — PARTIAL — Four separate copies of the same basename one-liner in core: `getFileName` in editor.logic.ts, `getFileName` in fs.logic.ts (byte-identical bodies), `

- Path: `src/lib/core/editor/editor.logic.ts:18` | tag `shrink` | claimed 6 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) "byte-identical bodies" is false for the editor.logic/fs.logic pair — diff rc=1, the parameter is named `path` in editor.logic and `filePath` in fs.logic. The genuinely byte-identical pairs are editor.logic↔vault.logic (bodies) and link-updater.extractNoteName↔backlinks.getNoteName (bodies). 2) Unstated prerequisite: 5 test suites and 10 production call sites must move. 3) Conflicts with finding #59's proposed home; they must be resolved together. 4) A fifth private copy lives at e2e/mocks/virtual-fs.ts:59 (out of scope for a $lib helper, but worth knowing).

**Corrections (fable-5 cross-exam):**

Minor precision: link-updater.service.test.ts also references extractNoteName (7th test file touching these names), so the migration's test sweep is one file larger than the first agent's five-suite list.

**Risk of applying as written:**

Mechanically safe (all five bodies are semantically identical basename/stem), but the entry understates the blast radius. Five test suites assert on the four names and must be migrated or rewritten: editor.logic.test.ts:5-24 (5 cases incl. the `'/'` → `''` and trailing-slash → `''` edge cases), fs.logic.test.ts:164-171, vault.logic.test.ts:4-23, link-updater.logic.test.ts:7-30, and backlinks.logic.test.ts:97-112 if getNoteName is folded in too. Ten production call sites across 6 files need re-pointed imports.
Cross-finding conflict: #51 proposes stem() in utils/path.ts while #59 proposes search.logic importing getNoteName from features/backlinks. Applied as written the two create two different homes for the identical stem helper. Pick utils/path.ts (ADR-0004:53) and fold #59 into it.
Judgement caveat: replacing vault.logic.extractVaultName with a generic basename() loses a domain-named function that vault.store.svelte.ts:52 reads clearly; keeping a 1-line named wrapper there costs nothing and preserves intent.

### #52 — PARTIAL — Hand-rolled local YYYY-MM-DD formatting duplicated in collection/calendar.logic.ts:174 (formatDateKey) and file-history/file-history.logic.ts:175 (toD

- Path: `src/lib/features/collection/calendar.logic.ts:174` | tag `native` | claimed 6 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) The audit missed the helper that already exists: `formatDate(date, 'YYYY-MM-DD')` from src/lib/utils/date.ts:47 — reuse that instead of adding dayjs imports to two feature .logic.ts files or hand-rolling anything. 2) The `toLocaleDateString('en-CA')` suggestion in the Verification block is unsafe in a WKWebView-hosted app and should be dropped. 3) file-history.logic.ts:175 toDateKey is module-private (`function`, not `export function`) — the entry implies both are exports. 4) The 'native' tag is wrong (the audit admits this); with utils/date.ts it is really a 'reuse-what-exists' finding. 5) Spans are 171-179 (9) and 174-180 (7); net saving ≈6-8 depending on whether the named wrappers are kept.

**Corrections (fable-5 cross-exam):**

One nuance to the utils/date.ts recommendation: importing utils/date.ts pulls in dayjs plus five dayjs plugin extensions at module scope (lines 1-12), where the audit's plain `import dayjs from 'dayjs'` would not — immaterial for bundle size since date.ts is already in the app graph, but worth knowing the reuse is not import-free.

**Risk of applying as written:**

The recommended replacement is the weakest part. `date.toLocaleDateString('en-CA')` — offered in the Verification as "the actual native one-liner" — is ICU/locale-data dependent and this app renders in WKWebView on macOS, not in the Node/full-ICU environment vitest runs in; en-CA has historically returned non-ISO output in WebKit builds. A green vitest run would not prove the shipped app formats the same string, and formatDateKey's output is a Map key joining calendar cells to events (calendar.logic.ts:33/53/117/124, collection-block-widget.ts:172/189) — a format drift silently empties the calendar. Do not take that suggestion.
`dayjs(date).format('YYYY-MM-DD')` is safe and matches the existing plugins/calendar/calendar.logic.ts:75 precedent; one behavior delta on invalid Dates (hand-rolled → "NaN-NaN-NaN", dayjs → "Invalid Date") which no caller can currently hit since all inputs are constructed Dates or `new Date(snapshot.timestamp)`.
Do NOT import plugins/calendar's toDateKey from features/ — that inverts the ADR-0003 layer order (features must not depend on plugins).
Tests to update: calendar-logic.test.ts:34-41 (describe('formatDateKey')) stays valid if the export keeps its name; file-history's copy is private so no test touches it.

### #53 — CONFIRMED — hand-rolled local-time `YYYY-MM-DDThh:mm:ss` formatter (a `pad` helper plus a 4-line template-literal array join)

- Path: `vite.config.js:22` | tag `stdlib` | claimed 5 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (fable-5 cross-exam):**

Two non-verdict nits on the first agent's prose: (1) it described the offset arithmetic as 'adding it shifts' while the audit's replacement subtracts (the subtraction as written in findings.md:938 is the correct sign — Date.now() minus a positive-west offset lands on local wall time); (2) the atomicity difference (original samples one `new Date()`, replacement calls Date.now() and new Date() separately) is theoretically observable across a DST flip between the two calls — irrelevant for a display-only build stamp, noted for completeness.

**Risk of applying as written:**

Applying it as written is safe: the string is never parsed, only rendered in the settings BuildInfo widget and one confirm dialog, and the two helpers it deletes have no other users. vite.config.js:1 carries `// @ts-nocheck` so the new line is exempt from the repo's `checkJs: true`. The change is frontend/config, so CLAUDE.md rule 6 requires `pnpm check` + `pnpm vitest run` + `pnpm build` before committing; no test file exists or is needed for vite.config.js. Only real downside is stylistic: the offset-arithmetic idiom is the clever variant of this trick (people routinely misapply it as a general-purpose UTC->local converter), and the audit's own sibling finding #52 preferred the locale route; the boring one-liner with identical output is `new Date().toLocaleString('sv').replace(' ', 'T')` (Swedish locale renders `YYYY-MM-DD HH:MM:SS`, and Node ships full ICU). Either is a single line — no reason to reject the finding, just an alternative worth offering.

### #54 — CONFIRMED — `flushPendingSaves()` in editor.service.ts — flushes the two autosave debounce timers. No production code calls it; app-close/vault-switch use `saveAl

- Path: `src/lib/core/editor/editor.service.ts:192` | tag `delete` | claimed 5 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (fable-5 cross-exam):**

None material. Same extra step the first agent listed: delete the test import at src/tests/lib/core/editor/editor.service.test.ts:70 and the describe block :403-424 (verified bounds — saveAllDirtyTabs describe starts at :426).

**Risk of applying as written:**

Low. Only required extra step: delete the import at src/tests/lib/core/editor/editor.service.test.ts:70 and the whole `describe('flushPendingSaves')` block at :403-424 (~22 lines). Nothing else references it. Note the deleted test was the only coverage asserting that a pending debounced autosave can be forced to disk synchronously — but since no production path does that, no coverage of live behavior is lost.

### #55 — CONFIRMED — `closeVault()` in vault.service.ts — a 3-line wrapper that only forwards to `vaultStore.close()`, with no callers at all.

- Path: `src/lib/core/vault/vault.service.ts:38` | tag `delete` | claimed 5 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (fable-5 cross-exam):**

None material beyond the first agent's extra step (delete the vault.service.test.ts import at :37 and the describe('closeVault') block starting :141; store-close semantics stay covered by vault.store.test.ts:94-105).

**Risk of applying as written:**

Low. Required extra step the entry does not spell out: delete the import at src/tests/lib/core/vault/vault.service.test.ts:37 and the whole `describe('closeVault')` block at :141-168 (~28 lines, two cases: 'closes the vault and clears state' and 'preserves recent vaults after closing'). Those assertions test vaultStore.close() semantics, which are already covered independently in src/tests/lib/core/vault/vault.store.test.ts:39/:97/:105 — so no real coverage is lost. Only forward-looking cost: if a 'Close vault' UI is ever added it will call vaultStore.close() directly, which is fine.

### #56 — PARTIAL — `Reranker::with_batch_size` — a builder override for `batch_size` that no caller ever chains

- Path: `src-tauri/src/semantic/reranker.rs:91` | tag `delete` | claimed 5 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

None beyond the first agent's two (test-file `//!` doc must be updated in the same commit; `pub` lib-crate API note). Both re-verified accurate.

**Risk of applying as written:**

Deleting the method itself is safe — no caller, no registry, no doc. Two corrections the audit omits: (1) **src-tauri/tests/semantic_reranker_test.rs:3-9** is a `//!` module doc that explicitly lists "`with_batch_size()` chaining" as an EXCLUDED test surface. It is a comment so nothing fails to compile, but it becomes a false statement about the code, and project rule 11 ("check test files BEFORE committing") means that file has to be edited in the same commit — the audit flagged the comment only to dismiss it as "not a call" and never said it needs updating. (2) Because `Reranker` is `pub` in a `pub mod` of the `kokobrain_lib` lib crate, this is nominally a library-API removal; verified harmless here (only the binary + src-tauri/tests link it), but it also explains why no compiler warning ever surfaced this. Line count is 5 as claimed, 6 in practice once the orphaned blank line goes. `cargo test --manifest-path src-tauri/Cargo.toml` is the required gate; `default_constants_regression` (:16-22) still passes since `DEFAULT_BATCH_SIZE` stays.

### #57 — REFUTED — `getWatcherCounters()` in fs.watcher.ts — snapshot accessor for the watcher debug counters, read only by tests (the counters themselves are still logg

- Path: `src/lib/core/filesystem/fs.watcher.ts:44` | tag `delete` | claimed 4 lines | opus-5: REFUTED, fable-5: REFUTED

**Corrections (fable-5 cross-exam):**

One nuance weakening (but not undoing) the doc-protection leg: ADR 0017's body is stale well beyond the nonexistent 'debug UI' — it documents the pre-Phase-9 TS watcher (plugin-fs watch() with delayMs, `counters.accepted`, `counters.skippedKokobrain`, hidden-dir filter at fs.watcher.ts:22-33) while the current fs.watcher.ts (header comment :9-28) is a thin consumer of the Rust-native `vault-files-changed` event and its counters struct (:31-37) has entirely different fields — `counters.accepted` cited by ADR line 45 does not exist. So the protecting ADR needs correcting regardless of this finding; the honest larger finding (delete the whole counters block + amend ADR 0017) that the first agent sketched remains the right framing.

**Risk of applying as written:**

Runtime safety is not the problem — nothing in production calls it. The finding fails on two grounds. (1) Documented intent: deleting it silently invalidates an active ADR; applying the finding as written requires amending docs/adr/0017 §Consequences line 45, which the entry does not mention (and its verification explicitly denied the ADR exists). Note the ADR is already partly stale — the "debug UI" it claims as a consumer does not exist, and `counters.skippedAncestorPaths` (declared fs.watcher.ts:34, reset :236) is never incremented anywhere, so the ADR needs correcting either way. (2) The proposed replacement is not equivalent: "tests can assert on watcher effects instead of instrumentation" fails for the counters that have no observable effect. src/tests/lib/core/filesystem/fs.watcher.test.ts:288-297 ('increments rawEvents and debounceFires per Rust-emitted batch') has no non-counter assertion at all and would simply be deleted; :278-286 ('starts with all counters zeroed') and :299-310 ('resets counters on stopWatching') verify the counter reset at fs.watcher.ts:236 that no other API exposes; :443 supplements an existing `expect(refreshTree).not.toHaveBeenCalled()`. Net effect of applying it as written: -4 production lines, ~40 test lines deleted, three behaviors made unverifiable, and one active ADR left lying. If the goal is genuinely to shrink this, the honest version deletes the whole counters block (fs.watcher.ts:30-46, :128-129, :140, :159, :185, :193, :236 and logCounters at :39-41/:135) and updates ADR 0017 — a different, larger finding.

### #58 — CONFIRMED — core/types.ts — DecorationEntry has zero references and LineInfo just restates CodeMirror's own `Line` type (text/from/to/number)

- Path: `src/lib/core/markdown-editor/extensions/live-preview/core/types.ts` | tag `delete` | claimed 4 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

One arithmetic nuance in the audit's favour: `-4` counts only the deleted file, but `get-all-lines.ts:2` (`import type { LineInfo } from './types';`) also disappears — `Line` folds into the existing line 1 as `import type { EditorState, Line } from '@codemirror/state';` — so the real net cut is 5 lines. Also worth noting explicitly: `get-all-lines.ts` has no test file at all (no `src/tests` hit for getAllLines), so the change ships with zero direct coverage; the only guard is `pnpm check` type-checking the 8 downstream plugins.

**Corrections (fable-5 cross-exam):**

Two additions: (a) the jsdoc at get-all-lines.ts:4 ('Builds a LineInfo array...') references the deleted type name and needs a one-word edit — the first agent's hit count of 4 for LineInfo missed this comment line; (b) I confirm their net -5 arithmetic (types.ts 4 lines + the import at get-all-lines.ts:2, with `Line` folding into line 1's existing `import type { EditorState }` from @codemirror/state). Cosmetic only; verdict unchanged.

**Risk of applying as written:**

Very low, and TypeScript-only — no runtime API, no jsdom, no IPC. The one thing that could have broken it (a shared/mutable `Line` instance returned by `doc.line()`) was checked in the dependency source and disproved: `lineInner` allocates a new Line per call. All 8 consumers type their `lines` parameter structurally as `{text;from;to}[]`, so `Line[]` satisfies them; a regression would surface immediately as a `pnpm check` error, not as silent misbehaviour. No test file references getAllLines or either type, so no tests need updating (and none exercise the change). `docs/LIVE-PREVIEW.md:22,95` mention `getAllLines` but not `LineInfo`, so no doc edits are required. Per CLAUDE.md rule 11 a reviewer may still want a small test for getAllLines, since there is currently none.

### #59 — PARTIAL — search.logic.ts:154 getFileName is byte-identical to backlinks.logic.ts:59 getNoteName (split '/', strip extension)

- Path: `src/lib/features/search/search.logic.ts:154` | tag `delete` | claimed 4 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) "byte-identical" is false — diff rc=1 on all four lines (function name + `name` vs `fileName`). Semantically identical only. 2) The `delete` tag is wrong; search.logic's copy is live at :183, so it is a dedup + import swap (the audit's Verification already concedes this). 3) Unstated prerequisite: search.logic.test.ts:11 and :269-280 must be edited. 4) The cross-feature-import objection is weaker than stated — search.logic.ts:2 already imports features/tags/tags.logic. 5) Must be merged with #51 to avoid two competing homes for the same stem helper.

**Corrections (fable-5 cross-exam):**

Small precision on the first agent's coverage claim: the search-side describe block has 3 cases (extracts name / nested paths / no extension), not 'the same four cases' as backlinks.logic.test.ts:97-112 — the backlinks suite is a strict superset, so deleting the search-side block still loses no coverage, but the counts in the first agent's text are off by one.

**Risk of applying as written:**

Low mechanically — one internal call site (search.logic.ts:183) swaps to the imported name. Two prerequisites the entry omits: src/tests/lib/features/search/search.logic.test.ts must drop getFileName from its import at :11 and either delete describe('getFileName') at :269-280 or re-point it (backlinks.logic.test.ts:97-112 already asserts the same four cases on getNoteName, so the search-side block is pure duplicate coverage).
Cross-finding conflict, the material one: #51 proposes a shared `stem()` in src/lib/utils/path.ts, which is the same function. Applying #59 as written (search imports from features/backlinks) and #51 as written (stem in utils/path.ts) produces two homes for one helper and leaves backlinks.getNoteName + link-updater.extractNoteName + search.getFileName as three names for one body. Resolve as one change: stem() in utils/path.ts, all three delegate or are replaced.

### #60 — CONFIRMED — `is_char_boundary(s, byte_pos)` free function that re-wraps the std method with two redundant guards

- Path: `src-tauri/src/vault/parsing.rs:1064` | tag `stdlib` | claimed 4 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (fable-5 cross-exam):**

None material. The first agent's advice to keep the :1033-1034 explanatory comment is sound but optional; with production unreachability now established, even dropping the `idx > content.len() ||` term is safe on both the std contract and the reachability argument.

**Risk of applying as written:**

Minimal. The two call sites must be rewritten in **method** form — `content.is_char_boundary(idx)` at :1035 and `content.is_char_boundary(after_byte_pos_clamped)` at :1046 — because deleting the free fn makes the two-argument call form a compile error (a loud failure, not a silent one). Keep the explanatory comment at :1033-1034 ("prevents panics when stripped_lower and content have different byte lengths due to emojis"): dropping the `idx > content.len() ||` term is only correct because std returns false out of range, and that comment is the only place that rationale is recorded. In practice the removal is 5 lines, not 4, once the orphaned blank separator goes. No test references the free function; parsing.rs's `find_plain_text_mention_positions` suite (:2371-2530) and the two vault/index.rs callers are unaffected. Gate is `cargo test --manifest-path src-tauri/Cargo.toml`.

### #61 — CONFIRMED — `parseDate` in date.ts — exported dayjs parse wrapper with no production caller

- Path: `src/lib/utils/date.ts:52` | tag `delete` | claimed 4 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Low. Deleting date.ts:51-54 breaks `src/tests/lib/utils/date.test.ts` unless the same commit also removes `parseDate` from the import list at :3 and the `describe('parseDate')` block at :29-35 — the audit flagged the describe block but not the import line; both must go or `pnpm check` fails on an unresolved import. Per CLAUDE.md Quick Reference rule 6 the frontend gate (`pnpm check` + `pnpm vitest run` + `pnpm build`) applies. Coverage note: parseDate is the only place `dayjs(str, fmt)` is called NON-strictly; deleting it removes that test, but no production behavior depends on it.

### #62 — PARTIAL — `today(format)` in date.ts is a duplicate of `formatNow(outputFormat, 0)` — same dayjs call, differing only by a default argument

- Path: `src/lib/utils/date.ts:15` | tag `shrink` | claimed 4 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (fable-5 cross-exam):**

First agent's correction (3) is wrong and replaced: periodic-notes.logic.ts:90 belongs to getTodayPeriodicNoteTitle (:86-91), not getPeriodicNoteTitle (:79-84); there is NO overlap or ordering constraint with finding #32 (spans 79-84/100-105/107-112). The actual sequencing dependency is with finding #13, which deletes getTodayPeriodicNoteTitle and the then-unused `today` import from date.ts — apply #13 first and #62 shrinks to rewriting only the 3 kanban no-arg call sites (or vice versa, but the two findings both edit the `today` surface and must not double-count the periodic-notes site). Also: getTodayPeriodicNoteTitle is test-only dead code, so "4 production call sites, all reachable statically" over-counts by one.

**Risk of applying as written:**

Correct but the weakest item on the list, and it is not a pure 4-line win as written. Applying it as specified loosens the `formatNow` signature (`outputFormat` becomes optional), so a future `formatNow()` typo silently returns an ISO date instead of raising a TS error — the lazier and safer variant is to leave `formatNow(outputFormat, offsetDays = 0)` untouched and rewrite the 4 call sites as `formatNow('YYYY-MM-DD')` x3 and `formatNow(format)` x1, which deletes the same 4 lines with no API weakening. Either way the real diff is larger than 4 lines once `date.test.ts:3` and the `describe('today')` block at :5-15 are updated, and the 3 Svelte call sites get marginally less readable (`{@const proximity = getDateProximity(cardDate, formatNow())}`). Gate: frontend-only change -> `pnpm check` + `pnpm vitest run` + `pnpm build`. Sequence with finding #32 to avoid a conflicting edit at periodic-notes.logic.ts:90.

### #63 — CONFIRMED — `SETTINGS_SECTIONS` — a flattened copy of SETTINGS_SECTION_GROUPS "for lookups and iteration". Nothing looks up or iterates it; SettingsPanel.svelte r

- Path: `src/lib/core/settings/settings.logic.ts:107` | tag `delete` | claimed 3 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

None.

**Corrections (fable-5 cross-exam):**

None beyond the first agent's.

**Risk of applying as written:**

Essentially none — a derived constant with no reader, and its stated purpose ("for lookups and iteration") is served by SETTINGS_SECTION_GROUPS.flatMap at the two places that actually need a flat list (SettingsPanel iterates groups; the test at :204-206 does its own flatMap). Required extra step disclosed by the audit's own evidence: delete settings.logic.test.ts:218-224 (the `describe('SETTINGS_SECTIONS')`) and the name from the import at :11. Keep the `SettingsSectionItem` interface — it is still referenced by `SettingsSectionGroup.sections` at settings.logic.ts:57.

### #64 — PARTIAL — 6 of the 12 live-preview decorator kill-switches in the Troubleshooting settings pane are no-ops — nothing reads them

- Path: `src/lib/core/settings/sections/TroubleshootingSection.svelte:14` | tag `delete` | claimed 3 lines | opus-5: CONFIRMED, fable-5: PARTIAL, arbitrated

**Corrections (opus-5 deep dive):**

Cosmetic only: the -3 is really -2 with natural formatting (lines 13-17 collapse from 5 lines to 3), or -4 if the six survivors go on one line. Also worth knowing but not a defect in the finding: several BLOCK fields are also pushed ungated at live-preview.ts:56,59,61,65 (blockCommentField, collectionBlockField, metaBindButtonField, mermaidField, blockMathField, audioPlugin, videoPlugin, imagePlugin, footnotePlugin, wikilinkEmbedPlugin), so ADR-0008:33's "block fields ... with per-feature disable flags" is itself only partly true today.

**Corrections (fable-5 cross-exam):**

Beyond the first agent: (1) help/documentation/19-settings.md:245 must be updated in the same change (see reasoning) — neither the audit nor the first agent found it. (2) Their already-noted caveats re-verified: CLAUDE.md:241 and ADR-0008:78 advertise per-feature decorator isolation as the standard live-preview debugging workflow, which after this delete covers block widgets only — wiring the six into inlineExtensions() is a legitimate alternative to deletion and the audit presents delete as the only option; ADR-0008:33's 'block fields pushed individually with per-feature disable flags' is already only partly true (blockComment, collection, metaBindButton, mermaid, blockMath, audio, video pushed ungated at live-preview.ts:56,59,61). (3) Line count -3 is really -2 with natural formatting (first agent noted this as cosmetic; combined with the doc omission it supports PARTIAL). (4) Stale persisted keys in .kokobrain/settings.json survive harmlessly via the settings.service.ts:143-145 spread — confirmed.

**Risk of applying as written:**

Low. Users who previously flipped one of the six dead switches have a stale key persisted in .kokobrain/settings.json; settings.service.ts:143-145 spreads `...parsed.disabledDecorators` so the key survives in memory but has no reader and no longer renders a row — harmless. The real caveat the audit does not raise: CLAUDE.md:241 and ADR-0008:78 both advertise "disable per-feature decorators via settingsStore.disabledDecorators to isolate which plugin causes lag" as the standard live-preview debugging workflow. After this delete the inline pipeline has zero isolation switches, so that workflow covers block widgets only. Nothing is functionally lost (the six already do nothing), but if inline-pipeline lag is a recurring debugging need, the honest fix is to WIRE the six into inlineExtensions() rather than delete them — the audit presents delete as the only option. Whichever way it goes, update CLAUDE.md:241 / ADR-0008 so the doc stops promising per-feature isolation the code cannot deliver.

**Arbitration:**

Agent B was right. The core claim is true and I re-verified every leg myself: DECORATOR_NAMES (TroubleshootingSection.svelte:13-17) lists 12 names; the only two value-readers of disabledDecorators in production code are live-preview.ts:35 (isDisabled) and TroubleshootingSection.svelte:20; isDisabled() gates exactly six names (live-preview.ts:54 frontmatter, :55 codeBlock, :57 table, :58 callout, :60 queryjs, :66 metaBindInput); the inline pipeline (live-preview/inline/) contains zero settingsStore/disabledDecorators references and inlineExtensions() is pushed unconditionally, so link, inlineMarks, simpleWidget, heading, blockquote, markdownStyle are dead switches with no string-keyed dispatch, dynamic import, or test reaching them. ADR-0008:33 scopes the flags to block fields, so no documented intent protects the six. The decisive disputed fact — the one Agent A missed — checks out: help/documentation/19-settings.md:245 (user-facing Kokobrain User Guide, per help/documentation/README.md, deliberately maintained per tasks/done/help-docs-update.md task 12) enumerates all 12 decorator names verbatim in the Disabled decorators row. Agent A's census used grep --include="*.ts" --include="*.svelte" scoped to src, which structurally could not find it, and the audit entry ('Replacement: nothing') omits it. Acting on the finding exactly as written leaves user help advertising six switches the pane no longer renders, so the entry has a material omission: the same change must drop the six names from 19-settings.md:245 (and ideally fix the stale per-feature-isolation promises at CLAUDE.md:241 and ADR-0008:78, which Agent A did flag). The -3 line count is also slightly off (array collapses from 3 name-lines to ~2, net -1/-2). Core claim true, deletion safe functionally, but the audit entry has omissions and a wrong line count → PARTIAL.

### #65 — PARTIAL — The `tagsVisible` setting: declared in settings.types.ts and given a default in the store, but no UI toggles it and no code reads it.

- Path: `src/lib/core/settings/settings.types.ts:63` | tag `delete` | claimed 2 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) SCOPE ERROR: the audit's grep covered only `src src-tauri/src scripts` and therefore missed e2e/ entirely. "Zero readers, zero writers, not even a test" is false — three e2e files write `tagsVisible` into the settings.json they seed (e2e/fixtures/test-vault.ts:51 `tagsVisible: true`, e2e/fixtures/live-preview.ts:12 `tagsVisible: false`, e2e/specs/queryjs-basic.spec.ts:38 `tagsVisible: false`). They will not break the build (they are untyped object literals with no `LayoutSettings` annotation, and e2e/ is not in the svelte-check include set — `.svelte-kit/tsconfig.json` includes only `../src/**`, `../test/**`, `../tests/**`), but they become stale dead data and should be cleaned in the same commit. 2) LINE COUNT: it is 3 lines, not 2. settings.types.ts:62 is the JSDoc `/** Whether the tags panel is shown in the right sidebar */`, which must go with the field — CLAUDE.md § Conventions mandates JSDoc on every interface field, so leaving it orphaned above tableOfContentsVisible would mislabel that field. So: types.ts:62-63 + store.svelte.ts:59. 3) MISSING CONTEXT: the audit does not establish that no Tags panel exists in the right sidebar, which is what actually distinguishes 'dead setting' from 'broken feature wire' (evidence above).

**Corrections (fable-5 cross-exam):**

Beyond the first agent: (1) Their interpretive frame is wrong on both prongs — the Tags panel was neither 'never built' nor 'planned with the flag as the last stub': it existed in the right sidebar and commit bf55080 (2026-05-25) deliberately moved it to a virtual tab and 'Removed from right sidebar and settings toggle'; tagsVisible is cleanup that commit forgot, so the delete needs no user judgment call about a planned feature. (2) The same commit orphaned src/lib/features/tags/TagsPanel.svelte entirely — `rg -n TagsPanel src e2e` finds zero importers, only comment mentions (tags.service.ts:88, index-updater.service.ts:25) — a whole dead component the audit missed, a bigger deletion candidate than the 2-line flag; TagsView.svelte is a separate full-tab reimplementation, and it is gated by `{:else if isTagsTab}` at EditorView.svelte:51-52, not 'ungated' as the first agent wrote. (3) More stale references neither found: settings.types.ts:53 JSDoc for rightSidebarVisible still lists 'Tags' among right-sidebar panels; help/documentation/07-sidebar-panels.md:116-142 documents the right-sidebar Tags panel with a screenshot and 06-search-and-navigation.md:136 says 'Tags panel (right sidebar)' — all stale since bf55080; CLAUDE.md Indexing §2 also names TagsPanel as a live reactive consumer.

**Risk of applying as written:**

Low to delete, but the user should make the call knowingly. The docstring names a feature — "the tags panel is shown in the right sidebar" — that was never built; the four sibling panels all have their toggle in GeneralSection.svelte and their guard in AppShell.svelte. Deleting the key is the honest read (orphan); the alternative read is that a Tags right-sidebar panel was planned and the flag is the last stub of it. Nothing crashes either way: settings.service.ts merges `...parsed.layout` from disk, so an old settings.json carrying tagsVisible just contributes an ignored extra key after the type is gone. Concretely required: types.ts:62-63, store.svelte.ts:59, and — to avoid leaving misleading fixture data — the three e2e lines. No test in src/tests touches it, so `pnpm vitest run` is unaffected; `pnpm check` + `pnpm build` still needed per CLAUDE.md rule 6.

### #66 — CONFIRMED — `ColorPresetName` type export in color-presets.ts — never referenced

- Path: `src/lib/utils/color-presets.ts:33` | tag `delete` | claimed 2 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

None. Type-only deletion, erased at compile time, zero runtime surface, no test to update (color-presets.test.ts imports only the two constants). The only thing lost is a self-documenting name, and it does not even document correctly — it resolves to `string`, not a union of the seven palette keys, because PALETTE is annotated `Record<string, ...>`. If a real union is ever wanted the fix is `const PALETTE = {...} as const satisfies Record<string, [number,number,number]>`, which is an addition, not a reason to keep this line.

### #67 — PARTIAL — cargo `uuid` dependency (with the `v4` feature) — zero references in Rust source or tests

- Path: `src-tauri/Cargo.toml:36` | tag `delete` | claimed 1 lines | opus-5: CONFIRMED, fable-5: PARTIAL, arbitrated

**Corrections (opus-5 deep dive):**

None material. Two additions the audit missed, both of which strengthen rather than weaken it: (a) docs/adr/0022-terminal-portable-pty.md:26 explicitly documents this dependency ("uuid v4 (Cargo.toml:30) for session IDs"), but that ADR is `status: superseded` and portable-pty / the terminal plugin are gone from Cargo.toml and src/lib/plugins/ - so the dep is a confirmed removal leftover, not intentional; (b) docs/superpowers/plans/2026-07-03-p2p-sync.md:1843 contains `uuid::Uuid::new_v4()` in a not-yet-implemented plan. Neither is a caller. Also unstated: Cargo.lock regeneration.

**Risk of applying as written:**

Effectively zero runtime risk: no code path exists. The crate and its `v4` feature both remain in the resolved graph via tauri-codegen/tauri-utils/cfb/schemars, so not even compile output changes. Extra step the audit omits: src-tauri/Cargo.lock is tracked (`git ls-files src-tauri/Cargo.lock` -> tracked; `grep -n '^name = "kokobrain"' -A45` lists "uuid") and must be regenerated and committed with the Cargo.toml edit. Per CLAUDE.md rule 6 a Rust-only change requires `cargo test --manifest-path src-tauri/Cargo.toml` before commit. Docs: nothing needs updating, but note docs/adr/0022 (already superseded) and docs/superpowers/plans/2026-07-03-p2p-sync.md:1843 (`uuid::Uuid::new_v4()` inside an unimplemented plan) would each require re-adding the dep if that plan is ever built.

**Arbitration:**

Agent B was right. The finding's core claim holds at HEAD: Cargo.toml:36 declares uuid with the v4 feature, `grep -rniw uuid src tests build.rs` from src-tauri returns zero hits (rc=1), and `cargo tree --offline -i uuid -e features` shows uuid v1.24.0 and even its v4 feature remain in the resolved graph via tauri-codegen/tauri-utils/cfb/schemars, so depsSaved 0 is correct and the delete is safe for main today. But the decisive disputed fact settles against Agent A's CONFIRMED: I verified myself that feature/p2p-sync exists both locally and at origin (tip 4b64de7, 2026-07-05), is NOT merged into main (git merge-base --is-ancestor b2f31fb HEAD fails), and contains real implemented code using the dep at src-tauri/src/sync/engine.rs:321 (`uuid::Uuid::new_v4()`) among 2190 inserted lines including 542 lines of sync tests. The branch's diff touches the uuid Cargo.toml line only as hunk context (no +/-), meaning it inherits main's declaration: deleting uuid from main leaves that pushed branch compile-broken on any future merge. Agent A's correction text calling this 'a not-yet-implemented plan' is factually wrong. Additional omissions in the audit entry confirming PARTIAL rather than CONFIRMED: the tracked src-tauri/Cargo.lock lists "uuid" under kokobrain and must be regenerated and committed with the edit; ADR 0022 cites the dep but is status: superseded (stale terminal-plugin leftover, removal commit f5cc43c), so no doc protects it. Not REFUTED because no caller exists at HEAD and no living doc protects the dep; the deletion just needs coordination with the live feature/p2p-sync branch (re-add uuid on merge or abandon the branch) plus the Cargo.lock regeneration step.

### #68 — CONFIRMED — cargo `percent-encoding` dependency — zero references in Rust source or tests

- Path: `src-tauri/Cargo.toml:51` | tag `delete` | claimed 1 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Corrections (opus-5 deep dive):**

None. All material claims re-verified: line 51 exact, zero Rust references (including build.rs), only repo-wide hit is the declaration itself, `similar` genuinely has 4 qualified hits so the audit's parenthetical is accurate, and the depsSaved 1 -> 0 correction the audit already applied is right. Unstated but mechanical: Cargo.lock regeneration.

**Risk of applying as written:**

Zero runtime risk - the crate is never named in any Rust source, test, or build script, and it stays in the compiled graph anyway via tauri/reqwest/url/cookie. Extra step the audit omits: src-tauri/Cargo.lock is tracked and lists "percent-encoding" under the kokobrain package (`grep -n '^name = "kokobrain"' -A45 src-tauri/Cargo.lock`), so it must be regenerated and committed. Per CLAUDE.md rule 6, a Rust-only change requires `cargo test --manifest-path src-tauri/Cargo.toml` before commit. No tests or docs reference the dep, so nothing else needs updating. Only cosmetic caveat: do not delete the `# File History` comment at line 49 - `similar` at line 50 is live (used by src/commands/history.rs).

### #69 — PARTIAL — cargo `objc2-core-foundation` dependency — fonts.rs declares its own `extern "C"` CoreFoundation FFI instead of using the crate

- Path: `src-tauri/Cargo.toml:68` | tag `delete` | claimed 1 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1. The Verification's claim "the crate compiles regardless" is only half true and understates the change. The crate stays in the graph, but `cargo tree --offline -i objc2-core-foundation -e features` shows `objc2-core-foundation feature "default"` has exactly one enabler - kokobrain. Removing our line turns `default` off, which turns off ~30 CF* module features plus `block2`, `dispatch2` and `libc` for the whole build. The audit presents this as a zero-effect line deletion; it is a feature-unification change that must be validated with a real cargo build.
2. depsSaved 0 is nevertheless correct - verified that dispatch2 (rfd, tao) and block2 (objc2-app-kit, objc2-foundation, objc2-web-kit, rfd, tao, wry) both keep other dependents, and `std`/`alloc` are enabled by arboard/muda/rfd/sysinfo/window-vibrancy/wry independently of us.
3. Unstated prerequisite: Cargo.lock regeneration and the CLAUDE.md rule-6 Rust test gate.
4. Missing caveat worth recording: docs/adr/0013 (status: superseded) cites "Cargo.toml:68" for objc2-local-authentication, a crate that is absent from Cargo.toml; anyone grepping the ADRs for line 68 will get a false positive.
5. Everything else in the entry re-verified exactly as written: zero `objc2_core_foundation` references anywhere in the repo, fonts.rs:3 doc comment + fonts.rs:27 `#[link(name = "CoreFoundation", kind = "framework")]`, 12 `objc2` use sites, 2 `objc2_foundation` sites.

**Corrections (fable-5 cross-exam):**

Minor count fix to the first agent: the solely-default-enabled feature set is exactly 27 (24 CF* + block2 + dispatch2 + libc), not '~30'; their list also named CFPlugInCOM, which is enabled transitively via CFPlugIn rather than directly by default. Immaterial to the verdict.

**Risk of applying as written:**

The declaration is genuinely dead code-wise, but removing it is a BUILD-AFFECTING edit, not a no-op comment removal, because kokobrain is the sole enabler of the crate's `default` feature. After removal, objc2-core-foundation compiles with a much smaller module set (~30 CF* features plus block2/dispatch2/libc turned off). If any dependent - arboard, muda, objc2-app-kit, objc2-core-graphics, objc2-foundation, objc2-io-kit, objc2-web-kit, rfd, sysinfo, window-vibrancy, wry - currently free-rides on a CF* item it did not declare, the macOS build breaks with unresolved-path errors. The objc2 family is normally rigorous about feature declarations so probability is low, but this cannot be proven read-only; it requires a real `cargo build`/`cargo test --manifest-path src-tauri/Cargo.toml` and ideally a release build (some paths are cfg/profile-gated) before committing. Secondary: src-tauri/Cargo.lock is tracked and lists "objc2-core-foundation" under kokobrain, so it must be regenerated and committed. Tests: src-tauri/src/commands/fonts.rs:77-112 has four macOS-gated tests that exercise the raw FFI; they must still pass after the removal (they will - they touch no objc2 crate). Docs needing updates: none strictly, but docs/adr/0013:51 points at "Cargo.toml:68" for a crate (objc2-local-authentication) that no longer exists there, and after this edit line 68 becomes objc2-foundation, making the stale reference even more misleading.

### #70 — CONFIRMED — file-icons IconPackMeta.iconCount — hardcoded 0 for all 12 packs and never read anywhere

- Path: `src/lib/features/file-icons/file-icons.icon-data.ts:15` | tag `delete` | claimed 1 lines | opus-5: CONFIRMED, fable-5: CONFIRMED

**Risk of applying as written:**

Negligible. The field is write-only dead weight; removing it from the interface is a compile-time-checked change that `pnpm check` verifies in one pass. One trivial tidy the entry omits: delete the JSDoc line file-icons.types.ts:30 along with the field at :31, otherwise the comment dangles above the closing brace (making it a 2-line deletion, not 1). If the intent was ever to surface a real pack size in IconPicker, the honest fix is to compute it from the loaded pack rather than reinstate a hardcoded 0.

### #71 — PARTIAL — quick-switcher.logic.ts:4 barrel re-export `export { fuzzyMatch, type FuzzyMatchResult } from '$lib/utils/fuzzy-match'` — FuzzyMatchResult has zero co

- Path: `src/lib/features/quick-switcher/quick-switcher.logic.ts:4` | tag `delete` | claimed 1 lines | opus-5: PARTIAL, fable-5: PARTIAL

**Corrections (opus-5 deep dive):**

1) "fuzzyMatch has exactly one [consumer]" is false — there are two importers of the re-export: completion.logic.ts:2 and src/tests/lib/features/quick-switcher/quick-switcher.logic.test.ts:4. The audit's enumeration filtered out src/tests and never re-ran without the filter. 2) Unstated prerequisite: the test file's import block and its describe('fuzzyMatch') at :74-121 must be edited/deleted or vitest and pnpm check fail. 3) The FuzzyMatchResult-has-zero-consumers half is fully correct (3 hits, all declaration/re-export).

**Corrections (fable-5 cross-exam):**

One count fix to the first agent's test-dedup claim: the duplicated describe('fuzzyMatch') block and src/tests/lib/utils/fuzzy-match.test.ts each contain 9 it-cases with identical titles (verified via `rg -c "it\("` -> 9 and side-by-side `rg -n "it\('"` listings), not 10 — the coverage-not-lost conclusion is unchanged.

**Risk of applying as written:**

Deleting line 4 without touching the test file breaks it at runtime AND at type-check: vite/ESM throws on a named import that the module no longer provides, so `pnpm vitest run` and `pnpm check` both fail. The fix is two edits, not one: (a) completion.logic.ts:2 re-points to '$lib/utils/fuzzy-match' (net 0 lines — line 1 already imports the FileEntry type from quick-switcher.logic and must stay); (b) quick-switcher.logic.test.ts drops `fuzzyMatch` from the import block at :2-8 and deletes describe('fuzzyMatch') at :74-121 — that block is a near-verbatim duplicate of src/tests/lib/utils/fuzzy-match.test.ts (same 10 cases: empty query, exact, prefix, scattered, out-of-order, case-insensitive, no-match, prefix-vs-scattered scoring, length penalty), so coverage is not lost. That is a ~48-line test win the entry never claims.

