# Cross-audit run state (2026-08-19)

Resume with: `.scratch/cross-audit-plan-2026-08-12/continuation-prompt.md`
Workflow script: scratchpad `issue-cycle.workflow.js` (takes `issues[]`, `branch`,
`worktree`, `gate`, `reviewModel`; groups several issues per worktree).

## Closed this session (11)
46, 48, 26, 28, 27, 47, 30, 34, 49, 37, 38 — all merged ff-only into main.
main was at d6f4bbef at checkpoint.

## In flight (4 worktrees, workflows were running at checkpoint)
- issue-29        -> 29 (keystone: applyNoteChange, 8 call sites, 6 writers, 2 ADRs)
- issue-31-32-33  -> settings chain; 31 has a MANDATORY $effect.root spike gate.
                     If the spike fails, 31/32/33 all stop as needs-info.
- issue-41-42     -> queryjs KBDateTime + kb-ui preamble
- issue-40-50     -> task-metadata deletion (must re-file the dependsOn regex bug
                     as new issue 51 BEFORE deleting) + table-field frontmatterSlice
Check each worktree's branch for commits before relaunching anything.

## Not yet started
- 25 (typed openNoteAt) — free, no blockers
- 35 (collection producer + removeRecord) — needs 29 merged
- 39 (applyPathChange) — needs 29 merged
- Skips: 43 (needs-info, external freeze investigation), 44 (ready-for-human),
  45 (wontfix ledger). Do not delete these files.

## Debt to fix in a final cleanup commit
1. REGRESSION (real, not cosmetic) TypeNoteList.svelte selfUpdate branch: it now
   adopts seededViewPath/seededViewHash for whatever view is current, so a view
   switch racing an in-flight persistViewState latches the NEW view as seeded with
   the OLD view's toolbar state, and unlike the pre-fix code nothing heals it later.
   Needs a red test.
2. QuickSwitcher.svelte: `$lib/utils/path` import sits between two local `./`
   imports, violating the documented import grouping.
3. app-lifecycle.service.test.ts (~:276): comment claims loadSettings is the only
   awaited call without try/catch in initializeVault. False: saveAllDirtyTabs() and
   ensureTemplatesFolder() are too.
4. Em-dashes committed against the repo rule: TypeNoteList.svelte:245,248 and
   TypeNoteList.view.test.ts:20,24,12.

## Non-deterministic / suspect tests seen on untouched trees
- TypeNoteList.perf.test.ts — hard 1500 ms wall-clock ceiling, fails under parallel
  worktree load (saw 1637 ms). Should be made load-independent.
- live-preview inline/pipeline-dom.test.ts — failed then passed on an untouched tree.
- live-preview parsers/table.test.ts `findAllTables > finds multiple tables` — got 1
  table, expected 2, on an untouched tree at ed1a7b38, while other runs at the same
  commit were green. Smells like test pollution / order dependence, not timing.
  Worth a real investigation, this one is not a clock flake.

## Follow-up candidates surfaced by reviewers
- buildIndex swallows IPC errors, so markIndexReady() stays optimistic on a failed scan.
- teardownVault() racing a live scan clears isBuilding/pendingRebuild mid-flight.
- Folder rename/move leaves child notes' dedupe signatures stale (scope of issue 39).
- Rust scan_vault `sort_by` arg is now provably constant 'name' from the only client.
- Carried from 2026-08-18: stale Tags-panel help docs; docs/LIVE-PREVIEW.md block-plugin
  template; isExternalEdit/isTabSwitching latch try/finally hardening; settingsKey vs
  registry-key drift; watcher external-delete not clearing the view parse cache.
