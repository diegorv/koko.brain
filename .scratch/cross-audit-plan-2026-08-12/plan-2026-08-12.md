# Kokobrain Remediation Plan — merged ARCH + PONY audits (2026-08-12, HEAD clean)

## Executive summary

- **Inputs:** 22 ARCH findings + 8 live bugs (LB1–LB8); 75 PONY findings (#1–#71, B1–B4). 18 verified overlap clusters, of which **5 are genuine conflicts** (C01–C05), all resolved below with code-verified evidence.
- **Headline conflicts:** wire-vs-delete on the six dead decorator toggles (C01, **wire wins**), the view-parse-cache clears (C02, **wire wins**), collection `removeNoteFromIndex`/`removeRecord` (C03, **wire wins — deleting would make bug M08 permanent**), the seven settings clamps (C04, **keep wrappers, shrink bodies**), and `isInsideSystemFolder` (C05, **delete wins**). PONY **#57 is REFUTED — never apply as written**; PONY **#64 is dropped** (refuted by construction once C01's plumbing lands).
- **The single most important ordering rule:** *every live user-facing bug gets a standalone commit with a regression test BEFORE any refactor that relocates it, and PONY's pure deletions land BEFORE the ARCH refactor that rewrites the same file* — so the shared test files are rewritten once, not twice.
- **Per-commit gate (non-negotiable):** Rust → `cargo test --manifest-path src-tauri/Cargo.toml`; frontend → `pnpm check` + `pnpm vitest run` + `pnpm build`; both → all four. `Cargo.toml` edits regenerate and commit `Cargo.lock`; `package.json` edits run `pnpm install`. Test collateral lands in the same commit as the source change.
- **Path corrections carried throughout** (both audits cite wrong paths): ADR-0008 is `docs/adr/0008-codemirror-live-preview-architecture.md`; ADR-0026 is `docs/adr/0026-type-definitions-relationships-lifecycle.md`; `project_note_record`/`get_all_property_records`/`create_note`/`get_all_vault_entries_v2` live in `src-tauri/src/commands/vault.rs` (not `vault/vault.rs`); the watcher handler is `src/lib/core/app-lifecycle/watcher-handler.service.ts`; `extractNoteName` is in `src/lib/core/filesystem/link-updater.logic.ts`; the LP plugin files live under `live-preview/plugins/`; the stale-comment file for B1 is `src/lib/core/app-lifecycle/index-updater.service.ts`.

---

## Overlap map

| Cluster | ARCH | PONY | Relation | Resolution (one line) |
|---|---|---|---|---|
| C01 | 1.0, 1.1 | #64 | conflict | **Wire wins:** ship `inlineExtensions(disabledDecorators)` plumbing now; #64 dropped as refuted by construction. |
| C02 | 6.0, 6.2, LB8 | #21 | conflict | **Wire wins:** #21 lands reduced (dead accessors only); the two clears are wired into `deleteItem` + teardown, one commit. |
| C03 | 0.0, 3.1, 7.0 | #23 | conflict | **Wire wins:** strike `removeNoteFromIndex`/`removeRecord`/`flushScheduledTagIndexRebuild` from #23's cut; ARCH wires them (closes M08). |
| C04 | 4.1, 4.2 | #29 | conflict | **ARCH 4.1 wins:** keep the 7 clamp wrappers (2nd caller = `normalizeSettings`); #29 ships shrunk (`utils/clamp.ts` + one-line bodies). |
| C05 | 6.1, 6.2 | #20, #26 | conflict | **PONY wins on `isInsideSystemFolder` (delete it, port 3 edge-case tests); rest composes** — one surviving inbox predicate `getInboxCount`; do NOT rewire `countNavItems`. |
| C06 | 3.1, 5.1, 2.0, 2.1, 2.2 | #15, #44, #54 | sequencing | Deletions first (#54 → #44 → #15, corrected ranges); ONE note-change module (3.1's shape; 5.1's forgetNote = first slice, writeNote = upsert branch). |
| C07 | 1.0, 1.1, 1.2 | #1, #10, #8, #16, #24, #36, #41, #58, #2 | sequencing | Pure deletions → #24/#16 merges → arch 1.0 (two commits) → #8 re-scoped → arch 1.1 fold last (freeze-gated). |
| C08 | 0.0, 0.1, 3.1, 5.1, LB5, LB6 | #39, B2, #51, #27, #59 | sequencing | B2+#39 first, then one path-helper change into existing `src/lib/utils/path.ts`, then forgetNote → 3.1 → 0.0 last. |
| C09 | 4.0, 4.1, 4.2, LB4, 1.0 | B1, #65, #63, #49 | sequencing | LB4 standalone fix first; pony deletions (B1+#65, #63, #49) before 4.0 → 4.2 → 4.1. |
| C10 | 4.0, 3.0 | #47 | sequencing | #47 first (straight −23, no prose move), arch 3.0 second (kills the third stale throttle comment in `+layout.svelte:76-80`), 4.0 last. |
| C11 | 7.0, 5.0, 0.0 | #9, #25 | sequencing | Decide 7.0's producer shape (option 2: `vault-index-updated` listener seam) → #25 → #9 (keep `properties_index` write-only) → 7.0 → 0.0's `rename_note` last. |
| C12 | 2.0 | #28, #57 | sequencing | One ADR-0017 rewrite, owned by #28 (preserve Decision item 5); arch 2.0 needs no ADR edit; #57 not applied. |
| C13 | 2.2, 0.0, 3.1 | #38 | sequencing | #38's call sites take `openFileInEditor` directly and stay there — calendar has no position target; not a 2.2 caller. |
| C14 | 3.0 | #55 | sequencing | Apply #55 (delete `:36-40`, not `:38-42` — file is 40 lines); correct arch 3.0's collision rationale, keep its conclusion. |
| C15 | 5.1, 2.0, LB2 | #43 | same-file | #43 first (zero test churn), then LB2 regression test + 2-line sync fix inside `frontmatter-icon.service.ts` (not file-icons.service — `newContent` is local there). |
| C16 | 2.2, 2.1, LB1 | #27 | same-file | LB1 standalone fix first; #27 flips only 4 sites (file-history's 3 stay — SQLite snapshot keys); arch 2.2 subsumes the conversion later. |
| C17 | 1.1, 7.0 | #19 | same-file | Zero symbol overlap; #19 as written (port the 5 ADR-0029 yaml-quoting `it`s to `serializeProperties`). |
| C18 | 5.0 | #23, B1 | same-file | Thinner than mapped — 5.0 never edits `tags.service.ts`; B1 fixes its stale comment; no sequencing needed. |

---

## Conflicts resolved

### C01 — decorator toggles: WIRE, drop #64
Every one of the six "dead" names maps onto handlers already in the registry (`inline-extensions.ts:25-47,57`); `inlineExtensions(disabled)` is ~15 lines. CLAUDE.md:241, the ADR-0008 Consequences bullet, and the **open freeze hunt** (`tasks/todo/audit-vault-and-freeze.md:9-10`, candidate causes a/e are inline-handler paths) all depend on inline isolation. After the wire, all 12 names in `help/documentation/19-settings.md:245` become true — #64's "mandatory doc edit" inverts into "no doc edit needed". Keep #64's two corrections: stale persisted keys are harmless via `settings.service.ts:143-146` (a pre-wire toggle silently becomes honored — one line in the commit message).

### C02 — view-parse-cache clears: WIRE, #21 lands reduced
`app-lifecycle.service.ts:411-415` clears five peer caches on teardown but never this one; `deleteItem` clears the dedupe entry but leaves a deleted `.view`'s parsed definition resident. Keeping `clearAllViewParseCache` also dissolves #21's own HIGH risk (it's the test harness's only `parseCache` reset at `view-parse-cache.test.ts:23`). Delete only `queryResultCache` + `getCachedViewYaml`/`setViewQueryResult`/`getViewQueryResult`; wire the clears in the same commit.

### C03 — collection eviction: WIRE, #23 shrunk to three exports
Both audits agree on the facts; #23's own risk block concedes deletion makes M08 (`tasks/todo/bug-hunt-fixes.md:47`, phantom collection pages) permanent. `removeNoteFromIndex`/`removeRecord` get wired into `fs.service.ts:188/:229/:269` and `watcher-handler.service.ts:156` by the ARCH work. Also strike `flushScheduledTagIndexRebuild` (live test seam, 3 call sites). Revised #23 = `addSort`, `finishAllEditing`, `isDurationString` only.

### C04 — settings clamps: keep the wrappers, shrink the bodies
ARCH 4.1's `normalizeSettings` (run at `settings.service.ts:120` and before the stringify at `:194`) is a second, non-markup caller for every clamp — inlining into `.svelte` markup would duplicate all seven bounds and lose unit-testability (#29 concedes both). #29 ships as: `src/lib/utils/clamp.ts` + test, seven one-line bodies (contentWidth keeps its `<= 0` sentinel in `.logic.ts`), and the shared helper applied at the 16 nested `Math.max/min` sites in 9 non-settings files + `clampRetentionDays`. `settings.logic.test.ts:15-152` stays green unchanged — that's the behaviour-neutrality proof.

### C05 — `isInsideSystemFolder`: DELETE (pony wins); the rest composes
Zero production callers (only its own definition at `type-sidebar.logic.ts:57`); ARCH 6.2's proposed dedup would move two regexes per entry inside the filter loop at 4 call sites and forfeit the same-array early return at `:81`. Port the 3 unique edge cases (test `:1078/:1083/:1097-1098`) into the surviving `excludeSystemFolder` describe. Corrections to ARCH 6.2: leave `countNavItems`' single-pass inline counter alone; the ADR file is `0026-type-definitions-relationships-lifecycle.md`. The `dock-inbox-badge.md` Task 7 gate is soft — the only artifact it needs (`getInboxCount` coverage at `inbox-workflow.logic.test.ts:41-49`) is explicitly preserved.

### C12 corollary — #57 REFUTED
ADR-0017 is `status: active` and names `getWatcherCounters()` verbatim (line 45); the proposed "tests assert effects" swap would simply vanish eight counter-only assertions. Not applied. Re-decide only after #28's ADR rewrite, and then only as the honest larger variant (delete the whole counters block + assertions), never as proposed.

---

## Prioritized execution order

### P0 — Live-bug surgical fixes (standalone commits, regression test first, independent of all refactors)

| # | Item | Source | Do | Gate |
|---|---|---|---|---|
| P0.1 | Sidebar toggles don't persist | **LB4** (arch 4.0 pre-fix) | One-line persistence at `AppShell.svelte:102/:109` + regression test. Must precede 4.0. | frontend |
| P0.2 | Semantic/hybrid jump broken | **LB1** (arch 2.2 pre-fix) | Convert 1-indexed `lineStart` to a char offset (`doc.line(n).from`) at `SearchResult.svelte:102/:109` + failing-test-first. Arch 2.2 later subsumes into the typed target. | frontend |
| P0.3 | Icon frontmatter data loss | **LB2** + **#43** + **arch 2.0** (fully discharged here) | Commit series: (a) #43 delete `setFrontmatterIconColor` (`frontmatter-icon.service.ts:49-59`, zero test churn); (b) regression test: dirty tab + `setIconForPath` keeps `_icon`; (c) `syncExternalContentToEditor(filePath, newContent, true)` after the two `writeTextFile`s **inside frontmatter-icon.service.ts** (not file-icons.service — `newContent` is local); (d) flip `type-definitions.service.ts:149` `markSaved` → `true` (prescribed by both 2.0 and 2.1 — land ONCE). No ADR-0017 edit. | frontend |
| P0.4 | Second vault never shows "Indexing…" | **LB3** + **arch 3.2** (part 1) | Additive `indexReady` on `vault.store.svelte.ts` (set in `bumpVaultIndexVersion`, cleared by a new reset from `teardownVault`); swap the two `vaultIndexVersion === 0` guards. **Never reset the counter** (completion.ts monotonicity). Test: teardown → false → bump → true. Part 2 (entries snapshot) merges into arch 5.0, P4. | frontend |
| P0.5 | Filter round-trip corruption | **LB7** + **arch 7.2** (reduced) | Emitter-side fix: quote non-numeric relational values at `filter.logic.ts:60-67` (covers both `.collection` and `.view` write paths); optional exhaustive `Record<FilterOperator>` round-trip test (zero production change). File next to L34. | frontend |
| P0.6 | createFile indexes empty content | **LB5** + **arch 0.1** (reduced — merge dropped) | Optional `content` param on `fs.service.createFile`; delete the empty-then-overwrite two-step + comments at the 5 callers (canvas, kanban, collection, type-definitions, note-creator). | frontend |
| P0.7 | Full-doc string alloc per rebuild | arch 1.1 sub-item | The `meta-bind-input-plugin.ts` `doc.toString()` one-liner — independent of the fold. | frontend |

### P1 — Conflict decisions that unblock everything (one small commit + recorded decisions)

1. **C01 commit:** `inlineExtensions(disabledDecorators)` plumbing (~15 lines, name→handlers table + two array filters), called from `live-preview.ts:64`. Revives all six toggles + keeps `metaBindInput`. → **#64 DROPPED.**
2. **C06/C03 decision:** the single note-change owner is **arch 3.1's `applyNoteChange({kind:'upsert'|'delete'})`**; arch 5.1's `forgetNote` is its first shipped slice, `writeNote` its upsert branch. Never build both. Amend #23's cut list per C03.
3. **C11 decision:** arch 7.0 producer = **option 2** (refresh `collectionStore` from the existing debounced `vault-index-updated` listener at `tauri-listeners.service.ts:97-131`) — touches no Rust commands, makes #9 trivially safe.
4. **C02/C04/C05/C12 decisions:** as resolved above (wire clears; keep clamps; delete `isInsideSystemFolder`; one ADR-0017 rewrite owned by #28; #57 never as written).

### P2 — Safe deletion batch (CONFIRMED + corrected small PARTIALs untouched by ARCH refactors; grouped commits, each with its test collateral)

**Deps/config** (per-commit gate as listed):
- **#4** `codemirror` meta-pkg; **#5** `@fortawesome/fontawesome-svg-core`; **#6** `@types/katex` — package.json + regenerated lock, frontend gate each.
- **#68** `percent-encoding` (keep the `# File History` comment; `similar` is live) — cargo + Cargo.lock.
- **#69** `objc2-core-foundation` — own commit; **real `cargo build --release` validation** (27 transitively-enabled CF features flip off); the four macOS-gated `fonts.rs` tests must pass.
- **#53** vite timestamp one-liner; **#11** `git rm scripts/settings-watcher.py`; **#22** delete `components/ui/label/`; **#30** `sanitizeMathHtml` (adopting it would be a regression); **#66** `ColorPresetName`; **#70** `iconCount` (2-line delete incl. JSDoc).

**Rust one-liners** (cargo gate):
- **#48** inline `split_once` chain; **#60** method-form `is_char_boundary` (keep the emoji comment); **#34** primary `find("```")` form ONLY (the `windows(3)` variant corrupts offsets); **#25** 4-line wrapper over `extract_outgoing_links`, keep `.filter(!is_empty)` — **must precede #9**; **#56** `with_batch_size` + fix the `semantic_reranker_test.rs:3-9` module doc; **#46** three edits (`search_index.rs:198-207`, `lib.rs:321`, `search_fts_test.rs:231-245` — the "no test reference" claim is false); **#12 + B3** together (`debug_semantic_embeddings` + `generate_handler!` at `lib.rs:333` + the `get_sample_chunks`/`ChunkSample` cascade).
- **#7 + #33** paired: `search_vault`/`text_search` + 306-line test + the four module decls + ADR-0011:41; then the `VaultSearchMatch` TS mirror (leave the e2e mock's own copy alone). cargo + frontend.

**Date/periodic surface, strict order** (shared files — delete by symbol, not line range):
- **#13 + B4** first (four dead exports + the compat re-export; patch test imports/describes) → **#61** (`parseDate` + its import at `date.test.ts:3`) → **#62** (delete `today()`, rewrite call sites as `formatNow(...)` — do NOT loosen `formatNow`'s signature) → **#32** (inline the three wrappers; rewrite the two live expectation helpers at service test `:75/:270`).

**Editor files, strict order (C06 steps 1–3, before any arch editor refactor):**
- **#54** `flushPendingSaves` (correct range `:191-195`) → **#44** `clearRecentSaves` (**corrected commit message** — the watcher deliberately does not clear, per `watcher-handler.service.ts:43-45` and ADR-0017 item 5) → **#15** transform hooks (**corrected tail `:119-148`** — never touch `notifyAfterSave` at `:150+`; collapse `editor.service.ts:139-145` to bare `writeTextFile`; reword `docs/perf/baseline-template.md:23`; note it reverses 80abf50).
- **#55** `closeVault` — delete `vault.service.ts:36-40` (file is 40 lines; the stated `:38-42` overshoots EOF); edit arch 3.0's collision rationale out, keep the "already implemented and tested" leg.

**Settings surface (before P3 Track D):**
- **B1 + #65** one commit (bf55080 fallout): delete `TagsPanel.svelte`, stale comments at `tags.service.ts:88` and `app-lifecycle/index-updater.service.ts:25`, `tagsVisible` (`settings.types.ts:62-63`, `settings.store.svelte.ts:59`), three e2e seeds, CLAUDE.md Indexing §2/`:264`. Drops arch 4.0's census from 10 to 9 modules.
- **#63** `SETTINGS_SECTIONS` (frees the name arch 4.2 cited); **#49** `isValidFolderName`; **#47** inline `shouldAutoCheckNow` (straight −23 delete; fix stale throttle clauses at `:45-46` and `help/documentation/19-settings.md:257`; RELEASE-CHANNELS.md already carries the rationale); **#29-shrunk** per C04; **#17** `Reflect.has` (keep the symbol branch at `:63-65`; reword 5 stale test mentions).

**Type-definitions/inbox/properties:**
- **#26 then #20** in one series (#26 first so `getInboxCount` is established as the sole inbox predicate before its `countInbox` twin is deleted; + **arch 6.2**, discharged here + in the C02 commit): delete the inbox trio + `countInbox` + `formatDatePair` + `isInsideSystemFolder` (C05); port 3 edge cases; getInboxCount survives as sole predicate; amend `docs/adr/0026-type-definitions-relationships-lifecycle.md:41` in-series; drop the unused `untrack` import in `TypeNoteList.svelte`.
- **C02 commit** — **#21-reduced + LB8**: delete `queryResultCache`/`getCachedViewYaml`/`setViewQueryResult`/`getViewQueryResult`, narrow `TypeSidebar.svelte:32`, clean the perf-test mock; wire `clearViewParseCache` into `deleteItem` beside `:184` and `clearAllViewParseCache` into teardown after `:415`, same commit.
- **#23-reduced**: `addSort`, `finishAllEditing`, `isDurationString` only (+ matching describes).
- **#31** lifecycle-filter trio (+ its ~35 test lines); **#19** properties trio (**port the 5 yaml-quoting `it`s at test `:668-698` to `serializeProperties` — ADR-0029's only emitter guards**); **#42** `mapPriorityFromTodoist` (cut `:30-52`, not `:41-52`); **#50** `getContrastTextColor` (cut `:26-37`); **#35** `extractAliasesFromContent` (file survives); **#40** `removeThemeOverrides`; **#45** kanban strip* twins; **#71** fuzzy-match barrel (re-point `completion.logic.ts:2`; ~48-line duplicate-test win).

**Calendar / LP / misc:**
- **#38** calendar wrappers — inline at `CalendarPanel` with a direct `openFileInEditor` import; **keep** the `openOrCreatePeriodicNoteForDate` import (Dayjs-vs-dateKey naming trap). Permanently NOT an arch 2.2 caller (C13).
- LP small deletions: **#2** debug-composition (+ 4 dead lines in editor-extensions + 3 prose comments; note the contrary judgment in `tasks/done/negative-patterns-remediation.md:21`); **#36** 5 Decoration constants + 2 CSS rules (keep `.cm-lp-code`); **#41** `HorizontalRuleWidget` + `.cm-lp-hr` (keep the `:15` import); **#58** `core/types.ts` (fold `Line` into the existing import, fix the jsdoc); **#18** canvas helpers — **corrected range 374-408 only** (`:411` is the LIVE `duplicateNode`), + the mandatory test-import surgery.
- **#52** reuse `formatDate(date,'YYYY-MM-DD')` — **never** `toLocaleDateString('en-CA')` (WKWebView ICU drift empties the calendar); no cross-layer import.
- **arch 7.1** (reduced): `persistState`/`persistCalendarConfig` end in `commitStructural` (~6 mechanical lines) + copy the no-op pre-guard into `KanbanView.applyChange` (fixes M27).
- **arch 1.2** (reduced): fix the false cross-reference comment at `queryjs-block-widget.ts:95`. Barrel deferred (P5).
- **#28**: drop the `tauri-plugin-fs` watch feature + `capabilities/default.json:223-242` **in one commit** + Cargo.lock + the **full ADR-0017 supersede/rewrite** (preserve Decision item 5 — it's the load-bearing citation behind arch 2.0's refutation; the ADR is stale about the JS watcher regardless). Gate: cargo test **+ a real `pnpm tauri build`** (ACL behavior is unverifiable read-only). Optional e2e mock cleanup rides along.

### P3 — ARCH Strong refactors in dependency order, interleaved with same-file PONY work

**Track A — Live preview (C07):**
1. **#1 + #10** together, #1 first: delete the 11 parsers (+911 own-test lines) and the dead `link.ts`/`math.ts` halves; do the 13 `combined-*.test.ts` surgeries ONCE. No doc edits (ADR-0008 already describes the unified pipeline).
2. **#24** meta-bind widget merge — merged `eq()` **must compare input type/opts** (class identity is today's only barrier to number↔date widget reuse); **#16** math widget merge — cache key **must include `displayMode`**, keep the span/div swap and the conditional empty-formula guard; update `app-lifecycle.service.ts:75-76/414-415` + CLAUDE.md:231.
3. **arch 1.0 commit A**: `core/block-decorator.ts` + collapse the eleven block ViewPlugin bodies (paths under `live-preview/plugins/`); separate `settingsKey`/`profileLabel`; callout via `rebuildOn:[toggleCalloutFold]`, queryjs via a narrower gate.
4. **arch 1.0 commit B**: registry + settings-toggle unification (names-only export so TroubleshootingSection pulls no katex/mermaid/DOMPurify); grows `DECORATOR_NAMES` and `help/documentation/19-settings.md:245` from 12 to ~22 names; amend `docs/adr/0008-codemirror-live-preview-architecture.md` §55/§74 + CLAUDE.md perf rule 4 in-series. The 10 new toggles are an intentional feature change.
5. **#8 re-scoped**: arch 1.0 already deleted both duplicated plugin bodies — remaining merge is parser + widget + CSS only (~half the claimed surface); keep derived `cm-lp-${tag}` classes (width vs maxWidth differ); preserve the four audio-only test cases; re-run `embed-widgets.spec.ts`.

**Track B — Editor/save (C06 steps 4–7; requires P2 editor deletions):**
1. **arch 2.1**: `schedule:'frontmatter'|'body'|'none'` param on `syncExternalContentToEditor` + external-edit annotation (same shape as `isTabSwitching`); audit `restoreSnapshot`'s silent no-op when `editorView` is null (`file-history.service.ts:82-105` — it takes no position arg; not a 2.2 caller). The `markSaved` flip was already taken at P0.3 — do not repeat.
2. **arch 2.2** (Strong): `openNoteAt(path, target: {kind:'offset'}|{kind:'line'})` in core/editor owning the await/branch/rAF/clamp/focus; absorbs `toc.service.ts::scrollToHeading` and subsumes P0.2's conversion into the type system. `pendingScrollPosition` and `editorStore.editorView` stay public.

**Track C — Filesystem/paths (C08):**
1. **B2 + #39** one commit: `sortTree` + the dead sort-option feature (decide the orphaned `sortBy` store state in the same commit); heavy `fs.service.test.ts` churn paid before ARCH adds lines.
2. **#51 + #59 + #27** as ONE change into the **existing** `src/lib/utils/path.ts`: `stem()` (5 copies) + `relativePath()` (4 copies); flip exactly 4 call sites (`search.logic.ts:171`, `SearchResult.svelte:64`, `QuickSwitcher.svelte:82`, `RelationshipSearch.svelte:79`); **file-history's 3 sites stay unflipped** (SQLite snapshot keys, ADR-0021); reconcile the sibling-prefix and `filePath===vaultPath` deltas first; keep a named `extractVaultName` wrapper.
3. **arch 5.1 slice 1 — `forgetNote`** (`clearIndexedEntry` + `remove_note_from_index` at `fs.service.ts:229/:269`, which today omit what `:184` does): closes **LB6** / the ADR-0009:63 violation (named enforcer `removeFileFromIndex` no longer exists — amend the ADR here); drop the `vi.mock` at `fs.service.test.ts:40-42`.
4. **arch 3.1 — `applyNoteChange`** (the ONE module, per P1 decision; absorbs 5.1's writeNote as the upsert branch, `afterSaveObservers` deliberately not fired from writes, auto-move not delegated): prerequisites **write `removeFrontmatterIconForFile` and `removeCalendarForFile` first** (they don't exist); one nullable FTS-key derivation built on Track C step 2's helper (watcher semantics: skip, never fall back to absolute); registration API instead of core→features imports (ADR-0003); wire all eight call sites; includes the collection `removeNoteFromIndex` wiring saved in C03 (closes M08); amend ADR-0009 + ADR-0003 in-series. Route the frontmatter-icon/file-icons/link-updater/type-definitions/kanban/collection writers through the upsert branch.

**Track D — Settings (C09/C10/C04; requires P2 settings deletions + P0.1):**
1. **arch 3.0** (narrowed): move only `autoOpenDailyNote` + `maybeAutoCheckForUpdates` into `initializeVault` after Step 8, behind the `initVersion` guard, same `setTimeout(...,0)`; deletes `+layout.svelte:65-84` incl. the third stale throttle comment (`:76-80`) neither audit listed; preserve the loadSettings rejection path/toast; rationale rewritten post-#55.
2. **arch 4.0** (Strong): delete `onchange` from all 17 sections + the 13 non-panel `saveSettings` imports; `startSettingsPersistence()` in settings.service via `$effect.root`, started/stopped by initialize/teardownVault, explicit `flush()` on quit; serialize inside the effect. Budget the ~14 mocked-saveSettings assertion rewrites. Now 9 modules, not 10 (B1). It also deletes `update-check.service.ts:4` and `:66-70` — superseding, not conflicting with, #47's second comment fix.
3. **arch 4.2**: `sectionComponents: Record<SettingsSection, Component>` map beside `sectionIcons`, delete the 35-line if/else; no new module, no derived union (`.svelte`-import constraint survives even though #63 freed the name).
4. **arch 4.1**: step 1 clamp-on-commit (`onchange` DOM attr — safe now that 4.0 removed the shadowing prop) as a standalone bug-fix commit with regression test; step 2 `normalizeSettings` wired at load + before stringify; **never clamp inside `settingsStore.updateEditor`**. The 110-line merge collapse → P5.

**Track E — Rust index (C11; after P1 decision + #25):**
1. **#9** full corrected checklist: 5 commands + 5 registrations + 3 index.rs lookups + **all 19 test fns** (compile-breaking otherwise) + 3 unused imports + CLAUDE.md:262 + `vault-v2.types.ts:65` + stale comments/'Phase 8' header + e2e mock handlers. **Do NOT delete `properties_index`** — accept it write-only.
2. **arch 7.0**: producer option 2 (listener seam, keeping the synchronous per-file path for editor.hooks/note-creator); `collectionStore` version counter re-keying `collection-block-widget`'s `cacheKey` (closes **M12**); `removeRecord` wired at all four removal sites (with Track C — closes **M08**; single owner, reuse forgetNote); **queryjs half dropped** (ADR-0010 / perf rule 9: live-DOM cache is never version-keyed).

### P4 — Worth-exploring variants and heavy-collateral PARTIALs

- **arch 5.0** (half): version-keyed `entries()` memo over `get_all_vault_entries_v2` (folds `completion.ts`'s cache, explicit invalidation on vault open/close — fixes the ~300 ms wrong-vault completion window and per-widget refetch) + `versionGated`/`isStillCurrentPath` extraction in `utils/inflight.ts`; **absorbs arch 3.2 part 2**; drop the subscribe(fetch,apply) half; document as cache-not-mirror in ADR-0025.
- **arch 6.0** (remaining after the C02 commit discharged its cache wiring): TypeNoteList effect reads `propertyIndex`/`isIndexReady` synchronously; reset `seededViewPath` on contentHash change; fix the stale comment. No store+service pair (ADR-0004).
- **arch 6.1** (reduced): three `getEntryByPath` swaps + `sortViewFiles(Map)` comparator (the only genuine hotspot); memoized `visibleEntries` optional. Rebases free over the C05 deletes.
- **arch 0.0** (last of the fs track): `applyPathChange({from,to,isDirectory})` owning ordering (disk-op callback so `deleteItem` keeps `closeTabsForDeletedPath` before `moveToTrash`); folder-prefix walk once; wires the icon/calendar/collection per-path removals built in Track C; **Rust `rename_note` written against the post-#9 `index.rs`**; amend ADR-0009/ADR-0025.
- **#3** task-metadata parser: delete source + own test, patch `makeTask`, rewrite the 8 `parsing.rs` parity citations, re-file the dependsOn regex bug against `parsing.rs:1310-1316`; keep `task-metadata.types.ts`.
- **#14** KBDateTime: delegate only startOf/endOf/weekNumber/quarter/hasSame/toISODate; **keep `toFormat` hand-rolled** (dayjs REGEX_FORMAT corrupts user format strings); add the plugin extends; pin plus/minus clamping with a new test first.
- **#37** kb-ui preamble: ~6-field helper (not the 3-field bag — doesn't compile), per-entry list not keyed map, `weekStartDay` caller-local; 29 assertions guard it.

### P5 — Deferred / not applied

| Item | Status |
|---|---|
| **#57** | **REFUTED — do not apply.** Re-decide after #28's ADR-0017 rewrite, and then only the honest full-counters-block deletion, never the as-proposed swap. |
| **#64** | **Dropped** — refuted by construction after P1's C01 plumbing (all 12 names live again). |
| **#67** | Deferred pending the `feature/p2p-sync` branch decision (tip 4b64de7 uses `Uuid::new_v4()`; deleting strands the branch at merge). |
| **arch 1.1 fold** (the four inline ViewPlugins) | Gated on `tasks/todo/audit-vault-and-freeze.md` 0.2/0.3 closing (both unchecked) — the fold collapses the last four LP-TRACE labels the freeze hunt needs. Add the two-overlapping-range + `![[img.png]]` tests before merging; keep per-handler profile labels. |
| **arch 1.2 barrel** (`clearAllWidgetCaches`) | Optional; if taken, after #16 (three clears remain, not four) + one teardown test. |
| **arch 4.1 merge collapse** (110-line generic merge) | Optional separable follow-up, zero correctness value — take last or never. |

---

## Sequencing constraints honored

- **Live bug before relocating refactor** (global constraint): LB4→P0.1 before 4.0; LB2 clobber→P0.3 before 5.1/3.1; meta-bind one-liner→P0.7 before the 1.1 fold; clamp-on-commit is its own bug-fix commit inside Track D; LB1→P0.2 before 2.2.
- **#1 before #10, same commit series** — 13 combined test files edited once (P3-A1).
- **#51/#59/#27 as one change, home `src/lib/utils/path.ts`** (which already exists) — P3-C2; file-history's 3 sites never flipped (ADR-0021 snapshot keys).
- **#13 → #62; #13+#32 delete by symbol; B4 rides #13; #61 same files** — P2 date group order.
- **#25 before #9; #9 before arch 7.0; arch 0.0's `rename_note` after #9** — Tracks E and P4; 7.0's producer shape decided at P1 before any Rust cut.
- **#20+#26 together** (surviving predicate = `getInboxCount`, dock-badge's import) + ADR-0026:41 in-series; dock-inbox-badge Task 7 gate treated as soft (its needed coverage is preserved).
- **Pairs kept:** #7+#33, #12+B3, #39+B2, #65+B1, #28 owns the single ADR-0017 rewrite (#57 not applied; arch 2.0 needs no ADR edit).
- **C06 order** #54 → #44 → #15 → arch 2.1 → forgetNote → 3.1; #44's commit message corrected (no "watcher should clear" story); #15 stops at `:148`, `notifyAfterSave` untouched.
- **One note-change owner** (0.0/3.1/5.1/7.0 overlap): forgetNote first (P3-C3), reused by 3.1 (P3-C4), by 7.0's removeRecord wiring (P3-E2), and by 0.0 (P4) — never three competing owners.
- **ADR amendments same-series:** ADR-0008 §55/§74 + CLAUDE.md rule 4 (1.0-B); ADR-0009:63 (forgetNote/3.1); ADR-0026:41 (#20/#26); ADR-0025 (5.0, 0.0); ADR-0017 (#28, Decision item 5 preserved); ADR-0011:41 (#7). ADR-0003 inversion avoided via registration API (3.1). ADR-0004: no preemptive store+service pairs (6.0, 4.2). ADR-0007: `openOrCreateNote` seam not widened (0.1 reduced). ADR-0010/perf rule 9: queryjs live-DOM cache never version-keyed (7.0 queryjs half dropped).
- **Counter monotonicity:** `indexReady` is additive; `vaultIndexVersion` never reset (P0.4).
- **4.0 before 4.1 step 1** — the sections' `onchange` prop would shadow a DOM `onchange=` attribute.
- **1.1 gated on freeze 0.2/0.3; C01 plumbing split out precisely because it is NOT gated.**
- **Gates:** ~14 mocked-saveSettings rewrites budgeted in 4.0; Cargo.lock regenerated for #28/#67/#68/#69; #28 additionally proven by a real `pnpm tauri build`; #69 by a real release cargo build; every multi-commit finding pays the per-surface test gate per commit.

---

## Non-overlapping remainders

**ARCH-only (no cluster):**

| Id | Action (post-correction) | Phase |
|---|---|---|
| 3.2 | Additive `indexReady` flag (pt 1); entries-snapshot leg merged into 5.0 (pt 2) | P0.4 / P4 |
| 7.1 | commitStructural latch consolidation + KanbanView pre-guard (M27) | P2 |
| 7.2 | Emitter-side quoting fix (= LB7) + optional round-trip test | P0.5 |
| LB3 | Guard swap via indexReady | P0.4 |
| LB7 | Quote non-numeric relational values | P0.5 |

**PONY-only (no cluster):**

| Ids | Phase |
|---|---|
| #4 #5 #6 #11 #17 #22 #30 #31 #32 #33(w/#7) #34 #35 #40 #42 #45 #46 #48 #50 #52 #53 #56 #60 #61 #62 #66 #68 #70 #71 B3(w/#12) B4(w/#13) | P2 |
| #7 #12 #13 #18 #69 | P2 |
| #3 #14 #37 | P4 |
| #67 | P5 (deferred) |

Clustered PONY items also executed in P2 outside the tracks: #19 (C17), #28 (C12), #29-shrunk (C04), #47 (C10).

All 75 PONY findings, all 22 ARCH findings, and LB1–LB8 are assigned exactly once above; the only items never applied are #57 (refuted) and #64 (superseded by the C01 wire).