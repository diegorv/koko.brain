# Upgrade Tauri to 3.0.0-alpha

Stage the Tauri 2 -> 3 upgrade on branch `update-tauri-to-v3` so the cost is
known and the branch is ready to follow upstream to beta/stable. Evidence and
rationale: `discovery-tauri-v3-2026-09-15.md` in this folder. The branch is not
merged while v3 is alpha (merging to `main` auto-ships to nightly users).

## Tasks

- [x] Task 1: Record the Tauri v3 discovery report and this plan
- [x] Task 2: Rust side with exact pins: `tauri =3.0.0-alpha.1`, `tauri-build =3.0.0-alpha.0`, `tauri-runtime-wry =3.0.0-alpha.1` with `macos-private-api`, nine plugin crates `=3.0.0-alpha.0`, refreshed `Cargo.lock`. Select the wry runtime in `lib.rs`, replace `tauri::Wry`, add a source-guard test (`src-tauri/tests/lib_test.rs`) that fails when the `.runtime(...)` call disappears from the builder chain, refresh the stale `AppHandle<Wry>` test comments and the tauri version in the glib advisory note. Gate: `cargo test`
- [x] Task 3: npm side with exact versions, no caret: `@tauri-apps/api` and the eight `@tauri-apps/plugin-*` at `3.0.0-alpha.0`, `@tauri-apps/cli` at `3.0.0-alpha.1`, refreshed `pnpm-lock.yaml`. Gate: `pnpm check` + `pnpm vitest run` + `pnpm build`. Until this lands the CLI version-mismatch check fails `tauri build`; `tauri dev` only logs it
- [ ] Task 4 (blocked on upstream beta/stable): re-pin to the release (drop the exact pins), then walk the manual QA checklist in the discovery report against that build, amend ADR-0001, update the "Tauri 2" mentions in CLAUDE.md, decide the Dependabot cargo cooldown exemption for `tauri*`

## Notes

- Exact `=` pins cover only the direct dependencies. tauri, tauri-runtime-wry,
  tauri-macros, tauri-build and the plugins require tauri-runtime /
  tauri-utils / tauri-macros / tauri-codegen / tauri-plugin with
  `~3.0.0-alpha.0` or `3.0.0-alpha.0`, which accept later alphas, and open PR
  #16023 on tauri `v3` breaks both the alpha.1 crates (`run_iteration`,
  `Error::CreateWindow`) and the published opener/dialog 3.0.0-alpha.0
  (`js_init_script` rename). The committed `Cargo.lock` is the real guard: do
  not run a bare `cargo update` on this branch until upstream republishes.
- npm `latest` is still 2.x for every `@tauri-apps/*` package (3.x is on
  `next`), so a bare `pnpm add @tauri-apps/<pkg>` pulls 2.x: always name the
  exact `@3.0.0-alpha.x` version. The `@tauri-apps/api` 2.11.1 copy under the
  plugin-* packages is expected until they depend on api ^3; no object crosses
  between the two copies in this app.
- Keep `macos-private-api` on BOTH `tauri` and `tauri-runtime-wry`; the
  migration guide's snippet fails `tauri-build`'s manifest check for this app.
- Stay on wry. CEF is rejected (origin change orphans localStorage, bundle
  size, ADR-0001).
- Smoke launch 2026-09-15, with Tasks 2 and 3 both in the working tree:
  `pnpm tauri dev` ran for about 3 minutes, opened the owner's vault, served
  IPC commands and events, and started semantic indexing, with no panic, no
  plugin error and no version-mismatch log. The composer, menu, global
  shortcuts and updater were not exercised; they stay in the QA checklist.
- Owner decisions 2026-09-15: report committed on this branch, exact pins,
  push the branch and open a PR, guard the runtime selection with a
  source-level Rust test (no gate launches the app, so a dropped `.runtime`
  call would otherwise pass every check and panic at startup).
